---
title: "Inverse Contextual Bandits without Rewards: Learning from a Non-Stationary Learner via Suffix Imitation"
title_zh: 无奖励的逆上下文多臂老虎机：通过后缀模仿从非平稳学习者中学习
authors: "Yuqi Kong, Xiao Zhang, Weiran Shen"
date: 2026-03-04
pdf: "https://arxiv.org/pdf/2603.03778v1"
tags: ["query:irlioc"]
score: 9.0
evidence: 从观察到的动作中恢复底层参数
tldr: 本研究探讨了逆上下文多臂老虎机（ICB）问题，即观察者在无法获取奖励信息的情况下，仅通过观察学习者的动作来恢复其底层参数。针对学习者从探索到利用的非平稳行为挑战，提出了“两阶段后缀模仿”框架，通过舍弃初始阶段数据并对后续数据进行经验风险最小化。研究证明，即使在信息缺失的情况下，观察者也能达到与奖励感知学习者相当的收敛速度，实现了从纯动作数据中有效恢复最优策略。
selection_source: fresh_fetch
motivation: 旨在解决在逆上下文多臂老虎机中，由于学习者行为从探索向利用转变导致的非平稳数据建模难题。
method: 提出了两阶段后缀模仿框架，通过剔除初始预热阶段的干扰数据，仅利用后续模仿阶段的动作数据进行策略恢复。
result: "理论证明该方法在预测决策损失上达到了 $\\tilde O(1/\\sqrt{N})$ 的收敛速度，与完全掌握奖励信息的学习者渐近效率一致。"
conclusion: 该研究表明被动观察者仅凭动作序列即可有效推断出最优策略，其性能可与主动学习者相媲美。
---

## 摘要
我们研究了逆上下文多臂老虎机（Inverse Contextual Bandit, ICB）问题。在该问题中，学习者寻求优化策略，而无法获取学习者奖励且仅能观测其动作的观测者，则旨在恢复底层的参数。在学习过程中，学习者的行为自然地从探索（exploration）转向利用（exploitation），导致了非平稳的动作数据，这给观测者带来了巨大挑战。为了解决这一问题，我们提出了一个简单且有效的框架，称为“两阶段后缀模仿”（Two-Phase Suffix Imitation）。该框架舍弃了初始预热阶段（burn-in phase）的数据，并仅利用后续模仿阶段的数据进行经验风险最小化。我们推导出了一个预测决策损失界，明确刻画了由预热长度选择所引起的偏差-方差权衡（bias-variance trade-off）。尽管存在严重的信息缺失，我们证明了无奖励观测者可以达到 $\tilde O(1/\sqrt{N})$ 的收敛速率，这与完全感知奖励的学习者的渐近效率相匹配。这一结果表明，被动观测者仅凭动作就能有效地揭示最优策略，并获得与学习者本身相当的性能。

## Abstract
We study the Inverse Contextual Bandit (ICB) problem, in which a learner seeks to optimize a policy while an observer, who cannot access the learner's rewards and only observes actions, aims to recover the underlying problem parameters. During the learning process, the learner's behavior naturally transitions from exploration to exploitation, resulting in non-stationary action data that poses significant challenges for the observer. To address this issue, we propose a simple and effective framework called Two-Phase Suffix Imitation. The framework discards data from an initial burn-in phase and performs empirical risk minimization using only data from a subsequent imitation phase. We derive a predictive decision loss bound that explicitly characterizes the bias-variance trade-off induced by the choice of burn-in length. Despite the severe information deficit, we show that a reward-free observer can achieve a convergence rate of $\tilde O(1/\sqrt{N})$, matching the asymptotic efficiency of a fully reward-aware learner. This result demonstrates that a passive observer can effectively uncover the optimal policy from actions alone, attaining performance comparable to that of the learner itself.

---

## 论文详细总结（自动生成）

这是一份关于论文《Inverse Contextual Bandits without Rewards: Learning from a Non-Stationary Learner via Suffix Imitation》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究背景**：在推荐系统、临床试验等领域，上下文多臂老虎机（Contextual Bandits）是核心框架。然而，在实际应用中，奖励信号（Rewards）往往是延迟、私密或不可观测的，外部观察者通常只能获取“上下文-动作”日志。
*   **核心问题**：论文探讨了**逆上下文多臂老虎机（ICB）**问题，即观察者如何在完全没有奖励反馈的情况下，仅通过观察一个正在学习的代理人（Learner）的动作序列，来恢复底层的最优策略和环境参数。
*   **主要挑战**：学习者的行为是**非平稳的**。在学习初期，学习者为了探索会采取大量次优动作；如果观察者盲目模仿所有历史数据（行为克隆），会被早期的噪声数据误导。

### 2. 方法论
*   **核心思想**：提出了**两阶段后缀模仿（Two-Phase Suffix Imitation）**框架。其核心直觉是“少即是多”——通过主动丢弃学习者早期的探索数据，仅利用其后期趋于稳定的高质量动作进行模仿。
*   **关键技术细节**：
    *   **第一阶段（预热期/Burn-In）**：设定一个截止点 $T(N)$，观察者在此阶段仅监测但不记录数据，允许学习者通过探索逐渐收敛。
    *   **第二阶段（模仿期/Imitation）**：收集 $t > T(N)$ 之后的动作数据。此时学习者的错误率已降至较低水平（满足动态 Massart 噪声条件）。
    *   **算法流程**：
        1. 接收长度为 $N$ 的上下文-动作序列。
        2. 丢弃前 $T(N)$ 个样本。
        3. 对剩余的后缀数据执行**经验风险最小化（ERM）**，通过最小化 0-1 损失（实验中采用 Softmax 交叉熵作为凸替代损失）来拟合线性策略参数 $\tilde{\theta}$。
*   **理论保证**：论文证明了即使没有奖励，观察者的预测遗憾（Predictive Regret）也能达到 $\tilde{O}(1/\sqrt{N})$ 的收敛速率，这在渐近意义上与拥有完整奖励信息的学习者效率一致。

### 3. 实验设计
*   **场景设置**：合成随机线性上下文多臂老虎机环境。
*   **数据集/参数**：特征维度 $d \in \{20, 50\}$，候选臂数量 $K \in \{50, 100, 200\}$，总步数 $N=10,000$。
*   **Benchmark（对比基准）**：
    *   **Learner (Online Baseline)**：拥有奖励反馈的在线学习者（如 LinUCB, LinTS），作为性能上限。
    *   **Naive Imitation**：不进行预热处理，直接使用全量数据进行模仿。
    *   **Oracle Burn-in**：事后通过遍历找到最优预热长度的理想观察者。
    *   **Rule-based ($T=N^{0.9}$)**：论文提出的启发式规则，用于验证理论建议的预热长度。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或训练时长。
*   **软件环境**：提到使用了 Python 3.9, NumPy 和 Scikit-learn。由于是线性模型和合成数据，推测该实验对算力要求较低，普通的 CPU 或单张消费级 GPU 即可在短时间内完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同的学习算法（LinUCB 和 LinTS）分别进行了测试。
    *   覆盖了多种维度（$d$）和臂数（$K$）的组合。
    *   所有结果均基于多个随机种子（如 20 个）取平均值，并提供了 95% 置信区间。
*   **充分性评价**：实验设计较为充分。除了主实验外，还包含了：
    *   **消融实验**：验证了预热长度指数 $\alpha$ 对性能的影响，呈现出清晰的 U 型曲线（偏差-方差权衡）。
    *   **诊断性实验**：验证了“学习者行为随时间变得更可预测”这一核心假设。
    *   **客观性**：对比了朴素方法和理想状态下的 Oracle 方法，证明了所提规则的实用性。

### 6. 主要结论与发现
*   **信息不对称的消除**：被动观察者仅凭动作序列，就能在渐近性能上追平主动学习的学习者。
*   **预热期的必要性**：实验证实，如果不丢弃早期数据，模仿性能会因分布偏移（Distribution Shift）而严重受损。
*   **鲁棒性**：观察者对预热长度的选择具有一定的容错空间，只要满足多项式级别的增长（如 $N^{0.9}$），就能获得良好的收敛效果。

### 7. 优点
*   **理论贡献**：首次在无奖励的 ICB 设定下证明了 $\tilde{O}(1/\sqrt{N})$ 的收敛率，填补了该领域的理论空白。
*   **简洁实用**：提出的“后缀模仿”不需要复杂的逆强化学习（IRL）迭代，计算开销极小，易于在工业界日志分析中部署。
*   **视角独特**：将“学习一个学习者”建模为处理非平稳噪声标签的过程，视角新颖。

### 8. 不足与局限
*   **模型假设限制**：目前仅限于线性奖励函数和离散动作空间，尚未扩展到深度神经网络或连续动作空间。
*   **学习者依赖性**：该方法假设学习者最终会收敛（即满足无遗憾性质）。如果学习者本身算法失效或持续震荡，观察者将无法学到有效策略。
*   **超参数敏感性**：虽然理论给出了范围，但在实际有限样本下，如何在线确定最优的预热长度 $T$ 仍是一个挑战。
*   **应用场景单一**：实验完全基于合成数据，缺乏在真实世界数据集（如真实推荐系统日志）上的验证。

（完）
