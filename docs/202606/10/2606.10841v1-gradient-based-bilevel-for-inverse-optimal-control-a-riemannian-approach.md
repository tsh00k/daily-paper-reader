---
title: "Gradient based Bilevel for Inverse Optimal Control, a Riemannian approach"
title_zh: 基于梯度的双层逆向最优控制：一种黎曼方法
authors: "Ahmed-Manaf Dahmani, Vincent Bonnet, David Daney, François Charpillet"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10841v1"
tags: ["query:inverse-rl"]
score: 10.0
evidence: 基于黎曼梯度的逆最优控制
tldr: 逆最优控制旨在从观测轨迹恢复代价函数，但传统双层优化计算昂贵，近期投影方法因约束资格失效而数值不稳定。本文揭示最优轨迹满足的条件集合自然构成流形，据此提出黎曼逆最优控制方法，将观测轨迹投影到该流形上并保持可行性。实验表明，在人臂运动数据上，该方法计算时间降低约四分之三，重建精度与经典方法相当或更优。该工作展示了几何优化对提升逆最优控制可扩展性与可靠性的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10841v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 618, \"height\": 415, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10841v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 493, \"height\": 380, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10841v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 745, \"height\": 806, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10841v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 850, \"height\": 975, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10841v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 919, \"height\": 574, \"label\": \"Table\"}]"
motivation: 传统双层逆最优控制计算代价高，投影方法数值不稳定，问题根源在于可行集的流形几何结构。
method: 将逆最优控制重写为流形上的优化问题，提出黎曼逆最优控制（RIOC），在最优解流形上投影并保持可行性。
result: "在人臂运动实验中，RIOC计算时间减少约75%，重建精度与经典方法相当或更优。"
conclusion: 几何优化方法可提升逆最优控制的可扩展性和可靠性，适用于机器人及人体运动分析。
---

## 摘要
逆向最优控制旨在恢复能够解释观察到的轨迹作为最优控制问题解的成本函数。经典的双层优化公式反复求解嵌套的最优控制问题，对于实际系统很快变得计算上不可行。近期的基于投影的方法提供了一种有前景的替代方案，但由于违反标准约束规格，在使用基于梯度的方法求解时会出现数值不稳定性。本文表明，这些困难源于逆向最优控制可行集的几何结构。我们证明了满足最优性条件的轨迹集自然形成一个流形，并将逆向最优控制重新表述为该流形上的优化问题。基于这一洞察，我们提出了一种黎曼逆向最优控制方法，该方法将观察到的轨迹投影到最优解的流形上，同时通过构造保持可行性。在真实人类手臂轨迹上的实验表明，所提出的方法在重建精度上与经典双层逆向最优控制相当或更好，同时计算时间减少了约四倍。这些结果突显了几何优化方法在提高机器人学和人体运动分析中逆向最优控制的可扩展性和可靠性方面的潜力。

## Abstract
Inverse Optimal Control (IOC) aims to recover the cost function that explains observed trajectories as solutions of an optimal control problem. Classical IOC formulations rely on bilevel optimization, which repeatedly solves a nested optimal control problem and quickly becomes computationally prohibitive for realistic systems. Recent projection-based approaches offer a promising alternative but suffer from numerical instability when solved with gradient-based methods due to violations of standard constraint qualifications.   In this paper, we show that these difficulties stem from the geometric structure of the IOC feasible set. We demonstrate that the set of trajectories satisfying the optimality conditions naturally forms a manifold and reformulate IOC as an optimization problem on this manifold. Based on this insight, we propose a Riemannian Inverse Optimal Control (RIOC) method that projects observed trajectories onto the manifold of optimal solutions while preserving feasibility by construction. Experiments on real human arm trajectories show that the proposed method achieves comparable or better reconstruction accuracy than classical bilevel IOC while reducing computation time by about a factor of four. These results highlight the potential of geometric optimization methods to improve the scalability and reliability of IOC for robotics and human motion analysis.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：逆向最优控制（IOC）旨在从观测轨迹中恢复解释该轨迹的最优控制代价函数，广泛应用于机器人学和人体运动分析。经典IOC采用双层优化（bilevel optimization），反复求解嵌套的最优控制问题，计算代价极高，难以扩展到高维系统。近期的投影方法（Projected IOC, PIOC）试图将IOC转化为单层优化，通过最小化观测轨迹到“奇异曲线”（singularity curve）的距离来进行代价识别，理论上更高效。
- **核心问题**：PIOC方法在使用基于梯度的求解器（如内点法IPM）时，因约束梯度在可行解处恒为零（违反线性无关约束资格LICQ），导致数值不稳定甚至无法求解。论文指出，这一困难源于IOC可行集（满足KKT条件的轨迹集合）的几何结构。
- **整体含义**：本文从黎曼几何视角出发，论证了满足最优性条件的轨迹集合自然地构成光滑流形。基于此洞察，将IOC重新表述为该流形上的优化问题，提出黎曼逆向最优控制（RIOC）方法，从而避免了PIOC的数值不稳定性，同时保持了投影方法的优势。该方法在真实人臂运动数据上的实验表明，计算时间降低约四分之三，重建精度与经典双层方法相当或更优，展示了利用几何结构提升IOC可扩展性与可靠性的潜力。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 将IOC视为在流形 **M = SC ∩ G** 上的正交投影问题，其中 **SC** 是满足KKT平稳性条件的轨迹集（奇异曲线），**G** 是满足原始可行性的轨迹集（如等式约束）。在适当的正则性条件下，**M** 是一个光滑子流形。
- 利用代价权重参数化 **M**（归一化后自由度为 nc-1），将IOC转化为流形上的无约束或简单有约束的优化问题，并使用黎曼优化方法求解。

### 关键技术细节
1. **流形构造**：
   - 定义 J(s) = [∂C/∂s, ∂g/∂s, ∂h/∂s]（针对不同约束）。
   - SC = {s ∈ R^m | det(J(s)^T J(s)) = 0}（仅含等式约束时）。
   - G = {s ∈ R^m | g(s) = 0}。
   - 交集 M = SC ∩ G 在满足条件（光滑性、LICQ、J的微分满秩、最小秩、横截性）时构成 nc-1 维流形。

2. **参数化**：
   - 以代价权重 ω 为参数，归一化使得 ∑ω_k = 1, ω>0。令优化变量 γ ∈ R^{nc-1}，ω_i = γ_i (i<nc), ω_nc = 1 - ∑γ_k。
   - 则对于每个 γ，直接最优控制（DOC）给出最优轨迹 D(γ) 满足 KKT 条件。

3. **RIOC 公式**（式13）：
   - 目标：最小化 ‖s_obs - D(γ)‖²_2
   - 约束：γ ≥ 0, ∑γ_k ≤ 1（即 ω > 0）
   - 这是一个标准的非线性优化问题，但可行域由 γ 的简单约束定义，可使用梯度方法求解。

4. **黎曼雅可比的计算**（关键贡献）：
   - 对于等式约束情形，由KKT条件 J_ω ω + J_λ λ = 0, g(s)=0，推导出关于 γ 的导数公式（式21）：
     ∂D/∂γ = -N (N^T M N)^{-1} N^T J_ω (∂ω/∂γ)
     其中 N 是 J_λ^T 零空间的一组基，M = ∑ ω_i ∂²C_i/∂s² + ∑ λ_i ∂²g_i/∂s²。
   - 对于包含不等式约束的情况，采用内点法的屏障函数近似，得到平滑版本（式23）：
     ∂D/∂γ = -μ_target N (N^T μ_target M_μ N)^{-1} N^T J_ω (∂ω/∂γ)
     其中 M_μ = M + ∂²F_barrier/∂s²。
   - 这些雅可比可通过求解线性系统得到，从而允许使用一阶优化器（如梯度下降、L-BFGS）有效优化。

5. **算法流程**（文字描述）：
   - 输入：观测轨迹 s_obs，cost函数基 {C_k}，系统动力学与约束。
   - 对当前参数 γ，调用DOC求解器（如IPOPT）得到 D(γ) 及KKT乘子。
   - 根据公式（21）或（23）计算黎曼雅可比 ∂D/∂γ。
   - 使用梯度下降或其他一阶方法更新 γ。
   - 重复直到收敛，输出最优权重 ω。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集**：来自 Berret 等人的经典人臂指向任务（2011），数据包含一位受试者在不同起始位置下的五次运动轨迹。**场景**：平面两自由度臂模型，关节角度、力矩等实时记录。
- **直接最优控制（DOC）公式**：如表I所示，采用多重打靶法（multiple shooting），包含7个成本函数（角速度、角加速度、到目标距离、末端执行器速度与加速度、扭矩、功率）和约束（初始/最终位置、初始速度、动力学等式、关节与力矩限制）。
- **基准/对比方法**：
  - **经典双层IOC（bilevel IOC）**：求解式(5)，使用无导数优化方法（如Powell或COBYLA），内层反复调用DOC。
  - **本文方法RIOC**：使用梯度优化（文中未明确指定具体梯度下降算法，但提到基于梯度的求解器，实验中可能使用了L-BFGS或类似方法）。
- **评价指标**：关节角度的均方根误差（RMSE）、计算时间、恢复的代价权重分布。定性上还展示了笛卡尔空间轨迹对比（图3）。

## 4. 资源与算力

- 论文**未明确提及**算力资源（如GPU型号、数量、训练时长）。实验中仅设置总运行时间上限为3分钟，无并行计算或特殊硬件说明。可以推断实验在标准CPU上完成，因为DOC求解通常使用CPU端的内点法（如IPOPT）。
- 计算成本主要体现为多次DOC求解的次数：bilevel方法因需无导数搜索，DOC调用次数更多；RIOC因可计算梯度，DOC调用次数显著减少，从而缩短总时长。

## 5. 实验数量与充分性

- **实验数量**：仅对**一个受试者的5条轨迹**（不同起始位姿）进行测试。每条轨迹上，bilevel和RIOC各运行一次（图4(a)中每个轨迹对应一个“面积”曲线，可能为多次迭代的收敛过程，但未明确重复多次随机初始化）。表中给出了平均RMSE和标准差，但标准差来源可能来自不同轨迹间的差异，而非同一轨迹多次重复实验。
- **充分性分析**：
  - 实验覆盖了多个起始位置，但未对不同受试者、不同任务类型、不同复杂度模型（如3D臂、全身运动）进行测试。**验证仍初步**。
  - 未进行消融实验（如改变成本函数基、对比不同μ_target值对RIOC的影响）。
  - 未与更广泛的IOC方法（如基于KKT残差的方法、IRL等）对比。
  - 统计量方面，仅给出了5个轨迹的平均RMSE和时间，未报告多次运行的方差或显著检验，结果的**稳定性未充分评估**（尤其是RIOC的时间标准差较大，23.9s，说明不同轨迹收敛速度差异大）。
  - 总体而言，实验作为概念验证足够，但在全面性和统计严谨性上存在不足。

## 6. 论文的主要结论与发现

- **几何洞察**：IOC的可行集（满足KKT条件的轨迹）在合适正则条件下是光滑流形，投影法中的LICQ违反根源在于约束全局极小处的梯度为零。
- **方法有效性**：RIOC在所有5条轨迹上均能收敛，且计算时间平均比bilevel减少约4倍（100.2s vs 26.7s）。
- **精度相当**：关节角度RMSE：RIOC 4.2°±2.2°，bilevel 4.4°±2.2°，二者非常接近；在一条轨迹P5上，RIOC误差（8.7°）显著低于bilevel（19.9°），说明RIOC在某些情况下更稳健。
- **权重可解释性**：两种方法均识别出“到目标距离”是主导代价，与运动生理学直觉一致。
- **鲁棒性**：即使成本函数基不完全（如P5），RIOC仍比bilevel更稳定地给出较低误差。

## 7. 优点

- **理论贡献**：揭示了PIOC不稳定的几何根源，并巧妙利用流形结构构建了可导的黎曼雅可比，使IOC可用高效的梯度方法求解。
- **计算效率**：相比无导数双层方法，RIOC大幅降低了DOC调用次数，计算时间缩短约75%。
- **保持投影优势**：RIOC直接最小化观测与最优轨迹间的欧氏距离，保留了bilevel的目标（接近观测），而其他近似方法（如KKT残差）可能偏离这一目标。
- **处理不等式约束**：通过屏障近似平滑处理不等式约束，扩展了可用性（PIOC最初仅考虑无约束情形）。
- **实验验证**：使用真实人臂数据，结果直观且有说服力。

## 8. 不足与局限

- **理论假设较强**：RIOC的可行性与雅可比有效性依赖于多个条件（LQIC、秩条件、横截性等）。实践中这些条件未必总是成立，论文未讨论当条件不满足时方法的行为或如何检测/绕行。
- **实验覆盖有限**：仅单个受试者、单一任务（平面点指）、5条轨迹，缺乏对多任务、多人体模型、高维系统的验证，泛化能力存疑。
- **未与主流IOC/IRL方法全面对比**：仅对比了经典bilevel，未与KKT残差法、最大熵IRL、投影SVD法等比较。
- **超参数敏感性**：RIOC使用μ_target控制屏障近似精度，但未研究其对收敛性和解精度的影响。
- **统计鲁棒性不足**：未进行多次重复实验，无法判断方法的方差与可靠性；时间标准差（23.9s）较大，部分轨迹可能收敛很慢。
- **成本基设计预设**：实验结果依赖于预设的7个成本函数基，未讨论基的选择对可识别性和解几何的影响。
- **未提供公开代码或复现细节**：不利于社区复现和验证。

（完）
