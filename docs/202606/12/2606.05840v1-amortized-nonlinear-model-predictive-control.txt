Title: Amortized Nonlinear Model Predictive Control

URL Source: https://arxiv.org/pdf/2606.05840v1

Published Time: Fri, 05 Jun 2026 00:49:01 GMT

Number of Pages: 6

Markdown Content:
# Amortized Nonlinear Model Predictive Control 

Francesco Pillitteri, Alberto Bemporad 

Abstract — Nonlinear Model Predictive Control requires solv-ing a constrained nonlinear program (NLP) in real-time at every sampling instant, a computational bottleneck that limits deploy-ment on resource-constrained hardware or at high sampling rates. We address this challenge for the broad class of input-affine nonlinear systems to show that the optimal control move can be approximated by a state-dependent quadratic program 

(QP) whose cost parameters depend on the current state and reference. We propose a single-network residual-corrector 

architecture: a state-dependent analytic baseline provides initial QP parameters, and the network learns only the corrections needed to match the full NLP solution; the QP is solved by a differentiable interior-point layer, guaranteeing constraint satisfaction for the first control action. The network is trained offline on data generated by an NLP solver using a hybrid loss that combines supervised imitation and KKT-residual penalties. We validate the approach on a three-link planar robotic arm with Cartesian end-effector tracking, demonstrating orders-of-magnitude speedup over the NLP solver while maintaining comparable tracking performance. 

I. I NTRODUCTION 

Model Predictive Control (MPC) is widely recognized as a powerful approach to constrained optimal control: at each time step, the controller solves a finite-horizon optimization problem, applies the first element of the optimal input sequence, and repeats the process at the next step [1]. From the perspective of optimization theory, MPC is inherently a 

parametric program: the current state xk (and any output reference vector) acts as the parameter, and the optimal first input u⋆

> 0

(xk) is the corresponding solution map. For linear systems with quadratic costs and polyhedral con-straints the resulting program is a convex QP that can be solved in milliseconds and less by modern active-set [2], [3] or interior-point solvers [4]. An explicit piecewise-affine solution map can be even pre-computed offline via multi-parametric programming [5] to avoid online optimization altogether. For nonlinear plant models, the problem is a nonlinear program (NLP), whose per-step solve time may be orders of magnitude larger, often rendering real-time implemen-tation infeasible, and explicit nonlinear MPC is in general intractable. A classical workaround is to perform only a single Newton-type iteration per step, warm-starting from the previous solution shifted by one time step. This underpins 

real-time iteration (RTI) schemes [6], [7], which have proved  

> This work was funded by the European Union (ERC Advanced Research Grant COMPACT, No. 101141351). Views and opinions expressed are however those of the authors only and do not necessarily reflect those of the European Union or the European Research Council. Neither the European Union nor the granting authority can be held responsible for them. The authors are with the IMT School for Advanced Studies, Lucca, Italy. Email: francesco.pillitteri@imtlucca.it

effective in many applications, though the per-step cost can still be prohibitive at high sampling rates or on resource-constrained hardware. Recent advances in machine learning offer an alternative route: use function approximation to replace the online solver, in analogy to explicit MPC, or to substantially accelerate it by providing a good initial guess. This idea, also referred to as amortized optimization [8], has attracted growing interest for MPC due to the microsecond inference times it enables once the approximation is trained offline. A related approach to reduce the online computational burden in nonlinear MPC is to approximate the cost-to-go function, see, e.g., [9], [10]. Due to the approximation, preserving closed-loop guar-antees can be a challenge, and several strategies have been explored in the literature. For example, stability and feasibil-ity can be recovered by augmenting the approximator with a safety filter [11]. Alternatively, learning a warm-start rather than the full solution preserves problem feasibility while reducing iteration counts [12]. Differentiable optimization layers embed a QP as a differentiable mapping inside the network, guaranteeing that the output satisfies the QP con-straints by construction [13]. Our method belongs to this last family. 

Contribution. Rather than learning the full optimal con-trol sequence, we learn a network that predicts the parameters of a small QP optimizing the first control move, which is then solved exactly by a differentiable layer. This separates the learning problem (predicting good QP-problem matrices) from constraint enforcement (handled by the solver). After formalizing the MPC problem for general input-affine non-linear systems and showing how the input-affine structure yields a parametric QP for the first control step, we propose a residual-corrector QP architecture , which learns how to correct state-dependent QP baseline obtained analytically. Hard constraints are enforced by the QP solver, while state constraints are softened to avoid infeasibility. The network is trained offline on NLP solutions via a hybrid KKT loss 

combining supervised imitation and KKT-residual penalties. We validate the approach on a three-link planar robotic arm with Cartesian end-effector tracking, demonstrating orders-of-magnitude speedup over the NLP solver while maintaining comparable tracking performance and constraint satisfaction. The paper is organized as follows. Section II formu-lates the MPC problem for input-affine systems. Section III presents the proposed architecture and training procedure. Section IV reports experimental results on the robotic arm. Section V concludes the paper. 

Notation. We denote by In the identity matrix of order 

> arXiv:2606.05840v1 [eess.SY] 4 Jun 2026

n and by diag( v) the diagonal matrix formed from vector 

v. For a matrix Q = Q′ ⪰ 0, ∥a∥2 

> Q

:= a⊤Q a and for 

Q ≻ 0 we denote by L = chol( Q) its lower-triangular Cholesky factor, Q = LL ⊤. The operator [·]+ = max( ·, 0) 

and [·]− = max( −· , 0) extract the positive and negative parts, respectively. II. P ROBLEM FORMULATION 

A. Input-Affine Nonlinear Models 

We consider discrete-time input-affine nonlinear prediction models of the form 

xk+1 = f (xk) + G(xk) uk, (1) where xk ∈ Rnx is the state vector, uk ∈ Rnu the control input, and f : Rnx → Rnx and G : Rnx → Rnx×nu define the state-update equation. Affine systems (1) either arise naturally in many engineering domains, such as in robotics; at the price of introducing an input delay, any general dynamics xk+1 = F (xk, u k) can be always transformed into affine form by treating uk as an additional state. We assume that (1) is subject to constraints, namely box constraints 

U = {u ∈ Rnu : u ≤ u ≤ u},

X = {x ∈ Rnx : x ≤ x ≤ x}, (2) and affine-in-input constraints 

Ac(xk) uk ≤ bc(xk), (3) where Ac(xk) ∈ Rnc×nu and bc(xk) ∈ Rnc . Constraints of the form (3) capture, for example, joint-torque limits expressed via inverse dynamics ( τ = M (q)u + h(q, ˙q)), as well as mixed state/input polytopic constraints that become affine in uk for any given xk.

B. Receding-Horizon Optimal Control Problem 

At each time step k, MPC requires solving the following finite-horizon optimal control problem parameterized by the current state xk and a task reference rk:

min  

> {xt},{ut}

Vf (xN ; rk) +  

> N−1

X

> t=0

ℓ(xt, u t; rk) (4a) s.t. xt+1 = f (xt) + G(xt) ut, t = 0 , . . . , N − 1,

(4b) 

x0 = ˆ x, (4c) 

u ≤ ut ≤ u, t = 0 , . . . , N − 1, (4d) 

x ≤ xt ≤ x, t = 1 , . . . , N, (4e) 

Ac(xt) ut ≤ bc(xt), t = 0 , . . . , N − 1. (4f) where, with a slight abuse of notation, t denotes the prediction time k + t, ℓ : Rnx × Rnu → R≥0 is a stage cost and Vf : Rnx → R≥0 is a terminal cost, both depending on the task reference rk. MPC applies uk = u⋆

> 0

, i.e., only the first element of the optimal input sequence, and solve again problem (4) at the next step k + 1 .By collecting all the optimization variables into a single vector 

w = x0⊤ u0⊤ x1⊤ u1⊤ . . . x N −1⊤ uN −1⊤ xN ⊤⊤ (5) with w ∈ Rnw and nw = ( N + 1) nx + N n u, Problem (4) can be written more compactly as the NLP 

w⋆ = arg min  

> w

J(w; rk)

s.t. geq (w; xk) = 0 ,h ≤ h(w; xk) ≤ h, w ≤ w ≤ w, 

(6) where J is the total cost, geq collects all equality con-straints (4b)–(4c), h collects general inequality constraints affine in the input, and w, w encode the box constraints (2). 

C. KKT Conditions and Optimality Certificates 

The standard first-order KKT conditions for (6) require stationarity, primal/dual feasibility, and complementarity. De-noting the Lagrange multipliers for equality constraints, ranged inequalities, and variable bounds by μ, λ, and 

zL, z U ≥ 0 1 respectively, stationarity reads 

∇wL(w⋆, μ ⋆, λ ⋆, z ⋆L, z ⋆U ) = 0 , (7) where L = J +μ⊤geq +λ⊤h+zL⊤(wL −w)+ zU ⊤(w−wU ).Primal/dual feasibility and complementarity conditions can be written in smooth form via the Fischer–Burmeister (FB) function [15], [16]: 

ϕFB (a, b ) = pa2 + b2 − (a + b), (8) satisfying ϕFB (a, b ) = 0 ⇐⇒ a ≥ 0, b ≥ 0, ab = 0 .

D. Input-Affine Structure and Single-Step QP 

The input-affine structure of (1) implies that the one-step state map is affine in u0 for any fixed x0:

x1 = f0 + G0 u0, (9) where f0 ∈ Rnx is the drift under zero input and G0 ∈

Rnx×nu is the control-gain matrix, both evaluated at x0.Every constraint that is affine in u0 for a fixed x0 takes the form (3). We distinguish two roles. Hard constraints for input bounds u ≤ u0 ≤ u and any additional affine-in-input bound are encoded directly as 

Ac(x0) u0 ≤ bc(x0). (10) Instead, we model the bounds x ≤ x1 ≤ x on the first predicted state as soft constraints 

As(x0) u0 −s ≤ bs(x0), As =

 G0

−G0



, bs =

x − f0

f0 − x



.

(11) where s ∈ Rns 

> ≥0

is a vector of non-negative slack variables (with ns = 2 nx), which are used to preserve the feasibility 

> 1Bound multipliers are split into separate lower and upper components following the IPOPT convention [14], which treats them as strictly positive quantities in the interior-point framework.

of the resulting state-dependent QP in (u0, s ):

(u⋆

> 0

, s ⋆) = arg min  

> u0, s ≥012

u0⊤H(x0) u0 + c(x0, r )⊤u0 + ρ 1⊤s

s.t. Ac(x0) u0 ≤ bc(x0),As(x0) u0 − s ≤ bs(x0),

(12) where H(x0) ≻ 0 and c(x0, r ) are the parameters of the quadratic objective, which depend on the state and reference 

r = rk, and ρ > 0 is the weight associated with the linear penalty on soft constraint violation. The joint decision vector 

z = [ u0⊤, s ⊤]⊤ has dimension nz = nu + ns, far smaller than nw = ( N + 1) nx + N n u for typical horizons N .We consider a general stage cost of the form 

ℓ(x, u ; r) = ∥φ(x) − yref ∥2 

> Q

+ ∥u − uref ∥2 

> R

, (13) where φ : Rnx → Rnφ is a (possibly nonlinear) output map, 

Q ≻ 0, R ≻ 0 are weight matrices, and r = [ yref ⊤uref ⊤]⊤

is the task reference. By linearizing φ(x1) around x0 we get the approximation 

φ(x1) ≈ φ(x0) + Jφ(x0) ( f0 − x0)

| {z }

> φdrift

+ Jφ(x0) G0

| {z }

> Juφ

u0,

(14) which is linear in u0 for any fixed x0, where Jφ(x0) ∈

Rnφ×nx is the Jacobian of φ at x0. Substituting (14) into (13) and expanding yields the analytic baseline parameters: 

Han = 2  Juφ

> ⊤

Q J uφ + R,can = 2 Juφ

> ⊤

Q (φdrift − yref ) − 2R u ref . (15a) When φ(x) = x, Jφ = I, Juφ = G0, and φdrift = f0,so (15a) reduces to the familiar quadratic case: 

Han = 2( R+G0⊤QG 0), can = 2 G0⊤Q(f0−xref )−2Ru ref .

(15b) In the sequel, we denote by Lan = chol( Han ).As (H(x0), c (x0, r )) must encode the entire N -step value function, not merely the one-step stage cost, while the analytic baseline (15) captures only the linearized one-step contribution, we need to learn the residual (∆ L, ∆c) to account for the remaining N −1 horizon steps and, when 

φ is nonlinear, also for the linearization error. We describe next how to learn them via a neural network. III. A MORTIZED QP FOR INPUT -A FFINE NMPC As shown in Fig. 1, to learn the residual corrections 

(∆ L, ∆c), we propose the use of the neural network ϕψ .The network takes the concatenated input ξ = [ x⊤ 

> 0

, r ⊤]⊤

and returns (∆ L, ∆c) to complement the analytic baseline 

(Lan , c an ). The corrected Cholesky factor Lfull = Lan + ∆ L

defines a positive-definite cost matrix Hu = Lfull L⊤

> full

, which together with cfull = can + ∆ c and the state-dependent constraints A(x0), b (x0) fully specifies a small QP, to be solved online to generate the command input u∗

> 0

. The net-work is trained offline via a hybrid loss combining supervised imitation of NLP solutions, KKT stationarity, and Fischer-Burmeister complementarity residuals. 

A. QP-Parameter Network 

The network ϕψ outputs nu(nu + 1) /2 lower-triangular Cholesky corrections ∆L and nu linear cost corrections ∆c,totalling nu(nu + 3) /2 scalar outputs. The full linear cost is 

cfull = can + ∆ c, and the off-diagonal Cholesky entries are corrected additively as [Lfull ]ij = [ Lan ]ij + [∆ L]ij for i > j .The diagonal is replaced by [Lfull ]ii = softplus([ Lan ]ii +[∆ L]ii ) + ε, where softplus( x) = log(1 + ex) and ε > 

0, guaranteeing strict positivity of the diagonal and hence 

Hfull = Lfull Lfull ⊤ ≻ 0. The constraint matrices Ac(x0),

bc(x0), As(x0), bs(x0) are computed analytically from the known dynamics and input-affine structure. During training, the QP is solved by a batched differentiable interior-point solver [17], producing u0 satisfying (10) and slack s as in (11). 

B. Hybrid Loss Function 

Let D = {(x(i)0 , r (i), w ⋆(i), μ ⋆(i), λ ⋆(i), z ⋆(i) 

> L

, z ⋆(i) 

> U

)}Mi=1 be an offline dataset of M optimal NLP solutions and their dual variables. Given the network’s predicted first-step control 

ˆu(i)0 , we define the mixed primal vector ˆw(i) as the full optimal solution w⋆(i) with the first control block and the resulting one-step-ahead state replaced by the network’s prediction: 

ˆw(i) = w⋆(i)    

> u0←ˆu(i)0,x1←f(i)0+G(i)0ˆu(i)0

. (16) The network is trained by minimising the following hybrid loss: Ltotal = αsup Lsup +αKKT LKKT +αFB LFB +αreg Lreg ,

where αsup , α KKT , α FB , α reg > 0 are tunable weights, and the individual loss terms are defined as follows. 

a) Supervised imitation loss: 

Lsup = 1

M

> M

X

> i=1

ˆu(i)0 − u⋆(i)0

> 2

(17) penalizing the deviation from the optimal first control action. 

b) KKT stationarity loss: To enforce (7), we penalize 

LKKT = 1

M

> M

X

> i=1

∇wL( ˆ w(i), μ ⋆(i), λ ⋆(i), z ⋆(i) 

> L

, z ⋆(i) 

> U

) 2

(18) at the optimal dual variables from the dataset, measuring the stationarity residual of the network’s prediction ˆw(i) for the full NLP. 

c) Fischer–Burmeister complementarity loss: 

The violation of the complementarity conditions and inequality constraints are penalized via the FB function (8). For box constraints on w, we set Lbox FB = 1

> M

PMi=1 



ϕFB ( ˆ w(i) − w, z ⋆(i) 

> L

) 2

+

ϕFB (w − ˆw(i), z ⋆(i) 

> U

) 2

.For ranged inequality constraints hj ≤ hj (w) ≤ hj with signed multiplier λ⋆j (positive when the upper bound is active, negative when the lower bound is active), we penalize instead 

Lineq FB = 1

> M

PMi=1 

P

> j



ϕFB (hj − hj ( ˆ w(i)), [λ⋆(i) 

> j

]+) 2

+

ϕFB (hj ( ˆ w(i)) − hj , [λ⋆(i) 

> j

]−) 2

.Inference 

> Training (offline)
> x0
> r

Analytic Base 

> Lan , c an

Concat ξ Net ϕψ 

> (∆ L, ∆c)

Cholesky                        

> Lfull =Lan +∆ Lcfull =can +∆ cHu=Lfull L⊤
> full
> Lii = sp( Lii )+ ϵ
> min u,s 12u⊤Hf ull u+c⊤
> full u+ρ1⊤s
> s.t. A(x0)[ u;s]≤b(x0),s≥0
> Hf ull ≻0
> u∗
> 0
> A(x0), b (x0)

Hybrid Loss L = αsup ∥u∗ 

> 0

− uopt 0 ∥2 +

αKKT ∥∇ wL( ˆ w, λ ∗)∥2 + αFB LFB + αreg (∥∆L∥2 + ∥∆c∥2)                  

> uopt 0, w ⋆
> λ⋆, z ⋆L, z ⋆U
> ∂L/∂θ Fig. 1. Residual-corrector architecture for amortized input-affine MPC. An analytic baseline (Lan , c an )is computed from the current state and reference; the network ϕψpredicts residual corrections (∆ L, ∆c). The Cholesky factor Lfull =Lan + ∆ Lyields a guaranteed positive-definite Hessian Hu≻0
> after applying softplus to the diagonal. The resulting QP is solved by a differentiable interior-point layer, producing u∗
> 0with constraint satisfaction. The network is trained offline on NLP solutions via a hybrid loss combining supervised imitation and KKT-residual penalties.

The total FB loss is LFB = Lbox FB + Lineq FB . The optimal multipliers in D are treated again as fixed constants during training. 

d) Regularization towards the analytic baseline: To prevent the learned corrections from deviating too far from the analytic baseline and regularize the learning problem, we include a small ℓ2 penalty on the residuals: Lreg =

> 1
> M

PMi=1 

  ∆L(i) 2 + ∆c(i) 2. The weight αreg on Lreg 

is set to a small value. Note that ˆw(i) in 16 satisfies the dynamic equations only at the first step when transitioning from x0 to x1; the dynamics at t ≥ 1 are violated when ˆu0̸ = u⋆

> 0

, as the subsequent states 

x2, . . . , x N are still taken from the optimal solution w⋆ and thus are consistent with u⋆ 

> 0

rather than ˆu0. Therefore, the KKT and FB losses are evaluated at a dynamically infeasible point, accurate to first order in ˆu0 − u⋆

> 0

.IV. C ASE STUDY : P LANAR ROBOTIC ARM 

A. System Model 

We test the proposed nonlinear MPC approximation method on a three-link planar robotic arm operating in the vertical plane under gravity, with link lengths l =[1 .0, 1.0, 0.5] m, masses m = [1 .0, 1.0, 0.5] kg, centre-of-mass offsets lc = [0 .5, 0.5, 0.25] m, and moments of inertia 

I = [0 .0833 , 0.0833 , 0.0104] kg m 2. The system is described by the Euler–Lagrange equations 

M (q) ¨ q + C(q, ˙q) ˙ q + g(q) = τ, (19) where M (q) ∈ R3×3 is the inertia matrix, C(q, ˙q) the Coriolis matrix, g(q) the gravity vector, and τ ∈ R3 the joint torques. Choosing joint accelerations u = ¨ q ∈ R3 as the control input yields an input-affine system (1) with state x =[q⊤, ˙q⊤]⊤ ∈ R6. Both the MPC prediction model and the plant model are integrated with a fixed-step RK4 scheme; the plant model is simulated using the full nonlinear dy-namics (19), while the prediction model uses the simplified double-integrator model ¨q = u. Torque signals are recovered via inverse dynamics τ = M (q)u + C(q, ˙q) ˙ q + g(q).The affine one-step map (9) used to build the QP con-straints is obtained by applying zero-order hold to the double integrator, giving the exact discrete matrices 

G0 =

 12 T 2 

> s

Inq

TsInq



, f0 =

q + Ts ˙q

˙q



. (20) 

B. MPC Problem Setup 

We setup the MPC problem (4) with horizon N = 15 and the Cartesian end-effector tracking objective 

ℓ(x, u ; pref ) = ∥p(q) − pref ∥2 

> Wee

+ ∥ ˙q∥2 

> Wdq

+ ∥u∥2 

> Wu

, (21) where p(q) ∈ R2 is the Cartesian end-effector position (forward kinematics), pref ∈ R2 is the Cartesian target, and 

Wee = diag(150 , 150) , Wdq = 0 .5 I3, Wu = 0 .02 I3. The terminal cost uses Wee,f = 20 Wee , Wdq,f = 10 Wdq . Here 

r = pref .The following constraints are enforced at every horizon step: box constraints on joint positions |qi| ≤ π rad, joint velocities | ˙qi| ≤ 5 rad/s, and accelerations |ui| ≤ 20 rad/s 2

for all i = 1 , 2, 3; affine-in-input constraints (3), with joint torques expressed via inverse dynamics (19), |τi| ≤ 50 N m for joints 1–2, and |τ3| ≤ 20 N m for the distal link. The stage cost (21) is nonlinear in x (through the forward kinematics p(q)), so the closed-form parameters (15b) do not apply directly. We derive an analytic baseline by linearising the end-effector position around the current configuration: 

p(q1) ≈ p(q) + Jee (q) TsI3 ˙q

| {z }

> pdrift

+ Jee (q) 12 T 2 

> s

I3

| {z }

> Juee

u, 

where Jee (q) ∈ R2×3 is the end-effector Jacobian and pdrift 

is the drift under zero control. Substituting into (21) and expanding yields 

Han = 2  Ju⊤ 

> ee

Wee Juee + T 2 

> s

Wdq + Wu

, (22) 

can = 2 Ju⊤ 

> ee

Wee (pdrift − pref ) + 2 TsWdq ˙q. (23) (a) t = 0.0 s (b) t = 0.9 s (c) t = 1.9 s (d) t = 2.8 s        

> Amortized QP acados RTI IPOPT Direct MLP Target pref Fig. 2. End-effector trajectories in the Cartesian workspace for the amortized QP (solid blue), RTI (dashed orange), IPOPT (dashed green), and direct MLP (dashed yellow), from a common initial configuration (filled circle) to the target pref (star). All solvers successfully reach the target; only the amortized QP closely matches the IPOPT trajectory.

The hard constraints (10) encode the torque bounds τ =

M (q)u + h(q, ˙q) ∈ [τ , τ ] together with the acceleration bounds: 

Ac(x) = 



M (q)

−M (q)

I3

−I3

, bc(x) = 



τ − hh − τu

−u

. (24) The one-step-ahead state is exact under the ZOH model (20): 

q1 = q + Ts ˙q + 12 T 2 

> s

u, ˙q1 = ˙q + Tsu. Stacking the four upper/lower bound pairs gives the arm-specific instance of (11) using G0 from (20): 

As =

 

> 12

T 2 

> s

I3

− 12 T 2 

> s

I3

TsI3

−TsI3

, b s(x) = 



q − (q + Ts ˙q)(q + Ts ˙q) − q

˙q − ˙q

˙q − ˙q

, (25) yielding ns = 4nu = 12 slack variables. The QP (12) therefore has nz = nu + ns = 15 decision variables and 

36 inequality rows ( 12 hard from (24), 12 soft from (25), and 12 non-negativity rows −s ≤ 0). 

C. Data Generation and Training 

Trajectories are generated under warm-started IPOPT [14] with ϵtol = 10 −6 on all termination criteria, Ts = 0 .05 s, and 

Tsim = 0 .005 s. Each trajectory runs up to 80 steps from an independently drawn (x0, p ref ); those failing Cartesian (0.04 m) or velocity ( 0.08 rad/s) tolerances are discarded. 

(q, ˙q, p ref ) ∈ R8 are drawn via Latin hypercube sampling: 

q ∼ U ([ −π, π ]3), ˙q ∼ U ([ −5, 5] 3), pref over the reachable annulus ρ ∈ [0 .30 , 2.35] m, yielding M = 100 000 samples. 

ϕψ is a three-hidden-layer MLP ( [128 , 128 , 64] , ReLU) mapping ξ ∈ R8 to the 9-dimensional residual (∆ L, ∆c)

over the analytic baseline (22)–(23), initialized to zero so the QP recovers the analytic solution at startup. Train-ing uses Adam [18] with cosine decay ( 10 −3 → 10 −5), 

200 epochs, batch size 256 , (αsup , α KKT , α FB , α reg ) =(1 , 10 −4, 10 −3, 10 −4), ρ = 1000 , ε = 10 −3, with QP gradients via implicit differentiation in JAX [19]. 

Remark 4.1: αKKT and αFB are kept small relative to 

αsup : both losses are evaluated at the mixed primal ˆw(i),giving a gradient approximation unreliable far from the NLP solution, and the KKT gradient (18) is numerically larger in scale than the supervised loss. The KKT and FB terms thus serve as a complementary signal near constraint boundaries once the supervised term has converged. 

D. Numerical Setup 

All evaluations are run on an Intel Core i5-9400H mobile CPU. We evaluate the trained policy in closed-loop against three baselines: an acados SQP-RTI solver [20] performing one Gauss–Newton iteration per step; full-horizon IPOPT as an optimality reference; and a direct MLP that shares the same architecture and composite loss as the amortized policy but omits the differentiable QP layer, directly predicting u⋆

> 0

from ξ. Each scenario draws q0 uniformly in [−π, π ]3 at rest ( ˙q0 = 0 ) and a target pref from the reachable annulus 

r ∈ [0 .30 , 2.35] m; all controllers drive the nonlinear plant for 4 s (Figure 2). Because IPOPT solves a nonconvex NLP it may fail to converge to a global minimum; such scenarios are discarded and replaced so that IPOPT can be used to get a lower bound on the closed-loop MPC performance: Jcl =PT −1 

> k=0

ℓ(xk, u k) + ℓT (xT ). We collect 100 valid scenarios under this protocol. 

E. Results 

Tables I–II and Figure 3 show the obtained results in terms of closed-loop performance and solution time across scenarios. The amortized policy converges on all 100 scenarios and achieves a mean closed-loop cost within 4.7% of the IPOPT optimum, while acados RTI diverges on 12 scenarios and sits 17 .5% above on average (up to 6.9× in the worst case). The direct MLP converges on only 73 /100 scenarios and sits 12 .0% above IPOPT, with hard constraint violations reaching 46 .3 N m in torque and 20 .8 rad/s 2 in acceleration; this confirms that the QP layer is essential for both feasibility and reliability. The amortized policy runs in 0.116 ms mean, a 21 × speedup over RTI and over 1000 × over IPOPT. Hard bounds on torque and acceleration are satisfied to numerical precision. Soft joint-velocity constraints are mildly Amortized  

> QP
> acados
> RTI
> IPOPT Direct
> MLP
> 10 −1
> 10 0
> 10 1
> 10 2
> 10 3
> 10 4
> Solve time [ms]
> med.
> 0.11ms
> med.
> 2.37ms
> med.
> 120.22ms
> med.
> 0.09ms

Fig. 3. Per-step solve time. The amortized policy (blue) is ≈21 × faster than RTI (orange) and ≈1826 × faster than IPOPT (purple). The direct MLP (red) is slightly faster, but this comes at the expense of constraint satisfaction and convergence reliability. Median solve times are reported above each distribution. TABLE I COMPUTATIONAL EFFICIENCY AND TRACKING PERFORMANCE (100 

> SCENARIOS

, T = 80 STEPS )

Metric Amort. RTI IPOPT MLP Mean time (ms) 0.116 2.453 211 .8 0.095 

Max time (ms) 3.780 224 .4 5837 0.284 

Speedup vs. IPOPT ∗ 1826 86 — 2229 

Speedup vs. RTI ∗ 21 — — 26 

Convergence 100 /100 88 /100 100 /100 † 73 /100 

Mean EE err. (m) 1.5e−3 4.2e−2 1.2e−3 4.0e−2

Max EE err. (m) 2.8e−2 1.163 3.4e−2 6.6e−1

> ∗

Ratio of mean solve times. 

> †

Non-converging scenarios discarded by construction. 

violated during transients, with smaller peak excursions for the amortized policy ( 1.42 vs. 3.30 rad/s for RTI). Slack is active in only 0.7% of steps, confirming that constraint softening plays a minor role in nominal operation. V. C ONCLUSION 

We have presented a learning-based amortized frame-work for MPC of input-affine nonlinear systems that only requires solving a state-dependent QP online. We showed that, by using a hybrid loss combining supervised imita-tion with physics-informed KKT-residual penalties, we get superior performance and feasibility compared to merely fitting collected nonlinear MPC data. Moreover, our approach can achieve considerable speedups over RTI and orders-of-magnitude speedups over full-horizon NLP solvers while maintaining comparable tracking performance. REFERENCES [1] J. B. Rawlings, D. Q. Mayne, and M. Diehl, Model Predictive Control: Theory, Computation, and Design , 2nd ed. Nob Hill Publishing, 2017. [2] H. J. Ferreau, C. Kirches, A. Potschka, H. G. Bock, and M. Diehl, “qpoases: A parametric active-set algorithm for quadratic program-ming,” Mathematical Programming Computation , vol. 6, no. 4, pp. 327–363, 2014. TABLE II CLOSED -LOOP COST AND CONSTRAINT SATISFACTION (100 

> SCENARIOS

). Jcl RATIOS RELATIVE TO IPOPT. 

Metric Amort. RTI IPOPT MLP Mean Jcl 3642 .5 4273 .0 3574 .9 3878 .2

Max Jcl 23451 29620 23239 26496 

Mean ratio 1.047 1.175 1.000 1.120 

Max ratio 1.596 6.914 1.000 2.633 

Max τ viol. (N m) 0 10 −11 10 −7 46 .3

Max u viol. (rad/s 2) 0 10 −15 10 −7 20 .8

Max ˙q viol. (rad/s) 1.42 3.30 2.65 2.56 

Slack active (%) 0.7 — 4.3 —

[3] G. Cimini, A. Bemporad, and D. Bernardini, “ODYS QP Solver,” ODYS S.r.l. (https://odys.it/qp), Sep. 2017. [4] G. Frison and M. Diehl, “Hpipm: a high-performance quadratic programming framework for model predictive control,” IFAC-PapersOnLine , vol. 53, no. 2, pp. 6563–6569, 2020. [5] A. Bemporad, M. Morari, V. Dua, and E. N. Pistikopoulos, “The ex-plicit linear quadratic regulator for constrained systems,” Automatica ,vol. 38, no. 1, pp. 3–20, 2002. [6] M. Diehl, H. G. Bock, and J. P. Schl¨ oder, “A real-time iteration scheme for nonlinear optimization in optimal feedback control,” SIAM Journal on Control and Optimization , vol. 43, no. 5, pp. 1714–1736, 2005. [7] S. Gros, M. Zanon, R. Quirynen, A. Bemporad, and M. Diehl, “From linear to nonlinear MPC: Bridging the gap via the real-time iteration,” 

International Journal of Control , vol. 93, no. 1, pp. 62–80, 2020. [8] B. Amos, “Tutorial on amortized optimization,” Foundations and Trends in Machine Learning , vol. 16, no. 5, pp. 592–732, 2023. [9] S. Abdufattokhov, M. Zanon, and A. Bemporad, “Learning Lyapunov terminal costs from data for complexity reduction in nonlinear model predictive control,” Int. Journal of Robust and Nonlinear Control ,vol. 34, no. 13, pp. 8676–8691, 2024. [10] M. Schaller, A. Bemporad, and S. Boyd, “Learning Parametric Convex Functions,” 2025, http://arxiv.org/abs/2506.04183. [11] K. P. Wabersich and M. N. Zeilinger, “A predictive safety filter for learning-based control of constrained nonlinear dynamical systems,” 

Automatica , vol. 129, p. 109597, 2021. [12] R. Sambharya, G. Hall, B. Amos, and B. Stellato, “Learning to warm-start fixed-point optimization algorithms,” J. Mach. Learn. Res. ,vol. 25, no. 1, Jan. 2024. [13] B. Amos and J. Z. Kolter, “OptNet: Differentiable optimization as a layer in neural networks,” in Proc. International Conference on Machine Learning (ICML) , 2017, pp. 136–145. [14] A. W¨ achter and L. T. Biegler, “On the implementation of an interior-point filter line-search algorithm for large-scale nonlinear program-ming,” Mathematical programming , vol. 106, no. 1, pp. 25–57, 2006. [15] A. Fischer, “A special Newton-type optimization method,” Optimiza-tion , vol. 24, no. 3–4, pp. 269–284, 1992. [16] L. L¨ uken and S. Lucia, “Self-supervised learning of iterative solvers for constrained optimization,” 2025. [Online]. Available: https://arxiv.org/abs/2409.08066 [17] K. Tracy and Z. Manchester, “On the differentiability of the primal-dual interior-point method,” 2024. [18] D. P. Kingma and J. Ba, “Adam: A method for stochastic optimiza-tion,” arXiv preprint 1412.6980 , 2014. [19] J. Bradbury, R. Frostig, P. Hawkins, M. J. Johnson, C. Leary, D. Maclaurin, G. Necula, A. Paszke, J. VanderPlas, S. Wanderman-Milne, and Q. Zhang, “JAX: composable transformations of Python+NumPy programs,” 2018. [Online]. Available: http://github. com/jax-ml/jax [20] R. Verschueren, G. Frison, D. Kouzoupis, J. Frey, N. v. Duijkeren, A. Zanelli, B. Novoselnik, T. Albin, R. Quirynen, and M. Diehl, “acados – a modular open-source framework for fast embedded optimal control,” Mathematical Programming Computation , vol. 14, no. 1, pp. 147–183, 2022.