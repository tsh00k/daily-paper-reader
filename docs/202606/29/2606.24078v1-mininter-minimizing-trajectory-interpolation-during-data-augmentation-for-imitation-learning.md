---
title: "MinInter: Minimizing Trajectory Interpolation During Data Augmentation for Imitation Learning"
title_zh: "MinInter: 在模仿学习的数据增强中最小化轨迹插值"
authors: "Qingyang Wang, Xingang Liu, Changwei Yao, Zikai Ouyang, Junwei Liu, Haibo Lu, Wei Zhang"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.24078v1"
tags: ["query:inverse-rl"]
score: 9.0
evidence: 通过轨迹选择进行模仿学习数据增强
tldr: 模仿学习依赖高质量演示数据，但采集成本高。轨迹级数据增强通过重组演示片段生成数据，但插入的非专家插值会降低质量。本文提出MinInter，为每个初始状态选择需最少插值的源演示，最小化插值干扰。在12个操作任务26个变体上，MinInter持续提升数据生成与策略成功率，尤其在接触密集、长时域和高方差场景中表现突出。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.24078v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1818, \"height\": 995, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24078v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1819, \"height\": 816, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24078v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 780, \"height\": 764, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24078v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 779, \"height\": 762, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.24078v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 793, \"height\": 517, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.24078v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1630, \"height\": 697, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.24078v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 914, \"height\": 660, \"label\": \"Table\"}]"
motivation: 现有轨迹级数据增强方法插入非专家插值，降低合成演示质量，限制模仿学习性能。
method: MinInter为每个初始配置选择需要最少插值形成完整轨迹的源演示，显式最小化插值引入的噪声。
result: 在MimicGen基准12个任务26变体上，MinInter提升数据生成成功率和策略成功率，接触密集、长时域、高方差任务增益最大。
conclusion: 最小化插值的简单策略能显著提升数据增强质量，为模仿学习提供更优的合成数据生成方法。
---

## 摘要
模仿学习使机器人能够从演示中获取复杂的操作技能，但其有效性受到高质量数据收集成本的限制。轨迹级数据增强方法通过在不同初始状态下重组专家演示来缓解这一挑战。然而，这类方法通常在不连续的部分之间插入插值或其他非专家过渡片段，这些非专家片段可能会降低生成数据的质量。本文介绍了最小化插值（MinInter），一种有效的轨迹选择方法，对于每个采样的初始配置，它选择需要最少插值以形成完整轨迹的源演示。通过在数据生成过程中显式最小化插值，MinInter 产生更高质量的合成演示，同时与现有数据生成框架保持兼容。在 MimicGen 基准测试中的 12 个操作任务（包含 26 个变体）上的实验表明，MinInter 持续提高了数据生成成功率和策略成功率，在接触密集、长视距和高方差设置中提升最大。与最近的 SkillGen 框架相比，尽管 MinInter 概念上更简单，但它实现了更高的策略成功率，突显了插值最小化在数据增强中的价值。

## Abstract
Imitation learning enables robots to acquire complex manipulation skills from demonstrations, but its effectiveness is limited by the cost of collecting high-quality data. Trajectory-level data augmentation methods alleviate this challenge by recombining expert demonstrations under varied initial states. However, such methods typically insert interpolations or other non-expert transition segments between disjoint parts, and such non-expert segments could reduce the quality of the generated data. This paper introduces Minimizing Interpolation (MinInter), an effective trajectory selection method that, for each sampled initial configuration, chooses the source demonstration requiring the least interpolation to form a complete trajectory. By explicitly minimizing interpolations during data generation, MinInter produces higher-quality synthetic demonstrations while remaining compatible with existing data generation frameworks. Experiments on 12 manipulation tasks with 26 variants from the MimicGen benchmark show that MinInter consistently improves both data generation success rates and policy success rates, with the largest gains on contact-rich, long-horizon and high-variance settings. Compared to the recent SkillGen framework, MinInter achieves higher policy success rates despite its conceptual simplicity, underscoring the value of interpolation minimization for data augmentation.

---

## 论文详细总结（自动生成）

# 论文详细中文总结：MinInter

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：模仿学习（Imitation Learning, IL）需要大量高质量演示数据，但人工收集成本高昂。轨迹级数据增强通过重组专家演示片段、在不同初始状态下生成新轨迹，是缓解数据稀缺的有效手段。然而，现有方法在拼接不连续的演示片段时，通常插入线性插值或非专家过渡段，这些非专家动作会引入不自然的运动，降低生成数据质量，进而损害下游策略学习性能。
- **研究动机**：论文发现，数据增强中额外引入的插值是导致合成演示质量下降的关键因素。因此，其核心目标是**最小化数据生成过程中的插值量**，以提升合成演示的质量，从而提升模仿学习策略的成功率。
- **整体含义**：这项工作提出了一种简单但有效的轨迹选择方法 MinInter，通过为每个采样初始状态选择所需插值最少的源演示，显著改善了数据生成质量和下游策略性能。该研究揭示了“插值最小化”在轨迹级数据增强中的重要价值。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
MinInter 的核心思想是在数据生成过程中，对于每个采样的初始物体配置，从所有源演示中选出那个**需要最少插值**才能形成完整轨迹的演示。通过显式最小化插值，减少非专家过渡段，从而提升生成轨迹的真实性和可学性。

### 关键技术细节
1. **插值度量（Interpolation Metric）**：定义了两个末端执行器位姿 \(E_1\) 和 \(E_2\) 之间的插值距离：
   \[
   \delta(E_1, E_2) = (1-w)\|p_1 - p_2\|_2 + w\|R_1^{-1}R_2 - I\|_F
   \]
   其中 \(p\) 为位置向量，\(R\) 为旋转矩阵，\(w \in [0,1]\) 平衡平移与旋转的贡献（实验设为 \(w=0.5\)）。
2. **总插值计算**：对于由 \(n\) 个片段组成的轨迹 \(\tau\)，总插值为相邻片段端点之间的插值距离之和：
   \[
   \text{Inter}(\tau) = \sum_{j=0}^{n-1} \delta(E^j_{H_j,\text{transf}}, E^{j+1}_{1,\text{transf}})
   \]
   其中 \(E^0_{H_0,\text{transf}}\) 为初始末端执行器位姿。
3. **选择策略**：给定初始状态 \(s_0\)，对所有源演示 \(\tau \in \mathcal{T}_{\text{exp}}\) 计算适应后的轨迹 \(\Gamma(\tau, s_0)\) 的总插值，选择插值最小的源演示：
   \[
   \tau^* = \arg\min_{\tau \in \mathcal{T}_{\text{exp}}} \text{Inter}(\Gamma(\tau, s_0))
   \]
4. **生成流程**：对于每个采样的初始配置，用所有源演示生成候选轨迹（包括变换片段和插值），计算每个候选的总插值，选择最小插值的轨迹。若该轨迹满足任务成功条件，则纳入训练数据集；否则丢弃并重新采样。该流程与 MimicGen 等现有数据生成框架完全兼容，仅增加了一个选择步骤。

## 3. 实验设计：数据集/场景、Benchmark、对比方法

- **基准（Benchmark）**：使用 **MimicGen** 公开基准中的 **12 个操作任务**(共 **26 个变体**)，涵盖基础任务（Stack, Stack Three）、接触密集任务（Square, Threading, Coffee, Three Piece Assembly, Hammer Cleanup, Mug Cleanup）以及长时域任务（Kitchen, Nut Assembly, Pick Place, Coffee Preparation）。
- **环境**：基于 **robosuite** 和 **MuJoCo** 仿真器实现。
- **变体难度**：每个任务提供三种初始状态分布 D0（默认）、D1（增加位置/方向变化）、D2（更大范围随机采样），难度递增。
- **数据生成**：每个任务变体使用 **10 条专家演示** 作为源数据，目标生成 **1000 条成功轨迹**（根据任务特定成功标准筛选）。
- **策略训练**：采用与 MimicGen 基准相同的 **BC-RNN (Behavior Cloning with Recurrent Neural Network)** 架构，输入为 RGB 图像（侧视+眼在手）以及末端执行器位姿和夹爪状态。训练 1200 个 epoch，每 20 个 epoch 评估 50 次 rollout，取最佳检查点。报告 3 个不同随机种子（101, 102, 103）的平均值和标准差。
- **对比方法**：
  - **Baseline**：MimicGen 原始数据生成方法（使用线性插值连接片段）。
  - **SkillGen**：一种最新的离线数据增强方法，在论文报告的子集（4 个任务）上进行对比。

## 4. 资源与算力

论文**未明确说明**使用的 GPU 型号、数量、训练时长等算力信息。仅提到每个策略训练 1200 个 epoch，使用 3 个随机种子，但未提供总计算时间或硬件配置。因此无法评估算力需求的具体规模。

## 5. 实验数量与充分性

- **实验数量**：覆盖 12 个任务 × 26 个变体，数据生成成功率和策略成功率均报告。此外还进行了插值减少量的对比（图3）、不同难度和任务分组的分析（图5）。与 SkillGen 的对比在 4 个任务上进行。
- **充分性**：实验设计较为全面：
  - 包含了不同难度等级（D0/D1/D2），覆盖了简单到困难场景。
  - 包含了多种任务类型（基础、接触密集、长时域），能够反映方法在不同行为模式下的表现。
  - 与最近 SOTA 方法 SkillGen 进行了直接对比，且使用了相同的训练和评估代码，确保了比较的公平性。
  - 训练采用 3 个随机种子取平均，增加了结果的可信度。
- **客观性**：实验设置清晰，采用统一的框架和评价协议，对比结果直接可复现。不足之处在于：SkillGen 对比仅覆盖了 4 个任务，可能不足以全面体现差距；部分任务（如 Stack Three、Pick Place）表现无改善甚至略下降，但作者未深入分析原因。

## 6. 论文的主要结论与发现

1. **数据生成成功率提升**：MinInter 在绝大多数任务变体上提升了数据生成成功率，平均提高 **+12.8%**。在接触密集和长时域任务中提升尤为显著（如 Hammer Cleanup D0 +50.1%, Nut Assembly D0 +31.1%）。
2. **策略成功率提升**：在 26 个变体中，19 个变体策略成功率提升或已达 100%，平均提高 **+4.8%**。最大增益出现在 Nut Assembly D0（+20.7%）。
3. **与 SOTA 对比**：在 SkillGen 报告的子集（4 个任务）上，SkillGen 平均提升 +3.8%，而 MinInter 达到 +4.9%，实现了新的 SOTA。
4. **插值减少与性能正相关**：策略成功率的提升与插值减少量一致（图3、图4），验证了最小化插值的核心假设。
5. **在困难设置下提升更显著**：随着初始状态分布难度增加（D0→D2）以及任务复杂性增加（基础→接触密集→长时域），MinInter 的增益更明显。

## 7. 优点

- **方法简洁有效**：仅通过选择最小插值轨迹，无需额外模型、规划或手动干预，即可显著提升数据质量和策略性能。
- **兼容性强**：可无缝集成到现有基于片段重组的数据生成框架（如 MimicGen）中，无需修改底层生成逻辑。
- **实验充分、验证有力**：在广泛的任务和难度变化上验证了方法的通用性和鲁棒性，且与 SOTA 进行了直接对比。
- **可解释性好**：明确了插值是有害因素，并通过消融（权重 w 的选择）验证了度量的有效性。
- **提升了数据生成效率**：减少了数据生成过程中的失败次数，实际降低了采集高质量合成数据的成本。

## 8. 不足与局限

- **实验覆盖的局限**：SkillGen 对比仅涉及 4 个任务，缺乏更大范围的比较；部分任务（如 Pick Place、Stack Three）策略成功率无提升甚至略降，未给出充分解释。
- **算力消耗未报告**：缺少训练和推理的计算资源信息，不利于复现和评估实际应用成本。
- **方法依赖源演示质量**：MinInter 本身不改善源演示的分布或多样性，仅从已有源演示中选择，可能无法应对源演示严重不足或质量极低的情况。
- **未考虑跨源演示的片段组合**：MinInter 固定使用单个源演示的所有片段，不允许跨不同源演示组合片段，这可能限制了生成轨迹的多样性。作者在方法部分明确说明“所有片段来自同一源演示”，这可能是出于简化考虑，但也是潜在局限。
- **应用限制**：实验仅在仿真环境（MuJoCo）中进行，未在真实机器人上验证。接触密集任务在仿真中的物理真实性可能有限，现实世界中的插值效果可能不同。
- **长期记忆**：题目标注为 2026 年论文，时间较新，但方法相对简单，后续可能被更复杂的方法超越。

（完）
