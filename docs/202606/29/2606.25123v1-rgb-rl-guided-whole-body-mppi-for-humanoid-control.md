---
title: "RGB: RL Guided Whole-Body MPPI for Humanoid Control"
title_zh: "RGB: 基于强化学习的全身MPPI人形机器人控制"
authors: "Yunsoo Seo, Sol Choi, Euncheol Im, Myo Taeg Lim, Yisoo Lee"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25123v1"
tags: ["query:mpc-wbc"]
score: 9.0
evidence: RL引导的全身MPPI人形控制，结合MPC和强化学习
tldr: 人形机器人全身控制器要求鲁棒且精确，但深度强化学习策略难以在不重新训练的情况下添加新反馈目标。本文提出RL引导的全身模型预测路径积分（MPPI）框架，将预训练RL策略作为采样先验，通过MPPI代价函数在线修正策略输出以实现任务目标。在29自由度Unitree G1仿真中，该方法以平均280Hz频率稳定控制，相比纯RL基线，直线行走漂移和全身参考信号跟踪精度显著提升，且无需重新训练即可灵活扩展新目标。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25123v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1837, \"height\": 687, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25123v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 884, \"height\": 1357, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25123v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 889, \"height\": 845, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25123v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 902, \"height\": 1189, \"label\": \"Table\"}]"
motivation: 现有RL策略耦合于训练目标，添加新反馈目标需重新训练，缺乏灵活性。
method: 将预训练RL策略作为MPPI的采样先验，通过MPPI代价函数在线修正先验，实现无需重训的任务适应性。
result: MuJoCo仿真中29自由度人形机器人实现280Hz控制，直线行走漂移减少，全身参考跟踪精度优于纯RL基线。
conclusion: RL引导MPPI框架结合了RL鲁棒性与MPPI任务灵活性，无需重新训练即可适应新反馈目标。
---

## 摘要
人形机器人在接触丰富的环境中需要既稳健又精确的全身控制器。虽然深度强化学习（RL）实现了稳健的稳定性，但其行为与训练目标和命令接口紧密耦合，使得在不重新训练的情况下难以添加新的反馈目标。在本研究中，我们提出了一种基于RL引导的全身模型预测路径积分（MPPI）框架，作为预训练RL策略之上的附加反馈控制器。我们并非将RL策略作为最终控制器，而是将其用作采样先验，使MPPI rollout偏向于动态可行的行为。任务目标通过模块化的MPPI成本项指定，MPPI通过在线持续纠正RL先验来满足这些目标，而无需重新训练策略，从而闭环控制。在MuJoCo中对29自由度的Unitree G1人形机器人的仿真展示了稳定的高频率控制（平均280 Hz）。在相同的命令接口下，所提出的方法比纯RL基线提高了任务级精度。这是通过在直行行走过程中纠正系统漂移以及跟踪通过成本施加的额外全身参考信号来实现的。

## Abstract
Humanoid robots require whole-body controllers that are both robust and precise in contact-rich environments. While deep reinforcement learning (RL) achieves robust stability, its behavior is tightly coupled to the training objective and command interface, making it difficult to add new feedback objectives without retraining. In this study, we propose an RL guided whole-body model predictive path integral (MPPI) framework that acts as an add-on feedback controller on top of a pretrained RL policy. Instead of using RL policy as the final controller, we use it as a sampling prior that biases MPPI rollouts toward dynamically feasible behaviors. Task objectives are specified through modular MPPI cost terms, and MPPI closes the loop by continuously correcting the RL prior online to satisfy these objectives without retraining the policy. Simulations on a 29-DoF Unitree G1 humanoid in MuJoCo demonstrate stable high-rate control (average 280~Hz). The proposed method improves task-level precision over a pure RL baseline under the same command interface. This is achieved by correcting systematic drift during straight walking and tracking additional whole-body reference signals imposed through the cost.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：人形机器人在接触丰富的环境中需要同时具备鲁棒性和精确性的全身控制器。深度强化学习（RL）能实现鲁棒的运动生成，但其行为与训练目标及命令接口高度耦合，导致添加新的反馈目标（如额外的全身参考信号、纠偏等）必须重新设计奖励并重训练，缺乏灵活性和可扩展性。模型预测控制（MPC）方法可以模块化地指定任务代价，但纯MPPI对采样分布敏感、在高维人形机器人上样本效率低，且难以处理接触非光滑性。
- **整体含义**：本文提出将预训练RL策略作为采样先验，与模型预测路径积分（MPPI）结合，形成即插即用的附加反馈控制器。RL策略保持不变，MPPI通过在线修正策略输出来满足新任务目标，无需重训练。该方法旨在融合RL的鲁棒性和MPC的灵活目标指定能力。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：使用一个预训练的RL运动策略（velocity-tracking locomotion policy）输出名义关节位置参考 \(q_{RL}\)，将其作为MPPI采样分布的均值。MPPI在该均值周围施加高斯噪声，生成候选控制序列，通过物理引擎（MuJoCo）模拟获得rollout轨迹及代价，再利用重要性权重更新优化控制序列。MPPI的代价函数可模块化地添加新任务目标，从而在不改变RL策略的情况下实现闭环修正。
- **关键技术细节**：
  - **三频率异步控制栈**：
    - RL策略：50 Hz，输出名义关节位置参考。
    - MPPI：异步循环，规划完成后输出更新的期望关节指令，平均有效更新率约280 Hz（CPU并行）。
    - 底层PD控制器：500 Hz，跟踪MPPI的输出。
  - **Knot-based MPPI**：使用H个节点（文中H=2）参数化控制序列，通过三次样条插值恢复完整时序，降低优化维度。
  - **RL作为采样先验**：采样噪声 \(\epsilon \sim \mathcal{N}(0, \nu I)\) 叠加在RL输出上，使得候选轨迹集中在动态可行区域。
  - **物理引擎rollout**：利用MuJoCo进行前向仿真，隐式处理接触动力学，无需解析导数。
  - **代价函数**：可组合运行代价和终端代价。对于直行走任务，代价包括横向位置偏差和航向偏差；对于下蹲任务，包括基座高度跟踪项。
  - **重要性权重更新**：标准MPPI公式（公式8），使用指数加权平均聚合采样扰动。
- **算法流程**（Algorithm 1）：
  1. 初始化名义节点。
  2. 循环：
     - 读取状态，若RL更新则获取新节点。
     - 当规划完成时：对每个采样，生成噪声，形成节点序列，插值得到控制序列，rollout并计算代价，计算重要性权重，更新节点。
     - 取第一个控制指令执行。
  3. PD控制器跟踪期望关节位置。

## 3. 实验设计

- **仿真场景**：MuJoCo，Unitree G1人形机器人（29自由度）。
- **RL策略**：基于PPO在IsaacLab中独立训练的命令速度跟踪行走策略，输出12个下肢关节位置。策略未经修改直接用于MPPI先验。
- **MPPI设置**：rollout数 N=128，预测时域 0.02s，控制步长 dt=0.002s，节点数 H=2，噪声标准差 \(\sigma=0.2\)。
- **对比方法**：纯RL基线（相同的RL策略直接作为控制器，无MPPI修正）。
- **任务场景**：
  - (a) **直行走纠偏**：给定命令 \((v_x^{des}=1, v_y^{des}=0, \dot{\psi}^{des}=0)\)。RL策略存在系统性左-右偏差导致漂移。MPPI添加横向位置和航向代价。评估横向位置RMSE、x/y速度跟踪RMSE。
  - (b) **下蹲任务**：添加基座高度跟踪的代价，参考高度在0.71-0.78m之间变化。RL命令接口不包含高度指令。定性地展示跟踪效果和快照。

## 4. 资源与算力

- **文中明确说明**：
  - CPU：Intel Core i9-14900KF，32GB RAM。
  - GPU：NVIDIA GeForce RTX 4070 Ti（仅提及存在，未说明用于何种计算）。
  - rollout利用CPU并行（通过MJPC）。
  - 平均有效更新率约280 Hz，在CPU上实现。
- **未提及**：RL策略训练所需的具体算力（GPU型号、训练时长等）、MPPI参数调优的算力开销。仅说明RL策略在IsaacLab中训练，未提供训练细节。

## 5. 实验数量与充分性

- **实验数量**：文中仅展示两个任务（直行走纠偏、下蹲任务）的定量或定性结果。直行走给出了RMSE和时域图；下蹲任务只给出了基座高度跟踪曲线和快照。
- **是否充分**：
  - 未进行消融实验（如不同节点数H、不同rollout数N、不同噪声方差的影响）。
  - 未对比其他混合方法（如基于梯度MPC、其他采样先验）。
  - 未在多个不同策略或不同机器人上验证。
  - 缺乏鲁棒性测试（如外部扰动、地形变化）。
- **客观性**：指标清楚（RMSE），但仅在一个场景下对比一个基线，存在片面风险。下蹲任务没有量化跟踪误差，仅展示曲线对比。

## 6. 主要结论与发现

- RL引导的MPPI在直行走任务中将横向位置误差RMSE从纯RL的0.339m降低到0.022m，显著抑制漂移。
- 前向速度跟踪性能相当（RMSE 0.773 vs 0.806），横向速度跟踪改进（0.021 vs 0.046 m/s）。
- 在不修改RL策略接口的情况下，成功实现基座高度跟踪（下蹲任务），证明了任务增强能力。
- 实现了平均280 Hz的实时控制频率，验证了计算可行性。

## 7. 优点

- **巧妙解耦**：将RL策略作为采样先验，MPPI仅通过代价修正，无需重训练即可适应新任务，即插即用。
- **物理引擎导向**：利用物理仿真隐含处理接触，避免人工设计接触约束。
- **模块化代价设计**：允许灵活组合任务目标，可扩展性强。
- **异步高效实现**：CPU并行rollout达到280 Hz，对计算资源要求相对低。
- **清晰的问题定义**：针对RL策略的系统性偏差和新任务增强需求，提供了具体解决方案。

## 8. 不足与局限

- **实验覆盖不足**：仅有两个场景，未在多任务、多策略、多难度的泛化能力上进行系统评估。消融实验缺失。
- **偏差风险**：RL策略可能存在其他未暴露的偏差（如不同速度下的表现），且MPPI的代价权重（如βy、βz）需要手动调整，可能影响公平性。
- **应用限制**：框架行为受限于RL先验；若期望的行为偏离先验太远，可能需要多先验或自适应机制。文中未处理更大范围的行为变化。
- **未提及现实部署**：仿真中假设完美状态估计和动力学匹配，未讨论对建模误差、接触估计误差的鲁棒性。
- **算力细节缺漏**：缺乏RL训练所需的资源和时间，无法评估整体系统开发成本。
- **效率对比缺失**：未与纯MPPI（无RL先验）对比样本效率和性能，未说明RL先验带来的具体提升量。

（完）
