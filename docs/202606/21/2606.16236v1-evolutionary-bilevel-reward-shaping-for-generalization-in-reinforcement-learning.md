---
title: Evolutionary Bilevel Reward Shaping for Generalization in Reinforcement Learning
title_zh: 进化双层奖励塑形用于强化学习泛化
authors: "Ekasit Usaratniwart, Xilin Gao, Marc Ong, Youhei Akimoto"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16236v1"
tags: ["query:inverse-rl"]
score: 8.0
evidence: 进化双层奖励塑形用于强化学习泛化
tldr: 强化学习在环境变化时泛化能力差，现有域随机化方法需充分轨迹数据，但隐私场景下仅能获取标量性能。本文提出GERS，一种双层进化奖励塑造方法：下层RL代理用成形奖励在有限训练环境学习，上层CMA-ES优化奖励参数以最大化验证环境的未成形奖励。连续控制任务中，GERS在未见测试环境上超越标准RL基线，且性能与域随机化相当，而后者需轨迹访问。该方法在数据访问受限下有效提升泛化。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16236v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1262, \"height\": 919, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16236v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1263, \"height\": 922, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16236v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1122, \"height\": 1300, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16236v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1267, \"height\": 1046, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.16236v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1262, \"height\": 906, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.16236v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1269, \"height\": 257, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16236v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16236v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1269, \"height\": 258, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16236v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1271, \"height\": 608, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16236v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 961, \"height\": 337, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.16236v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 955, \"height\": 533, \"label\": \"Table\"}]"
motivation: 现有RL泛化方法如域随机化需要轨迹数据，在隐私保护场景下不可行，因此亟需仅利用标量反馈的泛化方法。
method: 提出GERS双层优化框架：下层RL代理使用上层奖励参数化函数学习策略；上层CMA-ES进化算法优化奖励参数，最大化验证环境的累积未成形奖励。
result: 在连续控制任务中，GERS在未见测试环境上显著优于标准RL基线，且性能与域随机化相当，但域随机化需要轨迹访问而GERS不需要。
conclusion: GERS在数据访问受限场景下有效提升了强化学习泛化能力，验证了双层进化奖励塑造的可行性。
---

## 摘要
强化学习（RL）在部署到与训练环境不同的环境中时，往往会出现性能下降。现有的技术如域随机化（DR）可以缓解这一问题，但需要访问多样化的训练环境和完整的轨迹可观测性，这些假设在隐私保护或受限场景中无法成立，因为此类场景仅能提供标量性能指标。我们提出了通过进化奖励塑形实现泛化（GERS）——一种双层优化方法，仅利用来自验证环境的标量反馈来改进在未见测试环境上的泛化能力。在下层，由上层塑造的奖励函数引导的RL智能体在可访问轨迹数据的有限训练环境集上学习策略；在上层，CMA-ES优化奖励塑形参数，以最大化无法访问轨迹的独立验证环境上的累积未塑形奖励。连续控制任务的结果表明，GERS在未见测试环境上优于标准RL基线。尽管DR将GERS的训练和验证环境组合视为一个需要轨迹访问的单一训练集，而GERS无法访问验证轨迹，但GERS的性能与DR相当。这些结果证实，GERS在受限数据访问约束下有效增强了泛化能力。

## Abstract
Reinforcement learning (RL) often suffers from performance degradation when deployed in environments that differ from those encountered during training. Existing techniques such as domain randomization (DR) mitigate this, but require access to diverse training environments and full trajectory observability, assumptions that fail in privacy-preserving or restricted scenarios where only scalar performance metrics are available. We propose Generalization via Evolutionary Reward Shaping (GERS), a bilevel optimization approach to improve generalization on unseen test environments using only scalar feedback from validation environments. At the lower level, an RL agent guided via a reward function shaped by the upper level learns a policy on a limited set of training environments with accessible trajectory data; at the upper level, CMA-ES optimizes the reward shaping parameters to maximize the cumulative unshaped reward on separate validation environments for which trajectory access is unavailable. Results on continuous control tasks indicate that GERS outperforms the standard RL baseline on unseen test environments. GERS performance is comparable to DR, despite DR treating the combined set of training and validation environments of GERS as a single training set that requires trajectory access, whereas GERS cannot access validation trajectories. These results confirm that GERS effectively enhances generalization under restricted data access constraints.

---

## 论文详细总结（自动生成）

# 论文中文总结

## 1. 核心问题与整体含义
强化学习（RL）在实际部署中常因环境变化（如动力学参数改变）而性能下降，即泛化能力差。现有提升泛化的方法如域随机化（DR）需要访问大量训练环境及其完整轨迹数据，但在隐私保护或受限场景（如自动化红队测试）中，仅能获得标量性能指标（如累积奖励），无法获取内部轨迹。因此，论文提出 **GERS（Generalization via Evolutionary Reward Shaping）**，一种仅利用验证环境标量反馈来优化训练奖励函数，从而提升策略在未知测试环境泛化性能的双层优化方法。

## 2. 方法论：核心思想与关键技术

### 核心思想
- **双层优化架构**：下层使用PPO在**训练环境**（可获取轨迹）上学习策略，奖励函数由上层参数化；上层使用CMA-ES进化策略优化奖励塑形参数，以最大化在**验证环境**（仅标量奖励，无法获得轨迹）上的累积未塑形奖励。
- **奖励塑形函数**：线性参数化，`ŕ_ν(s,a) = r(s,a) + ⟨ν, ϕ(s,a)⟩`，其中`ϕ(s,a)=[s; a]`为状态-动作特征向量，ν为待优化参数。
- **加速策略**：预训练策略作为每个下层PPO的初始值；并行评价（λ=60个候选解）；遗传策略（选择验证分最高的策略作为下一代的初始策略）。

### 关键技术细节
- 下层：PPO，训练步数K=81920步。
- 上层：dd-CMA-ES，种群大小60，迭代100代，初始步长0.1。
- 训练环境：1个固定环境；验证环境：25个环境（均匀采样物理参数c∈[0.1,3.0]），每环境4 episode，共100 episode/候选解。
- 参数约束：ν∈[-5,5]^d_ν，采用镜像处理边界。

### 公式与算法流程（文字说明）
1. 预训练PPO获取初始策略θ^(1)。
2. 每代g，采样λ个候选ν_i；并行运行PPO（K步），使用成形奖励ŕ_ν训练策略π_{θ_i}。
3. 在验证集上评价各策略，得f(ν_i)=J_val(π_{θ_i})。
4. 用{(ν_i, f(ν_i))}更新CMA-ES分布。
5. 选择验证分最高的策略作为下一代初始策略θ^(g+1)。
6. 重复2-5直到G代结束。

## 3. 实验设计

### 使用场景与数据集
- **环境**：MuJoCo v5的四个连续控制任务：InvertedPendulum、Hopper、HalfCheetah、Ant。
- **环境参数变化**：对每个任务选择两个物理参数（如质量、摩擦、重力），乘数c∈[0.1,3.0]构成不同环境。
- **数据划分**：
  - 训练环境：1个（默认参数c=1.0）。
  - 验证环境：25个（c在0.1~3.0均匀采样）。
  - 测试环境：100个（c同上，与验证集独立采样）。

### Benchmark与对比方法
- **BasePPO**：仅在单个训练环境训练3×10^7步，作为基线。
- **Domain Randomization (DR)**：将GERS的25个验证环境+1个训练环境共26个环境作为DR的训练集，训练5×10^7步。DR可以获得全部轨迹数据。
- **GERS**：仅能获得验证环境的标量奖励，轨迹不可见。

### 评价指标
- 测试性能：在100个未见环境中，每环境4 episode，平均累积奖励。

## 4. 资源与算力
论文**未明确说明**所使用的GPU型号、数量或训练时长。仅提及：
- 预训练PPO：3×10^7步。
- 每次下层PPO：K=81920步，总计算量约为 G×λ×K = 100×60×81920 ≈ 4.9×10^8步（不含预训练）。
- 使用并行评价（60路），但未提及硬件细节。

## 5. 实验数量与充分性

### 实验组数
- 每个任务独立运行**5次试验**，报告均值和标准差。
- 每个试验包含G=100代CMA-ES进化过程。
- 无专门的消融实验（仅在附录讨论了CMA-ES收敛问题）。
- 未隔离奖励塑形效果与“best-of-many”选择效果。

### 充分性与公平性
- **充分性**：实验覆盖了四个不同复杂度的连续控制任务，验证了跨任务的一致性。但仅限连续控制、稠密奖励环境，未评估稀疏奖励或离散/视觉任务。
- **公平性**：GERS与DR对比时，DR使用了更多数据（全轨迹），但二者在测试集上性能相当，体现了GERS在受限条件下优势。统计检验使用了Wilcoxon秩和检验+Bonferroni校正，但由于样本量（5 trials）过小，结果未能达到统计显著性（p值>0.00625）。因此结论的统计可靠性有限。
- **客观性**：实验设计合理，但缺乏更多高计算量的独立试验（受限于计算成本）。

## 6. 主要结论与发现
- GERS在所有任务上均优于BasePPO基线，显著提升泛化性能。
- GERS与DR性能相当：在InvertedPendulum和Hopper上甚至超过DR；在HalfCheetah和Ant上略低于DR，但差异不具有统计显著性。
- 验证了在**仅能获得验证环境标量反馈**的严格限制下，通过进化奖励塑形可以有效引导策略学习，使其在未知测试环境上表现良好。
- 策略的验证性能与测试性能之间没有出现验证过拟合，说明学习到的奖励信号具有较好的迁移能力。

## 7. 优点
- **方法新颖**：提出一种适用于隐私保护/受限数据访问场景的泛化方法，突破了现有方法对轨迹数据的依赖。
- **问题设定实际**：针对现实中常见但研究较少的“仅标量反馈”场景，具有重要应用价值（如自动化渗透测试、联邦学习等）。
- **算法高效设计**：采用预训练策略初始化、并行评价、遗传选择等技巧，降低计算开销。
- **性能竞争力强**：在多个连续控制任务上达到与全轨迹访问方法（DR）相当的性能。

## 8. 不足与局限
- **任务覆盖窄**：仅在MuJoCo连续控制、稠密奖励任务上验证，未探索稀疏奖励、离散动作或视觉任务，限制适用性。
- **实验统计不充分**：每组仅5次独立试验，导致统计检验效力不足，无法确认性能提升的显著性。
- **消融分析缺失**：未隔离奖励塑形本身与“best-of-many”选择效果，无法确定性能提升的真正来源。
- **上层CMA-ES收敛问题**：由于下层训练步数有限导致评价噪声大，CMA-ES分布未收敛，可能影响优化质量。
- **缺少更多对比基线**：未与UED、数据增强、元学习等方法比较。
- **计算成本高**：总训练步数大，可能无法扩展到更复杂环境。

（完）
