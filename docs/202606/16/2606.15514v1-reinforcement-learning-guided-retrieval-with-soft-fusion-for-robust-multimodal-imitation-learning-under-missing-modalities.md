---
title: Reinforcement Learning-Guided Retrieval with Soft Fusion for Robust Multimodal Imitation Learning under Missing Modalities
title_zh: 基于强化学习引导的软融合鲁棒多模态模仿学习处理缺失模态
authors: "Hassan Ismkhan, Hamid Bouchahcia"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15514v1"
tags: ["query:inverse-rl"]
score: 8.0
evidence: 缺失模态下的模仿学习
tldr: 机器人多模态感知中传感器可能故障导致模态缺失。本文提出RL4IL，结合强化学习检索与软融合，在训练库中筛选最相关示范并聚合动作信号；针对缺失模态，通过专用检索策略和交叉注意力重构嵌入，无需重新训练。在LIBERO基准上显著优于现有方法，为鲁棒模仿学习提供新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15514v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1536, \"height\": 467, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15514v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1532, \"height\": 469, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15514v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1710, \"height\": 591, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15514v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1652, \"height\": 494, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15514v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1921, \"height\": 399, \"label\": \"Table\"}]"
motivation: 解决多模态模仿学习在传感器缺失场景下的鲁棒性不足问题。
method: 利用强化学习策略检索最相关示范，通过软交叉注意力融合动作信号，缺失时借助检索重构嵌入。
result: 在LIBERO基准测试中大幅超越现有方法，且无需重新训练策略网络。
conclusion: RL4IL实现了无需额外训练的鲁棒多模态模仿学习框架。
---

## 摘要
机器人系统通过多种输入模态感知世界——包括视觉摄像头流和自然语言指令——并必须基于这些信号选择适当的动作。然而，假设所有输入设备永久可用是不现实的，因为传感器可能在部署过程中发生故障、被遮挡或完全掉线。因此，鲁棒处理此类缺失模态场景对于真实世界的机器人操作至关重要。本文提出RL4IL，一种基于强化学习引导的模仿学习方法，通过从训练库中识别最相关的专家演示，为给定观察选择最合适的动作。通过利用广度优先搜索候选集上的近端策略优化训练的强化学习策略，对候选演示进行排序，并通过软交叉注意力融合头聚合其动作信号以产生最终预测。当推理时某一模态缺失时，专用的每模态RL检索策略从训练库中识别捐赠演示，并通过一个软插补头利用顶部排名捐赠者的交叉注意力重建缺失嵌入——无需任何系统重新训练。在三个LIBERO基准套件上的实验表明，RL4IL在传感器掉线条件下显著优于最先进的模仿学习方法，同时无需策略网络训练。代码可在https://github.com/h-ismkhan/Reinforcement-Learning-via-kNN-for-Robotic-Learning-with-Missing-Camera找到。

## Abstract
Robotic systems perceive the world through multiple input modalities -- including visual camera streams and natural language instructions -- and must select appropriate actions based on these signals. However, assuming the permanent availability of all input devices is unrealistic, as sensors may fail, become occluded, or drop out entirely during deployment. Robust handling of such missing-modality scenarios is therefore essential for real-world robot operation. This paper introduces RL4IL, a reinforcement learning guided method for imitation learning that selects the most suitable action for a given observation by identifying the most relevant expert demonstrations from a training library. A reinforcement learning policy, trained via Proximal Policy Optimisation over Breadth-First Search candidate sets, ranks candidate demonstrations and a soft cross-attention fusion head aggregates their action signals to produce the final prediction. When a modality is missing at inference time, a dedicated per-modality RL retrieval policy identifies donor demonstrations from the training library, and a soft imputation head reconstructs the missing embedding via cross-attention over the top-ranked donors -- without requiring any retraining of the system. Experiments on three LIBERO benchmark suites demonstrate that RL4IL substantially outperforms state-of-the-art imitation learning methods under sensor dropout conditions, while requiring no policy network training. The code can be found at https://github.com/h-ismkhan/Reinforcement-Learning-via-kNN-for-Robotic-Learning-with-Missing-Camera

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **核心问题**：机器人多模态模仿学习系统在部署时，传感器（如摄像头）可能因硬件故障、遮挡等导致模态缺失，现有方法假设所有模态始终可用，鲁棒性不足。
- **研究动机**：真实机器人操作中传感器掉线不可避免，而现有缺失模态处理方法（如Masked训练、多视角解耦）要么需要重新训练策略网络，要么在完全摄像头缺失时性能崩溃（如DisDP仅0.295成功率）。
- **整体目标**：提出无需任何策略网络重新训练、能在零样本条件下处理任意摄像头缺失的多模态模仿学习方法，同时保持高任务成功率。

## 2. 论文提出的方法论
### 核心思想
- **检索+融合**：利用强化学习（RL）从训练库中智能检索最相关的专家演示，并通过软交叉注意力融合多个候选演示的动作信号，替代传统硬选择或距离加权聚合。
- **缺失模态处理**：为每个可能缺失的模态训练专用RL检索策略，选出来自训练集的“捐赠演示”，通过软插补头用交叉注意力重建缺失嵌入，恢复完整观测表示，下游预测管道无需改动。

### 关键技术细节
- **整体框架**：冻结预训练编码器（CLIP ViT-B/32）提取各模态嵌入；通过模态公平归一化和距离度量构建k-NN候选集；采用广度优先搜索（BFS）扩充候选集，增加标签多样性。
- **RL检索策略（PPO）**：为每个训练样本构建BFS候选集（深度限制D=6），定义最优动作（标签匹配的最小图距离节点），使用注意力式打分网络（MLP）为每个候选打分，通过softmax输出策略分布。奖励定义为：选择动作与最优动作的损失差（分类任务为0/1损失），范围{ -1, 0}。使用PPO优化，无价值基线，熵正则化。
- **软融合头**：保留RL策略排序的前K′（=32）个候选，通过多头交叉注意力（H=4头，df=128）计算每个候选的权重，聚合得到软标签（回归：加权平均后经MLP精炼；分类：加权平均one-hot后经MLP输出logits）。
- **缺失模态插补**：为每个摄像头模态训练独立RL策略（50 PPO epoch），从该模态可用的训练样本中选捐赠者；使用BFS候选集和基于L2距离的排名奖励；软插补头（K′_imp=32，H_imp=2，dimp=64）通过交叉注意力聚合顶部捐赠者嵌入，训练使用MSE损失（模拟缺失模态，监督信号为真实嵌入）。
- **推理流程**：缺失模态→冷冻RL检索→软插补重建→完整嵌入→主RL检索+软融合→动作序列。

### 公式/算法（文字说明）
- 模态公平距离：d(i,j)= sqrt( (1/M) * Σ_m ( ||z̃_i^(m)-z̃_j^(m)||² / d_m ) )，其中z̃为z-score归一化后的嵌入。
- 策略分布：π(a=i|s)=exp(score(v_i)) / Σ_j exp(score(v_j))，score由MLP([h_s; h_v; h_s⊙h_v])计算。
- PPO优化目标：JPPO = E[ min(ρ_t A_hat, clip(ρ_t,1-ε,1+ε) A_hat) ] + α_ent H(π)，单步优势A_hat = r(s,a)。
- 软融合：注意力权重α_j = softmax( (Q K^T) / sqrt(d_f) )，软标签回归：ŷ = MLP([c; Σα_j y_j])；分类：类似，p̃=Σα_j e_j。

## 3. 实验设计
### 数据集/场景
- **LIBERO基准套件**：LIBERO-Spatial（空间推理，10任务）、LIBERO-Object（物体操作，10任务）、LIBERO-Goal（目标条件，10任务）。统一观测空间：agent-view RGB摄像头、in-hand RGB摄像头、自然语言指令（共3模态）。
- **缺失条件**：No Cam0（agent-view缺失）、No Cam1（in-hand缺失），每个条件评估所有任务。

### 对比方法
- BC (Behavior Cloning)，DisBC (Disentangled BC)，BESO-ACT，BESO-ACT-Dropout（带模态丢失训练），DisDP (Disentangled Diffusion Policy)——共5种基线。

### 评估指标
- **任务成功率**（%）：每个种子25次rollout，3个种子平均，总计75次/任务，步数上限260。

## 4. 资源与算力
- **论文未明确说明GPU型号、数量和具体训练时长**。仅在致谢中提及使用了Bournemouth大学的GPU资源，未提供详细算力信息。RL策略训练为30 epoch（预测）和50 epoch（插补），融合头训练30 epoch，每条任务数据规模较小（每套50演示/任务）。

## 5. 实验数量与充分性
- **实验数量**：在3个LIBERO套件（共30任务）上，每个套件评估2种缺失条件（No Cam0 / No Cam1），共计6种设置。每种设置报告成功率及标准差（3种子）。
- **消融实验**：3组消融——①软融合 vs 硬argmax（图1）；②模态公平归一化 vs 无归一化（图2）；③训练epoch数影响（图3，1~20 epoch）；④top-K融合候选数影响（图4，K∈{4,8,12,16,32}）。
- **充分性与公平性**：
  - 充分：覆盖了主任务和关键设计因素消融，但缺少对缺失模态比例、动态缺失（如运行中缺失恢复）的实验；未涉及其他类型传感器（如深度、触觉）。
  - 公平：所有基线在相同缺失设置下比较，RL4IL无需重新训练，基线使用原论文官方代码或合理重实现；但基线DisDP仅报告了部分缺失结果（如No Cam0下0.295），完整对比有缺失。

## 6. 论文的主要结论与发现
- RL4IL在所有缺失条件下显著优于所有基线，最高成功率0.733（LIBERO-Object No Cam0），而最佳基线DisDP仅0.295。
- 软融合始终优于硬argmax，证明聚合多个候选动作信号能补偿检索噪声。
- 模态公平归一化消除高维模态主导距离的问题，提升一致性和可预测性。
- 即使仅训练1个epoch，RL4IL仍超越所有基线；增加epoch数在LIBERO-Goal上持续提升，其他套件稳定。
- 软插补头能有效重建缺失模态嵌入，使下游检索正常进行。
- 核心优势：无需任何策略网络训练或微调，零样本处理传感器缺失。

## 7. 优点
- **创新性**：首次将强化学习应用于模仿学习中的最近邻选择，替代启发式距离加权；软融合头聚合多个候选而非硬选，提升鲁棒性。
- **实用性**：无需重新训练策略，适用于真实部署中不可预测的传感器故障；冻结编码器，轻量RL和融合头，计算开销小。
- **实验全面**：覆盖3个不同难度的LIBERO套件，消融实验验证每个组件贡献。
- **代码开源**：提供GitHub仓库，可复现性高。

## 8. 不足与局限
- **实验覆盖**：仅在LIBERO上测试，更多样化场景（如真实机器人、不同任务集、动态缺失、多种传感器同时缺失）未验证。
- **模型假设**：依赖训练库质量，若库中无相似任务相关演示，检索可能失败；当前仅支持2个摄像头缺失（agent/in-hand），未讨论语言指令缺失或多模态同时缺失场景。
- **超参数敏感度**：虽对top-K不敏感，但BFS深度、k-NN大小等未充分调优分析；模态公平归一化中的epsilon clamp值未讨论。
- **资源与算力不清**：缺少计算资源细节，影响可复现性评价。
- **性能上限**：开放循环执行检索到的演示动作序列，任务成功完全依赖检索质量，无法通过闭环修正误差，可能限制复杂长序列任务。

（完）
