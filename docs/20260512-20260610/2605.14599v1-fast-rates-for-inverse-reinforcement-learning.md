---
title: Fast Rates for Inverse Reinforcement Learning
title_zh: 逆强化学习的快速收敛率
authors: "Andreas Schlaginhaufen, Maryam Kamgarpour"
date: 2026-05-14
pdf: "https://arxiv.org/pdf/2605.14599v1"
tags: ["query:inverse-rl"]
score: 10.0
evidence: 逆强化学习的快速收敛率理论分析
tldr: 针对有限域MDP中线性奖励类的熵正则化min-max逆强化学习，本文证明其与MLE在总体和确定性动力学经验下等价。利用伪自协调性，推导出轨迹级KL散度和参数误差在Hessian范数下以O(1/n)快速收敛，无需探索假设且容忍模型误设。还扩展了奖励可识别性并给出软最优值函数导数新结果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.14599v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 963, \"height\": 180, \"label\": \"Figure\"}]"
motivation: 现有逆强化学习方法收敛速度慢或需强假设，本文旨在建立统计高效、鲁棒的Min-Max-IRL理论。
method: 通过熵正则化min-max优化与MLE等价性，利用伪自协调性分析损失函数，导出快速收敛率。
result: 在模型误设下，KL散度和参数误差均以O(1/n)速率衰减，无需探索假设；扩展了奖励可识别性。
conclusion: 所提方法实现了快速统计收敛，拓展了逆强化学习的适用性和理论完备性。
---

## 摘要
我们为有限时域、具有Borel状态和动作空间的MDP中的线性奖励类建立了熵正则化的最小-最大逆强化学习（Min-Max-IRL）的全新结构和统计结果。在结构方面，我们表明在总体层面上，以及在确定性动力学下的经验层面上，最大似然估计（MLE）和Min-Max-IRL是等价的。在统计方面，利用Min-Max-IRL损失的伪自和谐性，我们证明了轨迹级KL散度和Hessian范数下的平方参数误差均以快速率$\mathcal{O}(n^{-1})$衰减，其中$n$是专家轨迹的数量。我们的保证适用于误设情况，且无需探索假设。我们进一步将奖励可识别性结果推广到一般Borel空间，并推导了关于软最优值函数对奖励参数导数的新结果。

## Abstract
We establish novel structural and statistical results for entropy-regularized min-max inverse reinforcement learning (Min-Max-IRL) with linear reward classes in finite-horizon MDPs with Borel state and action spaces. On the structural side, we show that maximum likelihood estimation (MLE) and Min-Max-IRL are equivalent at the population level, and at the empirical level under deterministic dynamics. On the statistical side, exploiting pseudo-self-concordance of the Min-Max-IRL loss, we prove that both the trajectory-level KL divergence and the squared parameter error in the Hessian norm decay at the fast rate $\mathcal{O}(n^{-1})$, where $n$ is the number of expert trajectories. Our guarantees apply under misspecification and require no exploration assumptions. We further extend reward-identifiability results to general Borel spaces and derive novel results on the derivatives of the soft-optimal value function with respect to reward parameters.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：逆强化学习（IRL）旨在从专家演示中恢复奖励函数，从而训练出模仿专家行为的策略。现有IRL方法在统计效率上存在短板：通常收敛速度慢（$\mathcal{O}(n^{-1/2})$），依赖于强探索假设，且理论分析多局限于有限状态-动作空间。这不仅限制了实际应用（如机器人操作、语言模型对齐），也使得对熵正则化这一现代IRL核心机制的理解不够深入。
- **研究动机**：与行为克隆（BC）相比，IRL能利用MDP动态信息，理应获得更强的统计保证，但已有结果远未达到这一预期。本文旨在填补这一理论空白，证明熵正则化Min-Max-IRL能够实现与MLE类似的快速收敛率（$\mathcal{O}(n^{-1})$），且无需探索假设、允许模型误设，从而为IRL提供更坚实的理论基础。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：
  1. **结构等价性**：在总体层面，Min-Max-IRL与MLE在软最优策略类$\Pi^*(\mathcal{R})$上等价；在确定性动力学下，这一等价性在经验层面也成立。这一发现允许将IRL问题转化为密度估计问题，从而利用MLE的统计性质。
  2. **伪自和谐性**：证明Min-Max-IRL损失函数$L_{\text{IRL}}(r_\theta)$满足伪自和谐性（Pseudo Self-Concordance），即其三阶导数能被二阶导数控制：
     \[
     |D^3 J^*(\theta)[\xi,\xi,\zeta]| \le \beta^{-1} B_{A\phi} \|\zeta\|\, D^2 J^*(\theta)[\xi,\xi]
     \]
     其中$J^*$为最优值函数，$\beta$为熵正则化系数，$B_{A\phi}$为累积特征优势上界。
- **关键技术细节**：
  - **局部化分析**：在Hessian正定性假设下，利用伪自和谐性证明一旦样本量足够大，经验最小化$\hat\theta_n$会落入围绕$\theta^*$的Dikin椭球内，在该区域内损失函数是强凸的。
  - **向量Bernstein不等式**：通过该不等式控制样本协方差与总体协方差的差异，进而导出$\|\hat\theta_n - \theta^*\|_{H^*}^2$与KL散度的上界。
  - **参数估计与KL等价性**：在局部区域内，证明平方参数误差（Hessian范数）、轨迹级KL散度、平方Hellinger距离三者互相等价，使得所有度量都能以$\mathcal{O}(d_*/n)$速率衰减。
- **公式与算法流程**：
  - 风险函数：$\hat L_n^{\text{IRL}}(\theta) = J^*(\theta) - \frac{1}{n}\sum_{i=1}^n \sum_{t=1}^T \langle\theta,\phi_t(s_t^i,a_t^i)\rangle$
  - Hessian：$H(\theta) = \nabla^2 J^*(\theta) = \beta^{-1}\sum_t \mathbb{E}_{\pi^*_\theta}[A_{t,\phi}^{\pi^*_\theta,0}(A_{t,\phi}^{\pi^*_\theta,0})^\top]$
  - 有效维数：$d_* = \text{tr}(\Sigma_E H_*^{-1})$，其中$\Sigma_E$为专家轨迹下特征回报的协方差。
  - 算法流程（文字描述）：给定专家轨迹集$\mathcal{D}_E^n$和线性奖励类$\Theta$，求解凸优化问题$\min_{\theta\in\Theta} \hat L_n^{\text{IRL}}(\theta)$得到$\hat\theta_n$，然后输出软最优策略$\pi^*_{\hat\theta_n}$作为模仿策略。

#### 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法
- **实验情况**：本文为纯理论论文，未进行任何数值实验或仿真验证。文中没有提及任何数据集、基准场景或对比方法。所有结果均为数学定理与推论。

#### 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。
- **资源与算力**：文中未提及任何与计算资源、GPU型号、训练时长相关的信息。作为理论性工作，本文未涉及实际计算需求。

#### 5. 实验数量与充分性：大概做了多少组实验（如不同数据集、消融实验等），这些实验是否充分、是否客观、公平。
- **实验数量与充分性**：由于无实验，无法评估实验的充分性。但本文提供了完整的理论证明和推导，包括多个引理、定理和推论，并在附录中给出了详细证明步骤。从理论分析角度看，证明是严谨而充分的，覆盖了有误设和无误设情形、确定性/随机动力学、以及一般Borel空间。

#### 6. 论文的主要结论与发现
- **主要结论**：
  1. **快速收敛率**：在可误设、无探索假设条件下，Min-Max-IRL的轨迹级KL散度$\text{KL}(P^{\pi_E}\|P^{\hat\pi_n})$和参数误差$\|\hat\theta_n - \theta^*\|_{H^*}^2$均以$\mathcal{O}(d_*/n)$速率衰减，剩余项为$\mathcal{O}(1/n^2)$。
  2. **结构等价性**：Min-Max-IRL与MLE在总体层面等价；在确定性动力学下经验层面亦等价。
  3. **奖励可识别性**：将可识别性结果从有限状态-动作空间推广到一般Borel空间，并揭示了Hessian核与不可识别子空间的关系。
  4. **导数新结果**：推导了软最优值函数对奖励参数的一、二、三阶导数解析表达式，这些结果在表格设定中也是新的。

#### 7. 优点：方法或实验设计上有哪些亮点
- **方法论亮点**：
  - **结构等价性洞察**：首次严格建立了Min-Max-IRL与MLE之间的联系，为后续研究提供了桥梁。
  - **伪自和谐性的应用**：利用该性质将非凸问题局部转化为强凸问题，从而获得快速收敛率，该技术手段新颖且通用。
  - **无探索假设、容忍误设**：现有工作（如Schlaginhaufen & Kamgarpour 2024）需要探索假设，本文完全去除，实用性更强。
  - **理论覆盖范围广**：结论适用于Borel状态-动作空间，而非仅限于有限离散空间，更具一般性。
- **理论亮点**：
  - 得到$\mathcal{O}(1/n)$的速率，优于传统$\mathcal{O}(1/\sqrt{n})$，且与最优MLE行为克隆相当。
  - 给出了参数误差与KL散度、Hellinger距离之间的局部等价性，提供了统一的度量视角。

#### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **不足与局限**：
  - **缺乏实验验证**：纯粹的理论分析缺少数值实验，无法验证理论界的紧性以及在实际问题（如高维连续控制、复杂奖励设计）中的有效性。
  - **假设限制**：要求奖励函数为线性参数化，且特征有界；实际中复杂奖励可能高度非线性。Hessian正定性假设（Assumption 4.2）可能在某些退化问题中不成立，尽管论文指出可通过等价类避免，但使用时仍需验证。
  - **计算实现未涉及**：论文未讨论Min-Max-IRL的具体优化算法（如如何处理内层最大化、如何保证求解效率），仅论证统计性质。
  - **动力学依赖**：经验层面的等价性仅在确定性动力学下成立；对于随机动力学，MLE-IRL与Min-Max-IRL存在差异，且MLE-IRL被证明非凸，这限制了直接应用。
  - **状态/动作空间连续性**：尽管理论允许Borel空间，但分析依赖于可测性和有界性等抽象条件，对实践者而言转化难度较大。

（完）
