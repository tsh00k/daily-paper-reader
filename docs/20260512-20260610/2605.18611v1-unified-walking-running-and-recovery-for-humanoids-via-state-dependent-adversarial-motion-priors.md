---
title: "Unified Walking, Running, and Recovery for Humanoids via State-Dependent Adversarial Motion Priors"
title_zh: 通过状态依赖的对抗运动先验实现类人机器人的统一行走、奔跑与恢复
authors: "Yidan Lu, Yichao Zhong, Liu Zhao, Wanyue Li, Peng Lu"
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.18611v1"
tags: ["query:locowbc"]
score: 9.0
evidence: 使用对抗运动先验的人形机器人统一行走、奔跑和恢复
tldr: 为了使人类机器人能统一完成行走、跑步和跌倒恢复，传统方法常需显式模式切换或分离策略，限制实用性。本文提出基于状态依赖对抗运动先验的统一框架，通过投影重力单一阈值定义门控，将训练过渡路由至专用恢复判别器或速度条件运动判别器，后者联合覆盖行走与跑步。仅需三个LAFAN1参考片段即可正则化完整行为。在Unitree G1人形机器人上，单个冻结ONNX策略以50Hz运行，无需模式逻辑，成功从俯卧和仰卧跌倒恢复并实现行走至跑步平滑过渡。该工作显著简化多行为策略学习与部署，为复杂人形机器人控制提供高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.18611v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 899, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.18611v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1835, \"height\": 803, \"label\": \"Figure\"}]"
motivation: 现存人形机器人控制方法需显式模式切换或分离策略，难以实现单策略同时处理行走、跑步与跌倒恢复，且对跌倒鲁棒性不足。
method: 扩展AMP，以投影重力阈值为状态依赖门，训练样本路由至恢复判别器或速度条件运动判别器，仅用三个参考轨迹正则化。
result: 在Unitree G1硬件上，单策略以50Hz运行，成功从俯卧和仰卧跌倒恢复，并实现行走至跑步平滑过渡。
conclusion: 状态依赖门控AMP框架统一了人形机器人多种行为，简化训练部署，硬件实验证明其鲁棒性和实用性。
---

## 摘要
我们提出了一种统一的强化学习框架，能够使单一策略在Unitree G1类人机器人上执行行走、奔跑和跌倒恢复，并在实际硬件上进行了验证，部署时无需任何显式的模式切换指令。该框架扩展了对抗运动先验（AMP），用状态依赖的门控替代了传统的全局参考分布，该门控将每个训练转换路由至两个判别器之一：一个专用的恢复判别器和一个速度条件的运动判别器，后者联合覆盖行走和奔跑。该门控由投影重力上的单一固定阈值定义：当身体倾斜超过垂直方向约37°（|g_z+1|>0.6）时，激活恢复判别器；否则使用运动判别器，其中归一化指令速度作为条件，在行走和奔跑片段之间选择合适的参考轨迹。仅需三个LAFAN1参考片段即可规范完整的行为了集。在部署时，单一的冻结ONNX策略以50 Hz运行，无需运行时模式逻辑；硬件实验表明，在同一控制器下，该策略成功地从俯卧和仰卧跌倒中恢复，并实现了平滑的行走到奔跑的过渡。

## Abstract
We propose a unified reinforcement learning framework that enables a single policy to perform walking, running, and fall recovery on the Unitree G1 humanoid robot, validated on physical hardware without any explicit mode-switching command at deployment. The framework extends Adversarial Motion Priors (AMP) by replacing the conventional global reference distribution with a state-dependent gate that routes each training transition to one of two discriminators: a dedicated recovery discriminator and a velocity-conditioned locomotion discriminator that jointly covers walking and running. The gate is defined by a single fixed threshold on projected gravity: the recovery discriminator is activated when body tilt exceeds approximately $37^\circ$ from vertical ($|g_z+1|>0.6$); otherwise the locomotion discriminator is used, with the normalized commanded velocity serving as a condition that selects the appropriate reference trajectory between walk and run clips. Only three LAFAN1 reference clips are required to regularize the complete behavior set. At deployment, a single frozen ONNX policy executes at 50\,Hz with no runtime mode logic; hardware experiments demonstrate successful recovery from both prone and supine falls and smooth walk-to-run transitions under the same controller.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：传统人形机器人控制通常采用模块化架构，将行走、奔跑和跌倒恢复实现为多个独立控制器并通过手工状态机切换。这种方法存在过渡逻辑需手动调参、模式边界处行为脆弱、模式间切换时运动质量下降等局限性。近期强化学习（RL）方法已能端到端学习跌倒恢复，而运动先验方法（如Adversarial Motion Priors, AMP）可改善运动风格的真实性，但AMP天然适合单一行为模式，难以覆盖行走、奔跑和恢复这些运动学特征截然不同的行为。
- **核心问题**：如何设计一个单一强化学习策略，无需运行时模式逻辑，即可在Unitree G1人形机器人上同时执行行走、奔跑和跌倒恢复，且行为平滑过渡。
- **整体含义**：通过状态依赖的门控机制，将AMP框架扩展为两个判别器（恢复判别器与速度条件运动判别器），仅用三个参考运动片段即可规范完整行为集，在真实硬件上实现了统一控制，验证了方法的有效性与实用性。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：保持单一共享策略网络，但根据当前机器人状态和指令切换对抗运动先验。训练时使用状态依赖的门控函数，将每个训练过渡路由至两个并行的判别器之一，从而提供模式一致的AMP奖励信号。部署时只保留策略网络，无需任何运行时模式逻辑。
- **关键技术细节**：
  - **策略网络**：三层MLP，输入96维观测（含体角速度、投影重力、指令速度、关节位置/速度、上一动作等），并堆叠4帧历史，共384维。输出29维关节目标位置，由低层PD控制器跟踪。
  - **两个判别器**：
    - 恢复判别器 \(D_{rec}^\phi\)：仅使用跌倒恢复参考片段训练。
    - 速度条件运动判别器 \(D_{loco}^\phi\)：接受归一化指令速度 \(\hat{v}_t \in [0,1]\) 作为额外输入，联合使用行走和奔跑参考片段训练。判别器更新时，以概率 \((1-\hat{v}_t)\) 采样行走片段，以概率 \(\hat{v}_t\) 采样奔跑片段。
  - **门控函数**：固定阈值，基于投影重力 \(g_z\)：
    \[
    z_t = 
    \begin{cases}
    \text{rec} & \text{if } |g_z+1| > 0.6 \ (\text{倾斜}>37^\circ)\\
    \text{loco} & \text{otherwise}
    \end{cases}
    \]
    此阈值选择基于重力信号的双峰性，低占用区域使得门控鲁棒且无需学习。
  - **AMP奖励**：根据门控结果，使用对应判别器的负对数概率作为风格奖励 \(R_{AMP}\)。总奖励 \(R_{total} = R_t + \lambda_{amp} R_{AMP}\)，其中 \(\lambda_{amp}=0.5\)。
  - **参考运动**：仅使用三个LAFAN1片段，经逆运动学重定向到G1关节空间：walk1_subject1、run1_subject2、fallAndGetUp2_subject2。
  - **训练**：在Isaac Lab仿真中使用PPO训练。网络输出为ONNX模型，部署时以50Hz运行。

### 3. 实验设计：使用了哪些数据集/场景，benchmark，对比方法

- **数据集/参考运动**：LAFAN1数据集中的三个片段（行走、奔跑、跌倒恢复），经逆运动学重定向到Unitree G1。
- **实验场景**：在真实Unitree G1硬件上部署，测试包括：
  - 行走：速度指令范围 [-0.5, 1.0] m/s（正常模式）和 [-1.5, 3.0] m/s（快速模式，需显式命令）。
  - 奔跑：通过速度条件判别器自然过渡，无离散模式切换。
  - 跌倒恢复：从俯卧（prone）和仰卧（supine）两种姿态恢复。
  - 统一行为连续性：完整演示 recover→walk→run 序列（硬件视频截图见图2）。
- **Benchmark与对比方法**：论文未设置定量基准或对比方法（如模块化FSM或全局AMP基线）。仅定性描述了传统模块化方法的局限性，但未进行直接比较实验。

### 4. 资源与算力

- 论文中未明确说明训练所使用的GPU型号、数量或训练时长。仅提及在Isaac Lab仿真中使用PPO训练，并采用标准仿真参数随机化，无额外域随机化微调。因此无法提供具体算力消耗信息。

### 5. 实验数量与充分性

- **实验数量**：仅有一组真实硬件实验（包括两个恢复姿态+行走奔跑过渡），未进行仿真消融或对比实验。
- **充分性与客观性**：
  - 硬件实验成功演示了统一策略的行为，证明了概念可行性。
  - 然而，缺乏量化指标（如恢复成功率、步态周期参数、速度跟踪误差）和对比基线（如全局AMP或状态机方法），实验覆盖不够广泛。
  - 未测试在干扰、不同地面条件、更宽速度范围或连续上升/下降指令下的表现。
  - 因此，实验在证明方法有效性上是有限的，但作为技术验证是可以接受的。

### 6. 论文的主要结论与发现

- 状态依赖的门控AMP框架能够使单一策略同时覆盖行走、奔跑和跌倒恢复，无需运行时模式切换。
- 仅需三个LAFAN1参考片段即可规范化完整行为了集，这表明结构化的先验分离比参考多样性更重要。
- 投影重力上的单一固定阈值作为门控简单有效，可在训练时正确路由样本。
- 部署在真实Unitree G1机器人上，策略以50Hz运行，成功从俯卧和仰卧跌倒恢复，并实现平滑行走到奔跑过渡。

### 7. 优点：方法或实验设计上的亮点

- **方法简洁高效**：仅用一个固定阈值门控和两个判别器，无需复杂学习，即可实现多模式统一控制。
- **参考数据极度节约**：仅三个运动片段，显著降低了对大规模运动捕捉数据的需求。
- **部署友好**：单一ONNX模型，无运行时模式逻辑，推理简单稳定。
- **硬件验证**：在真实机器人上成功运行，涵盖了恢复-行走-奔跑全流程，证明了从仿真到现实的泛化能力。
- **泛化性潜力**：方法可推广至其他需要行为切换的场景（如不同地形）。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制

- **实验覆盖不足**：
  - 缺乏与现有方法（如模块化FSM、全局AMP）的定量对比。
  - 未评估恢复成功率、恢复时间、速度跟踪精度等量化指标。
  - 仅测试了平坦地面，未考虑斜坡、楼梯、障碍物等复杂环境。
  - 未进行消融实验（如移除速度条件判别器、改变阈值或仅用一个判别器），无法证明各组件贡献。
  - 快速模式需显式命令，并非完全无缝过渡。
- **偏差风险**：参考运动来自LAFAN1，动作风格受限于该数据集，可能不适用于其他机器人或更动态的行为。
- **应用限制**：
  - 门控阈值基于固定假设（重力分布双峰），若机器人在其他姿态（如被悬挂）可能失效。
  - 策略在50Hz运行，更高动态行为可能需要更高控制频率。
  - 未讨论对机器人损坏的鲁棒性（如电机过载、关节限位）。
- **可重复性**：未公开代码或详细超参数，不利于他人复现。

（完）
