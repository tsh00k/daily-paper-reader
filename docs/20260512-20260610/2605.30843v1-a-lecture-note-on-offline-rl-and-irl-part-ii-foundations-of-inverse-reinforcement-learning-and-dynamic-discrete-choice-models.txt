Title: A Lecture Note on Offline RL and IRL, Part II: Foundations of Inverse Reinforcement Learning and Dynamic Discrete Choice Models

URL Source: https://arxiv.org/pdf/2605.30843v1

Published Time: Mon, 01 Jun 2026 00:37:06 GMT

Number of Pages: 58

Markdown Content:
# A Lecture Note on Offline RL and IRL 

Part II: Foundations of Inverse Reinforcement Learning and Dynamic Discrete Choice Models Enoch Hyunwook Kang University of Washington, Foster School of Business June 1, 2026 

Abstract 

In the forward reinforcement-learning problem, the reward is fixed and known; the learner is asked to find a good policy or value function. Here we turn the question around. Given offline data generated by an expert, can we recover the reward the expert was optimizing? This is the inverse reinforcement learning problem, and remarkably, two communities, structural econometricians studying dynamic discrete choice (DDC) and machine learners studying entropy-regularized IRL , have been working on exactly the same probabilistic model under different names. We begin by proving their equivalence. We then develop the classical identification result of Magnac and Thesmar (2002) and the classical computational paradigms that grew out of it: Rust’s nested fixed-point algorithm (Rust, 1987), the conditional-choice-probability approach of Hotz and Miller (1993), and the two temporal-difference approaches of Adusumilli and Eckardt (2019): linear semi-gradient TD and approximate value iteration. Each route has its limits: dimensionality, transition-kernel estimation, the deadly triad, or projected fixed-point bias. We then walk through the modern ML/IRL strand: adversarial IRL (Fu et al., 2017), occupancy matching (Ho and Ermon, 2016), IQ-Learn (Garg et al., 2021), and offline ML-IRL (Zeng et al., 2023), deriving each method’s actual objective and stating precisely what it does and does not identify. We close with the empirical-risk-minimization framework of Kang, Yoganarasimhan, et al. (2025), which yields a gradient-based estimator for offline IRL/DDC. 

1

> arXiv:2605.30843v1 [cs.LG] 29 May 2026

# Contents 

1 Why IRL and DDC? 42 Preliminaries 4

2.1 Markov Decision Processes . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 52.2 Function Spaces, Norms, Projections . . . . . . . . . . . . . . . . . . . . . . . . . . . 52.3 The Dynamic Discrete Choice Formulation . . . . . . . . . . . . . . . . . . . . . . . . 52.4 The Entropy-Regularized IRL Formulation . . . . . . . . . . . . . . . . . . . . . . . . 62.5 The Soft Bellman Operator . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 72.6 DDC–MaxEnt-IRL Equivalence . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 82.7 The DDC–IRL Equivalence Theorem . . . . . . . . . . . . . . . . . . . . . . . . . . . 11 2.8 The Sample Soft Bellman Operator and Double Sampling . . . . . . . . . . . . . . . 12 

3 Identification: What Can Be Recovered from Behavior Alone? 13 

3.1 The Identification Failure . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 13 3.2 The Anchor-Action Assumption (Magnac–Thesmar) . . . . . . . . . . . . . . . . . . 14 3.3 The Adversarial-IRL Route and Its Limits . . . . . . . . . . . . . . . . . . . . . . . . 16 

4 Classical DDC Methods 25 

4.1 The Two Pieces: NLL and Bellman . . . . . . . . . . . . . . . . . . . . . . . . . . . . 25 4.2 Rust’s Nested Fixed-Point Algorithm (NFXP) . . . . . . . . . . . . . . . . . . . . . . 26 4.3 Hotz–Miller / Conditional Choice Probabilities (CCP) . . . . . . . . . . . . . . . . . 27 4.4 Temporal-Difference DDC: Linear Semi-Gradient and Approximate Value Iteration . 28 4.4.1 Linear Function Approximation and the Projected Bellman Equation . . . . 28 4.4.2 The Deadly Triad: Off-Policy Divergence of Linear TD . . . . . . . . . . . . . 29 4.4.3 Approximate Value Iteration as Fitted Value Iteration . . . . . . . . . . . . . 30 4.4.4 Application: The Two TD-DDC Methods of Adusumilli–Eckardt . . . . . . . 31 4.4.5 Why “Neural Orthogonalization” (Target Networks) Does Not Resolve It . . 32 25 Modern IRL Methods 34 

5.1 Adversarial IRL (AIRL) . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 34 5.2 Occupancy Matching: GAIL . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 36 5.3 IQ-Learn . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 38 5.4 Offline Maximum-Likelihood IRL (Zeng et al.) . . . . . . . . . . . . . . . . . . . . . 39 

6 The Empirical Risk Minimization Approach 40 

6.1 From classical DDC equations to two losses . . . . . . . . . . . . . . . . . . . . . . . 41 6.2 Expected risk: the one-shot population problem . . . . . . . . . . . . . . . . . . . . . 42 6.3 Why squared TD is not the empirical Bellman error . . . . . . . . . . . . . . . . . . 43 6.4 Bias correction and the minimax ERM formulation . . . . . . . . . . . . . . . . . . . 44 6.5 GLADIUS: alternating gradients for the corrected ERM . . . . . . . . . . . . . . . . 45 6.6 PL geometry and convergence . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . . 47 

A Deferred Proofs 50 

A.1 Local AIRL Gradient and Policy-Objective Identities . . . . . . . . . . . . . . . . . . 50 31 Why IRL and DDC? 

In the forward reinforcement-learning problem, the reward function is a fixed, known input. The world handed us r and asked us to learn Q⋆. In a great many real-world problems, however, r is exactly what we want to learn, and the closest thing we have to it is a log of decisions made by someone who knew what they were doing. 

• A labor economist watches workers choose between job offers and wants to recover the utility function that rationalizes their choices (McFadden, 2001). 

• A marketing analyst sees customers’ coupon redemptions and purchase histories, and wants to recover preferences in order to predict response to a new offer. 

• A health-economics researcher observes physicians’ treatment decisions and wants to quantify the implicit risk-benefit tradeoffs they are making. 

• A robotics or autonomous-driving engineer watches expert pilots fly a helicopter or expert drivers navigate traffic, and wants to extract a reward function that, when fed to a downstream RL algorithm, reproduces the expert’s behavior (Barnes et al., 2023). In each case the data is offline: a fixed log of state-action-(next-state) tuples from one or more experts, and the reward itself is never directly observed (Levine et al., 2020). Two literatures have been chipping away at this problem for decades, under almost identical probabilistic structure but completely different vocabularies: 

• Dynamic discrete choice (DDC) models from structural econometrics (Rust, 1987; Rust, 1994; Arcidiacono and Ellickson, 2011): the agent’s reward is observed plus a private idiosyncratic shock, generally Gumbel-distributed, which the econometrician integrates out. 

• Inverse reinforcement learning (IRL) with the entropy-regularized (maximum-entropy) variant of Finn et al. (2016) and Fu et al. (2017): the agent maximizes expected return plus an entropy bonus on its policy, producing the same softmax choice rule. Our first order of business (Section 2) is to prove these are the same model. Sections 3–5 then survey the methods each tradition developed, and Section 6 presents the unified ERM treatment of Kang, Yoganarasimhan, et al. (2025) that fixes the issues both traditions inherited. 

# 2 Preliminaries 

This section sets up the model, defines the soft Bellman operator, proves its contraction, and, most importantly, proves the equivalence between the DDC and entropy-regularized IRL formulations. 4Readers familiar with standard MDP notation can skim the MDP setup, but should not skip Section 2.6 (the Gumbel-max identity) or Section 2.7 (the equivalence theorem). 

2.1 Markov Decision Processes 

We consider an infinite-horizon discounted MDP M = ( S, A, P, ν 0, r, β ) where S is a (possibly continuous) state space, A is a finite action space, P : S × A → ∆( S) is the transition kernel, 

ν0 ∈ ∆( S) is the initial distribution, r : S × A → [−Rmax , R max ] is the (unknown) reward, and 

β ∈ (0 , 1) is the discount. A stationary Markov policy is a measurable map π : S → ∆( A) with 

π(a | s) the probability of a at s. We write Pν0,π and Eν0,π for the distribution and expectation over the resulting trajectory τ = ( s0, a 0, s 1, a 1, . . . ). The discounted state-action occupancy of π is 

dπ(s, a ) := (1 − β)

> ∞

X

> t=0

βt Pν0,π (st = s, a t = a),

a probability measure on S × A . The offline dataset is 

D = (si, a i, s ′

> i

) Ni=1 ,

sampled iid from dπ⋆

for a single expert policy π⋆. Note what is missing: we do not observe rewards in D: only states, actions, and next states. That omission is precisely what makes the problem “inverse,” and it is the canonical setup of both DDC and IRL. 

2.2 Function Spaces, Norms, Projections 

Let Q := {Q : S × A → R measurable, bounded }. The sup-norm is ∥Q∥∞ := sup s,a |Q(s, a )|.For ρ ∈ ∆( S × A ) we write ∥Q∥2 

> ρ

:= E(s,a )∼ρ[Q(s, a )2] and ⟨Q1, Q 2⟩ρ := Eρ[Q1Q2], so L2(ρ) is a Hilbert space. For a closed linear subspace F ⊆ L2(ρ), the L2(ρ)-projection is the unique element ΠF,ρ Q ∈ F satisfying ∥Q − ΠF,ρ Q∥ρ = inf f ∈F ∥Q − f ∥ρ. This restriction to closed linear subspaces matters: projections onto arbitrary closed nonconvex sets may fail to be unique. For closed linear subspaces, Π F,ρ is a non-expansion: ∥ΠQ1 − ΠQ2∥ρ ≤ ∥ Q1 − Q2∥ρ for all Q1, Q 2. We will need this fact, both the projection itself and its non-expansion property, when analyzing temporal-difference methods in Section 4.4. 

2.3 The Dynamic Discrete Choice Formulation 

The DDC setup follows the Type 1 Extreme Value (T1EV) derivation in Kang, Yoganarasimhan, et al. (2025). At each period the agent observes the deterministic flow payoff r(st, a t) plus an 5idiosyncratic alternative-specific shock ϵt,a t . We use the unit-scale Gumbel distribution with location parameter δ:

Fδ(x) = exp  − exp( −(x − δ)) , (1) 

fδ(x) = exp  −(( x − δ) + exp( −(x − δ))) , (2) 

E[ϵ] = δ + γE =: μϵ, (3) where γE is the Euler–Mascheroni constant. The mean-zero T1EV normalization used for the baseline DDC–MaxEnt equivalence is 

δ = −γE , μϵ = 0 .

The DDC agent maximizes 

π⋆ ∈ arg max 

> π

Eν0,π,G 

" ∞X

> t=0

βt r(st, a t) + ϵt,a t

#

, (4) where the expectation averages over the Markov chain and the idiosyncratic shocks. The policy is induced by choosing the alternative with the largest shock-augmented choice-specific value. The derivation in Section 2.6 shows that, under the mean-zero normalization δ = −γE , this DDC problem implies 

π⋆(a | s) = exp( Q⋆(s, a )) 

P 

> a′∈A

exp( Q⋆(s, a ′)) , (5) 

Q⋆(s, a ) = r(s, a ) + β Es′∼P (·| s,a )

"

log X

> a′∈A

exp( Q⋆(s′, a ′)) 

#

. (6) If δ̸ = −γE , the same derivation gives the same softmax rule but adds the constant β(δ + γE ) to the right side of the Q Bellman equation. 

2.4 The Entropy-Regularized IRL Formulation 

The MaxEnt-IRL setup in Kang, Yoganarasimhan, et al. (2025) instead treats the agent as choosing a stochastic stationary policy and explicitly rewards entropy. With regularization coefficient λ, the forward problem is 

π⋆ ∈ arg max 

> π

Eν0,π 

" ∞X

> t=0

βt r(st, a t) + λ H( π(· | st)) #

, (7) 6where H (π(· | s)) := − P 

> a

π(a | s) log π(a | s). Throughout the main text, and in the equivalence theorem below, we set 

λ = 1 ,

which is the entropy scale corresponding to unit-scale T1EV shocks. Section 2.6 derives the same softmax rule (5) and soft Bellman equation (6) from this entropy-regularized problem. 

2.5 The Soft Bellman Operator 

Definition 2.1 (Soft Bellman operator) . For Q ∈ Q , define 

(Tsoft Q)( s, a ) := r(s, a ) + β Es′∼P (·| s,a )

h

log X

> a′∈A

exp( Q(s′, a ′)) 

i

.

We write LSE (Q(s′, ·)) := log P 

> a′∈A

exp (Q(s′, a ′)) for the unit-temperature log-sum-exp. If the DDC shock mean is μϵ̸ = 0 , the corresponding uncentered DDC operator is 

(TDDC ,μ ϵ Q)( s, a ) := ( Tsoft Q)( s, a ) + βμ ϵ.

The log-sum-exp is a smooth surrogate for max , sandwiched between it and a log |A| slack: max a′ Q(s′, a ′) ≤ LSE (Q(s′, ·)) ≤ max a′ Q(s′, a ′) + log |A| . The two facts below are what we need to treat Tsoft as well-behaved as the ordinary hard Bellman operator. 

Lemma 2.2 (Log-sum-exp is 1-Lipschitz in sup-norm) . For bounded f, g : A → R,

LSE( f ) − LSE( g) ≤ max  

> a∈A

|f (a) − g(a)|.

Proof. Let ∆ := max a |f (a) − g(a)|, so g(a) − ∆ ≤ f (a) ≤ g(a) + ∆ for every a. Exponentiating preserves the inequalities and gives 

e−∆ X

> a

eg(a) ≤ X

> a

ef (a) ≤ e∆ X

> a

eg(a).

Taking logs, LSE( g) − ∆ ≤ LSE( f ) ≤ LSE( g) + ∆, hence | LSE( f ) − LSE( g)| ≤ ∆. 

Lemma 2.3 (Shift identity for LSE) . For bounded f : A → R and any c ∈ R,

LSE( f + c) = LSE( f ) + c, where (f + c)( a) := f (a) + c. 

Proof. LSE( f + c) = log P 

> a

ef (a)+ c = log  ec P 

> a

ef (a) = c + LSE( f ). 

Lemma 2.4 (Sup-norm contraction of Tsoft ). For any Q1, Q 2 ∈ Q ,

∥T soft Q1 − T soft Q2∥∞ ≤ β ∥Q1 − Q2∥∞.

7Proof. The reward terms cancel. For any fixed ( s, a ), 

|T soft Q1(s, a ) − T soft Q2(s, a )| = β Es′∼P (·| s,a )[LSE( Q1(s′, ·)) − LSE( Q2(s′, ·))] 

≤ β Es′

 LSE( Q1(s′, ·)) − LSE( Q2(s′, ·))  (Jensen) 

≤ β Es′

max  

> a′

|Q1(s′, a ′) − Q2(s′, a ′)| (Lemma 2.2) 

≤ β ∥Q1 − Q2∥∞.

Taking sup over ( s, a ) closes the bound. By Banach’s fixed-point theorem on ( Q, ∥ · ∥ ∞), Tsoft has a unique fixed point Q⋆ and soft value iteration Qk+1 := Tsoft Qk converges geometrically: ∥Qk − Q⋆∥∞ ≤ βk∥Q0 − Q⋆∥∞. So far this is identical to the hard-Bellman story; the action is in what comes next. 

2.6 DDC–MaxEnt-IRL Equivalence 

Lemma 2.5 (T1EV maximum, choice probability, and selected-value identities) . Let G1, . . . , G K

be independent with Gi ∼ Gumbel( νi, 1) and define S := P 

> i

eνi . Then 

max  

> i

Gi ∼ Gumbel(log S, 1) , (8) 

Emax  

> i

Gi

 = γE + log S, (9) 

P{k = arg max 

> i

Gi} = eνk

S , (10) 

EGk | k = arg max 

> i

Gi

 = γE + νk − log eνk

S . (11) 

Proof. For the maximum, independence gives 

P{max  

> i

Gi ≤ x} = Y

> i

exp {− exp( −(x − νi)) }

= exp 

(

−e−x X

> i

eνi

)

= exp {− exp( −(x − log S)) },

so max i Gi ∼ Gumbel (log S, 1) and its expectation is γE + log S. The choice probability follows from the standard T1EV integral: 

P{Gk > max  

> i̸=k

Gi} =

Z ∞−∞ 

Y

> i̸=k

Fi(x)fk(x) dx = eνk

P 

> i

eνi .

For the selected-value identity, let M−k := max i̸=k Gi. The first part shows M−k ∼ Gumbel (log P 

> i̸=k

eνi , 1). 8Applying the two-variable T1EV conditional-expectation calculation to Gk and M−k yields 

E[Gk | Gk ≥ M−k] = γE + νk + log 



1 + 

P 

> i̸=k

eνi

eνk



= γE + νk − log eνk

S ,

which is exactly (11). 

Lemma 2.6 (Entropy-regularized discrete choice identity) . For x = ( xa)a∈A and λ = 1 ,

max 

> q∈∆( A)

(X

> a

qaxa + H( q)

)

= log X

> a

exa , (12) arg max 

> q∈∆( A)

(X

> a

qaxa + H( q)

)

> a

= exa

P 

> b

exb . (13) 

Proof. The objective is strictly concave on the simplex. The Lagrangian first-order condition is 

xa − log qa − 1 − η = 0 ,

so qa ∝ exa and normalization gives (13). Substitution gives (12). 

MaxEnt-IRL derivation. Define the value and choice-specific value functions by 

V ⋆(s) := Eπ⋆

" ∞X

> h=0

βh r(sh, a h) + H( π⋆(· | sh))  s0 = s

#

,Q⋆(s, a ) := r(s, a ) + β Es′∼P (·| s,a )[V ⋆(s′)] .

At a fixed state s, the Bellman recursion is the finite-dimensional entropy-regularized choice problem 

V ⋆(s) = max 

> q∈∆( A)

(X

> a∈A

qa

 r(s, a ) + βE[V ⋆(s′) | s, a ] + H( q)

)

= max 

> q∈∆( A)

(X

> a∈A

qaQ⋆(s, a ) + H( q)

)

. (14) By Lemma 2.6, 

V ⋆(s) = log X

> a∈A

exp( Q⋆(s, a )) , (15) 

π⋆(a | s) = exp( Q⋆(s, a )) 

P 

> a′∈A

exp( Q⋆(s, a ′)) . (16) 9Substituting (15) into the definition of Q⋆ gives the choice-specific Bellman equation 

Q⋆(s, a ) = r(s, a ) + β Es′∼P (·| s,a )

"

log X

> a′∈A

exp( Q⋆(s′, a ′)) 

#

. (17) The paper also records the equivalent continuation representation. Since (16) implies 

Q⋆(s′, a ′) − log π⋆(a′ | s′) = log X

> b∈A

exp( Q⋆(s′, b )) for every a′,

Equation (17) can be written as 

Q⋆(s, a ) = r(s, a ) + β Es′∼P (·| s,a ), a ′∼π⋆(·| s′)

Q⋆(s′, a ′) − log π⋆(a′ | s′) . (18) This is the analogue of Equation (A5) in the paper. 

DDC derivation. Let ϵt,a i.i.d.

∼ Gumbel (δ, 1) across actions and dates. The shock-realized value and its shock average are 

V ⋆(s, ϵ ) := max 

> a∈A

r(s, a ) + ϵa + β E[ ¯V ⋆(s′) | s, a ] ,

¯V ⋆(s) := Eϵ[V ⋆(s, ϵ )] ,

and the DDC choice-specific value is 

Q⋆(s, a ) := r(s, a ) + β Es′∼P (·| s,a )[ ¯V ⋆(s′)] . (19) Thus 

V ⋆(s, ϵ ) = max  

> a∈A

{Q⋆(s, a ) + ϵa}. (20) For each action a, Q⋆(s, a ) + ϵa is Gumbel( Q⋆(s, a ) + δ, 1). Lemma 2.5 gives ¯V ⋆(s) = δ + γE + log X

> a∈A

exp( Q⋆(s, a )) , (21) 

π⋆(a | s) = P



a = arg max 

> b∈A

(Q⋆(s, b ) + ϵb)



= exp( Q⋆(s, a )) 

P 

> a′∈A

exp( Q⋆(s, a ′)) . (22) Substituting (21) into (19) yields 

Q⋆(s, a ) = r(s, a ) + β Es′∼P (·| s,a )

"

log X

> a′∈A

exp( Q⋆(s′, a ′)) + δ + γE

#

. (23) Under the mean-zero T1EV normalization δ = −γE , this becomes exactly (6) . This is Equation (3) in the paper. 10 The same DDC Bellman equation can also be written in the policy-continuation form used in the paper. Conditional on the selected next action a′, Lemma 2.5 gives 

E



Q⋆(s′, a ′) + ϵa′ a′ = arg max 

> b

(Q⋆(s′, b ) + ϵb)



= Q⋆(s′, a ′) + δ + γE − log π⋆(a′ | s′).

Therefore 

Q⋆(s, a ) = r(s, a ) + β Es′∼P (·| s,a ), a ′∼π⋆(·| s′)

Q⋆(s′, a ′) + δ + γE − log π⋆(a′ | s′) . (24) When δ = −γE , Equation (24) is identical to the MaxEnt-IRL continuation representation (18). 

2.7 The DDC–IRL Equivalence Theorem 

Theorem 2.7 (Equivalence of mean-zero unit-scale DDC and unit-entropy MaxEnt-IRL) . Fix S,

A, P , ν0, r, and β. Let the DDC shocks be iid Gumbel (δ, 1) with unit scale, and set δ = −γE . Let the MaxEnt-IRL regularization coefficient be λ = 1 . Then DDC and MaxEnt-IRL are characterized by the same three equations: 

Q⋆(s, a ) = r(s, a ) + β Es′∼P (·| s,a )

"

log X

> a′∈A

exp( Q⋆(s′, a ′)) 

#

,π⋆(a | s) = exp( Q⋆(s, a )) 

P 

> a′∈A

exp( Q⋆(s, a ′)) ,Q⋆(s, a ) = r(s, a ) + β Es′∼P (·| s,a ), a ′∼π⋆(·| s′)

Q⋆(s′, a ′) − log π⋆(a′ | s′) .

Consequently, the same reward function r and discount factor β produce the same unique choice-specific value function Q⋆, the same optimal policy π⋆, and hence the same joint distribution over observed (st, a t, s t+1 ) trajectories under any common ν0.Proof. The MaxEnt-IRL derivation gives the three displayed equations through Lemma 2.6. The DDC derivation gives the same equations through Lemma 2.5 once δ = −γE . The first displayed equation is the fixed-point equation of the soft Bellman operator Tsoft , which has a unique bounded fixed point by Lemma 2.4. Thus both models yield the same Q⋆. Applying the common softmax equation gives the same π⋆, and the observed trajectory law depends only on ( ν0, P, π ⋆). 

Remark 2.8 (Nonzero T1EV means) . If δ̸ = −γE , the DDC equation is (23) . Relative to the mean-zero equation, its solution is shifted by the constant β(δ + γE )/(1 − β), so the induced softmax policy is unchanged. The clean literal equality of the DDC and unit-entropy MaxEnt-IRL Bellman equations is obtained under the paper’s normalization δ = −γE .

Remark 2.9 (Two views, one model) . The two formulations differ in how they explain stochastic action choices. DDC attributes them to unobserved T1EV utility shocks; MaxEnt-IRL attributes 

11 them to entropy-regularized stochastic choice. Under λ = 1 and δ = −γE , the choice-specific Bellman equation and the induced policy are identical, so the two formulations are statistically indistinguishable from offline observations of (s, a, s ′).

2.8 The Sample Soft Bellman Operator and Double Sampling 

Given D = {(si, a i, s ′

> i

)}Ni=1 , the sample soft Bellman operator is ˆTsoft Q(s, a, s ′) := r(s, a ) + β LSE( Q(s′, ·)) .

For each fixed ( s, a ), Es′∼P (·| s,a )[ ˆTsoft Q(s, a, s ′)] = Tsoft Q(s, a ): one sample is unbiased for the operator’s value. Squaring breaks this. Define 

LBE (Q)( s, a ) :=  Q(s, a ) − T soft Q(s, a )2, (25) 

LTD (Q)( s, a, s ′) :=  Q(s, a ) − ˆTsoft Q(s, a, s ′)2, (26) and write VQ(s′) := LSE( Q(s′, ·)). 

Lemma 2.10 (Variance bias of the squared TD loss) . For any Q ∈ Q and any (s, a ),

Es′∼P (·| s,a )

LTD (Q)( s, a, s ′) = LBE (Q)( s, a ) + β2 Var s′∼P (·| s,a )

 VQ(s′). (27) 

Proof. Fix ( s, a ) and let X := ˆTsoft Q(s, a, s ′) − Q(s, a ), regarded as a random variable in s′. The variance decomposition E[X2] = ( EX)2 + Var( X) gives 

Es′ [X2] =  Tsoft Q(s, a ) − Q(s, a )2 + Var s′

  ˆTsoft Q(s, a, s ′).

Since r(s, a ) is constant in s′, Var s′ ( ˆTsoft Q) = Var s′ (r(s, a ) + βV Q(s′)) = β2 Var s′ (VQ(s′)). The discrepancy β2 Var s′∼P (·| s,a )(VQ(s′)) is the double-sampling bias . It is a Q-dependent population quantity that does not vanish with more data, so its gradient is nonzero at Q⋆. Minimizing the empirical squared TD residual is not the same as minimizing the population Bellman error. Two independent next-state samples from the same ( s, a ) would suffice to break the bias, but offline RL never gives us two next-states at the same ( s, a ). Section 6.4 fixes this by deriving the bi-conjugate correction explicitly. With the equivalence and the sampling subtlety in hand, we proceed to identification (Section 3), the classical DDC methods (Section 4), the IRL methods (Section 5), and the unified ERM treatment (Section 6). 12 3 Identification: What Can Be Recovered from Behavior Alone? 

Before discussing how to estimate the reward, we have to confront a more basic question: is the reward identifiable from offline expert data at all? 

Without further structure, the answer is no. This is not a quirk of any particular estimator; it is a fact about the model. We make it precise, then describe the two normalizations, one from econometrics and one from machine learning, that resolve it, and assess what each delivers (Rust, 1994; Magnac and Thesmar, 2002; Cao et al., 2021). 

3.1 The Identification Failure 

Suppose two reward functions r and r′ produce, via the soft Bellman equation, the same optimal policy π⋆ and the same trajectory distribution under P . Then the offline expert data D has identical distribution under ( r, π ⋆) and ( r′, π ⋆), and no estimator based on D alone can possibly distinguish them. Worse, the indeterminacy is large: the soft-Bellman fixed point is invariant under state-only potential-based shaping in the sense of Ng et al. (1999), and we can write down explicitly an entire family of rewards that all produce the same expert behavior. This non-identification was established in the DDC framework by Magnac and Thesmar (2002) (their Proposition 1); we restate it in the entropy-regularized language for self-containedness. 

Lemma 3.1 (Non-identification from a single expert policy; Magnac and Thesmar, 2002) . Fix transition P and discount β. Let r be any reward function, with Q⋆ and π⋆ the soft optimal pair (Theorem 2.7). For any bounded measurable Φ : S → R, define the shaped reward 

˜r(s, a ) := r(s, a ) + Φ( s) − β Es′∼P (·| s,a )[Φ( s′)] . (28) 

Let ˜Q(s, a ) := Q⋆(s, a ) + Φ( s). Then ˜Q is the unique soft-Bellman fixed point of ˜r, and its induced softmax policy equals π⋆.Proof. Bellman fixed-point check. Apply T ˜r 

> soft

to ˜Q. By Lemma 2.3 applied with c = Φ( s′), LSE( ˜Q(s′, ·)) = LSE  Q⋆(s′, ·) + Φ( s′) = LSE( Q⋆(s′, ·)) + Φ( s′).

Therefore 

T ˜r 

> soft

˜Q(s, a ) = ˜ r(s, a ) + β Es′∼P (·| s,a )

LSE( ˜Q(s′, ·)) 

= r(s, a ) + Φ( s) − β Es′ [Φ( s′)] + β Es′

LSE( Q⋆(s′, ·)) + Φ( s′)

= r(s, a ) + Φ( s) + β Es′ [LSE( Q⋆(s′, ·))] = Q⋆(s, a ) + Φ( s) = ˜Q(s, a ),

13 where the second-to-last equality uses the soft Bellman equation for r. So ˜Q is a fixed point of T ˜r

> soft

;by Lemma 2.4 it is the unique such fixed point. 

Policy invariance. For each s, by Lemma 2.3, exp ˜Q(s, a )

P 

> a′

exp ˜Q(s, a ′) = eΦ( s) exp Q⋆(s, a )

eΦ( s) P 

> a′

exp Q⋆(s, a ′) = exp Q⋆(s, a )

P 

> a′

exp Q⋆(s, a ′) = π⋆(a | s).

Magnac and Thesmar (2002) proved that, in the DDC framework, this is essentially the only 

non-identification: any two per-period utilities producing the same choice probabilities under the same ( P, β ) differ by a state-only potential. Cao et al. (2021) gave the corresponding result explicitly in the entropy-regularized MDP language. To make the result fully concrete: 

Theorem 3.2 (Reward identifiability up to shaping; Magnac and Thesmar, 2002; Cao et al., 2021) .

Fix (P, β ) and let r1, r 2 be two bounded reward functions on S × A . Let π⋆ 

> 1

, π ⋆ 

> 2

be the corresponding entropy-regularized optimal policies. Then π⋆ 

> 1

= π⋆ 

> 2

if and only if there exists a bounded measurable 

Φ : S → R such that 

r1(s, a ) − r2(s, a ) = Φ( s) − β Es′∼P (·| s,a )[Φ( s′)] ∀(s, a ).

In particular, (i) state-action rewards are identified only modulo this shaping family, regardless of how rich the data is; and (ii) state-only rewards r(s) are identified up to additive constant if and only if the only Φ for which Φ( s) − β Es′∼P (·| s,a )[Φ( s′)] is a function of s alone is the constant function, a condition on the transition kernel P .

Lemma 3.1 establishes the “if” direction; the “only if” is the content of Magnac and Thesmar (2002) and Cao et al. (2021). The structural takeaway is the following: 

From expert behavior alone, the reward function is identifiable only up to a state-dependent potential. To identify it any more sharply, we need additional structure: either restrictions on the form of r, or normalizations that fix the potential. 

The DDC and modern-IRL literatures take very different roads at exactly this fork. We examine both. 

3.2 The Anchor-Action Assumption (Magnac–Thesmar) 

The classical DDC route is to declare the value of r at one designated action per state (Magnac and Thesmar, 2002). 14 Assumption 3.3 (Anchor action) . For every s ∈ S there exists a known, distinguished action 

as ∈ A with 

r(s, a s) = 0 .

In the Rust bus-engine application, the anchor action is “replace the engine”; its payoff (the cost of a new engine) is part of the model specification, not something to be estimated from data. The anchor action is allowed to depend on s.

Theorem 3.4 (Identification under the anchor assumption; Magnac and Thesmar, 2002) . Under Assumption 3.3, Q⋆(s, a ) and r(s, a ) are identified for all (s, a ) from the triple (π⋆, P, β ).Proof. We proceed in three steps. 

Step 1: Hotz–Miller inversion gives Q⋆ differences. From the softmax (5) , π⋆(a | s) = eQ⋆(s,a )/ P 

> a′

eQ⋆(s,a ′).Taking logs and differencing, log π⋆(a | s) − log π⋆(as | s) = Q⋆(s, a ) − Q⋆(s, a s) ∀a ∈ A , s ∈ S . (29) Hence Q⋆(s, a ) is identified for every a once Q⋆(s, a s) is. 

Step 2: A linear fixed-point equation for V ⋆ alone. Taking logs of the softmax at the anchor action and rearranging, 

Q⋆(s, a s) = LSE( Q⋆(s, ·)) + log π⋆(as | s) = V ⋆(s) + log π⋆(as | s), (30) where V ⋆(s) := LSE (Q⋆(s, ·)). The soft Bellman equation (6) at the anchor action, using r(s, a s) = 0, gives 

Q⋆(s, a s) = β Es′∼P (·| s,a s)[V ⋆(s′)] . (31) Combining (30) and (31), 

V ⋆(s) = β Es′∼P (·| s,a s)[V ⋆(s′)] − log π⋆(as | s). (32) This is a Bellman-like fixed-point equation for V ⋆, with − log π⋆(as | · ) playing the role of a reward and the anchor-conditioned kernel P (· | · , a s) as the transition. The operator Ψ V (s) := − log π⋆(as |

s) + β Es′∼P (·| s,a s)[V (s′)] is a β-contraction on the space of bounded functions S → R in sup-norm by the same Jensen argument as Lemma 2.4 (simpler, since no LSE is involved). The pseudo-reward 

− log π⋆(as | s) is itself bounded under the standing assumption inf s π⋆(as | s) > 0, which holds automatically when π⋆ is the softmax of a bounded Q⋆ on a finite action set (Remark 6.2 below). By the Banach fixed-point theorem, V ⋆ is the unique solution of (32) and is therefore identified from ( π⋆, P, β ) alone, with no reference to r.

Step 3: Recover Q⋆ and r. From Step 2 we have V ⋆. From (30) we obtain Q⋆(s, a s) = V ⋆(s) + log π⋆(as | s). From (29) we then obtain Q⋆(s, a ) for every a. Finally, from the soft Bellman 15 equation, 

r(s, a ) = Q⋆(s, a ) − β Es′∼P (·| s,a )[V ⋆(s′)] . (33) A small but important point: the anchor-action assumption is a normalization , not a restriction. It fixes the location of the reward without restricting which preferences over ( s, a ) pairs the model can represent. It is the standard identification device in structural econometrics for DDC (Rust, 1994; Magnac and Thesmar, 2002; Arcidiacono and Miller, 2011; Arcidiacono and Ellickson, 2011). 

3.3 The Adversarial-IRL Route and Its Limits 

The machine-learning tradition takes a different fork at the identification problem of Lemma 3.1. The DDC route (Section 3.2) fixes the shaping potential by fiat: declare one action’s reward known at every state. Adversarial IRL (Fu et al., 2017) instead tries to learn the split between reward and potential, the state-dependent term whose Bellman difference can change the numerical reward without changing behavior, by building the potential-based shaping invariance of Ng et al. (1999) directly into the structure of a discriminator. This subsection asks how much that buys. The guiding distinction is simple. From transitions ( s, a, s ′), expert behavior directly reveals which actions look better relative to other actions at the same state ; it does not directly reveal how much of that attractiveness comes from immediate reward rather than continuation value. AIRL is an attempt to learn this split: one part should be the transferable reward, and the other part should be the shaping/value correction. The question is whether the split is actually identified. 

The AIRL language and objective. 

Fu et al. (2017) build on the MaxEnt-IRL framework of Finn et al. (2016) and the generative-adversarial framework of Goodfellow et al. (2020) and Ho and Ermon (2016). AIRL introduces a binary classifier D, called the discriminator . Given a transition x = ( s, a, s ′), D(x) is trained to be large on expert transitions and small on transitions generated by the current policy π. Before writing D itself, AIRL specifies the reward-like score that will enter the classifier. That score is constrained to the potential-shaped form 

fg,h (s, a, s ′) := g(s) + β h (s′) − h(s), (34) where g is intended as the transferable reward and h as the shaping potential, which is equivalently the soft value term that should telescope away. In words, AIRL writes the expert-like score as reward part + future potential − current potential .

16 The hope is that the reward part g survives transfer, while the potential terms only correct for where the transition starts and ends. We write the state-only reward-head version here because it is the version under which AIRL’s disentanglement claim can be true; if g is allowed to depend on (s, a ), the same ambiguity returns immediately. In the terminology of Cao et al. (2021), “state-only” is an action-independent reward restriction. It is a real identifying restriction: recovering such a reward also needs decomposability conditions on how actions move between states, not only the adversarial objective. This potential-shaped quantity is only a score. To turn such a score into a classifier, the adversarial-IRL derivation starts from the standard GAN/Bayes-classifier density-ratio identity. For a binary classifier trained to classify expert samples against current-policy samples, the Bayes classifier with equal class priors has 

D⋆(x) = pE (x)

pE (x) + pπ(x) , log D⋆(x)1 − D⋆(x) = log pE (x) − log pπ(x). (35) Thus the classifier logit is a log density ratio. The next step is to specify the two trajectory densities whose ratio will be used. Write a finite trajectory as τ = ( s0, a 0, s 1, a 1, . . . , s H−1, a H−1, s H ). The density-ratio motivation used in GAN-guided cost learning and AIRL starts from an energy model over trajectories: keep the environment part of the trajectory probability, ν0(s0) Q 

> t

P (st+1 | st, a t), and tilt it toward trajectories whose accumulated learned score P 

> t

f (st, a t, s t+1 ) is large. Exponential tilting gives the expert-side density 

pf (τ ) ∝ ν0(s0)

> H−1

Y

> t=0

P (st+1 | st, a t) exp 

H−1X

> t=0

f (st, a t, s t+1 )



,

whereas the current policy generates trajectories according to 

pπ(τ ) = ν0(s0)

> H−1

Y

> t=0

π(at | st)P (st+1 | st, a t).

Taking the log-ratio cancels the common initial-state and transition factors: log pf (τ ) − log pπ(τ ) = 

> H−1

X

> t=0

f (st, a t, s t+1 ) − log π(at | st) + constant .

By the Bayes log-density-ratio identity (35), the corresponding trajectory classifier has log-odds log Dπf (τ )1 − Dπf (τ ) =

> H−1

X

> t=0

f (st, a t, s t+1 ) − log π(at | st) + Cf , (36) where Cf absorbs the trajectory-energy normalizer and any class-prior constant. This trajectory classifier is the GAN-GCL starting point. 17 From trajectory odds to the local AIRL discriminator. AIRL (Fu et al., 2017) now makes a one-step localization of the trajectory-level odds relation. Since the trajectory logit in (36) is a sum of per-transition terms, the natural local contribution of a transition ( s, a, s ′) is 

f (s, a, s ′) − log π(a | s).

AIRL therefore defines the transition-level discriminator by the odds equation log Dπf (s, a, s ′)1 − Dπf (s, a, s ′) = f (s, a, s ′) − log π(a | s). (37) Equivalently, 

Dπf (s, a, s ′) = exp f (s, a, s ′)exp f (s, a, s ′) + π(a | s) . (38) How can we justify this localization? The clean way is not to view it as literally marginalizing the trajectory Bayes classifier down to one transition. A true marginal classifier for a single transition would have to account for the full distribution of times and histories that can produce the same (s, a, s ′). The justification is more local: because the trajectory logit in (36) is additive, the term 

f (s, a, s ′) − log π(a | s) is the one-transition contribution suggested by that logit. The lemma below records why this is the right surrogate for AIRL: it gives the local discriminator gradient used in the adversarial update and the entropy-regularized reward used in the policy update. 

Lemma 3.5 (Trajectory-to-transition localization, informal) . Fix a policy π and a transition score 

f . The transition discriminator (37) is the one-step local surrogate associated with the trajectory log-odds (36) . Its log-odds reward is 

log Dπf (s, a, s ′) − log(1 − Dπf (s, a, s ′)) = f (s, a, s ′) − log π(a | s),

so using this log-odds as the policy reward makes the policy maximize the expected sum of f plus the Shannon entropy of π. At the population level, the corresponding discriminator gradient has the same form as the maximum-causal-entropy IRL gradient after the intractable trajectory marginal is replaced by a local policy-based energy measure. The precise finite-horizon statement and proof are deferred to Proposition A.1 in Appendix A.1. 

Thus the disappearance of the trajectory-level P 

> t

is the answer to a localization question, not a new cancellation in the likelihood ratio: AIRL keeps the one-transition contribution because it has the right discriminator-gradient and policy-objective identities, not because the full trajectory Bayes classifier literally collapses to one step. This is also the right place to keep one interpretive caveat in view. The trajectory-energy model above and its corresponding localization is not the same object as the dynamic soft-control model in Section 2.4. It is myopic in the specific sense emphasized by Cao et al. (2021): the trajectory likelihood is tilted by accumulated one-step scores f (st, a t, s t+1 ), whereas a dynamically optimal 18 policy evaluates an action through immediate reward plus the continuation value induced by the transition kernel. In the dynamic model, log π⋆(a | s) = Q⋆(s, a ) − V ⋆(s) = r(s, a ) + β E[V ⋆(s′) | s, a ] − V ⋆(s).

So the local AIRL score should first be read as a policy-rationalizing advantage-like object. It becomes a reward only after a separate decomposition argument explains which part is immediate payoff and which part is value shaping. Specializing the generic local discriminator (38) to the structured AIRL score f = fg,h from (34) gives 

Dπg,h (s, a, s ′) = exp fg,h (s, a, s ′)exp fg,h (s, a, s ′) + π(a | s) . (39) The π(a | s) term has an intuitive role: a generated transition may contain action a simply because the current policy already takes a often at state s. AIRL therefore asks whether the expert-like score exp fg,h (s, a, s ′) is large relative to the current policy’s own probability of producing that action. This is why the discriminator is policy-dependent, written Dπg,h .In the trained model g = gθ and h = hϕ are parametrized, with fθ,ϕ := fgθ ,h ϕ and Dπθ,ϕ := Dπgθ ,h ϕ .We write fg,h when reasoning about identification at the level of functions, and fθ,ϕ when reasoning about the optimization. Also, in the adversarial objectives below, the notation ( s, a, s ′) ∼ dπ means (s, a ) ∼ dπ followed by s′ ∼ P (· | s, a ); equivalently it is the transition-augmented discounted occupancy. The two training updates now follow from the two roles of this classifier. 

• First, fix the current policy π. The policy-generated transitions are the negative samples for the discriminator. If y = 1 denotes an expert transition and y = 0 a generated transition, then the discriminator log-likelihood is 

y log Dπθ,ϕ (s, a, s ′) + (1 − y) log  1 − Dπθ,ϕ (s, a, s ′).

Averaging this log-likelihood over expert samples from dπ⋆

and generated samples from dπ, and dropping the irrelevant class-prior factor, gives the discriminator objective: increase log Dπθ,ϕ on expert transitions and increase log(1 − Dπθ,ϕ ) on policy transitions. 

• Second, fix ( θ, ϕ ) and update the policy. AIRL gives the policy the same discriminator’s log-odds as its reward signal. Substituting g = gθ and h = hϕ into the log-odds identity (37) gives log Dπθ,ϕ (s, a, s ′) − log  1 − Dπθ,ϕ (s, a, s ′) = fθ,ϕ (s, a, s ′) − log π(a | s).

Thus averaging the log-odds over transitions from dπ gives an entropy-regularized policy objective: the policy optimizer sees the learned score fθ,ϕ together with the entropy term induced by 19 − log π(a | s), rather than a raw logistic score. Operationally, the discriminator says which transitions look expert-like relative to the current policy, and the policy update tries to make those high-log-odds transitions more common. Putting these two derived objectives together, AIRL alternates: Discriminator (max  

> θ,ϕ

) : E(s,a,s ′)∼dπ⋆ [log Dπθ,ϕ (s, a, s ′)] + E(s,a,s ′)∼dπ [log(1 − Dπθ,ϕ (s, a, s ′))] , (40) Policy (max  

> π

) : E(s,a,s ′)∼dπ

log Dπθ,ϕ − log(1 − Dπθ,ϕ ) = Edπ

fθ,ϕ − log π(a | s),

so the policy maximizes entropy-regularized return under the implicit reward fθ,ϕ . The modeling bet is that (34) splits the soft advantage cleanly into a reward part and a shaping part. The bet can pay off only if that split is unique ; the rest of this subsection is about when it is. 

Identification in AIRL 

We are finally ready to discuss the identification logic of AIRL. We discuss that: 

• When the adversarial game actually reaches its saddle, under deterministic transitions, the AIRL discriminator identifies the soft advantage A⋆(s, a ) = log π⋆(a | s). This is exactly the expert’s conditional choice probabilities (the same information Hotz–Miller inversion provides) and nothing more. 

• Converting that advantage into a reward requires a separate decomposition argument from Fu et al. (2017). We show that it works, but under four restrictions: a state-only reward, deterministic dynamics, a connectivity-and-coverage condition on the transition graph, and an attained exact saddle. 

• Each of the four is load-bearing. Drop any one and the shaping ambiguity of Lemma 3.1 returns. Stochastic transitions, in particular, reintroduce it through a transition-kernel completeness condition that AIRL cannot enforce. 

The saddle identifies the advantage, not the reward. The next statement is conditional and population-level. It assumes (a) the policy player has reached the expert soft-optimal policy, so dπ =

dπ⋆

on the support under discussion, and (b) the discriminator class can realize its Bayes optimum there. These assumptions say that expert and generated transitions are truly indistinguishable to the classifier, and that the classifier class is rich enough to express the corresponding optimum. If either fails, the conclusions below do not apply: the logit would describe the current policy rather than the expert, or the AIRL class would not contain the Bayes-optimal discriminator. Under (a)–(b), the best discriminator cannot tell the two sources apart, so D⋆ ≡ 12 on the matched support. Since the policy has matched the expert, the π appearing in (39) is now π⋆. Hence 20 Dπ⋆ 

> θ,ϕ

= 12 exactly when exp fθ,ϕ = π⋆(a | s), which implies 

fθ,ϕ (s, a, s ′) = log π⋆(a | s) = Q⋆(s, a ) − V ⋆(s) =: A⋆(s, a ), V ⋆(s) := LSE( Q⋆(s, ·)) . (41) So an attained, realizable saddle pins down the soft advantage A⋆. Two consequences are worth stating plainly. 

This is not new information. In population, A⋆(s, a ) = log π⋆(a | s) is just the expert’s log conditional choice probability. Hotz–Miller inversion (29) already extracts the same object through the log-probability differences log π⋆(a | s) − log π⋆(as | s). This leads to the following Remark 3.6. 

Remark 3.6. The preceding discussion also clarifies what AIRL gains by adding the anchor actions, e.g., an exit action or outside-option normalization. Under deterministic transitions (and only under deterministic transitions, as we will discuss), AIRL saddle supplies A⋆(s, a ) = log π⋆(a | s),which is exactly what Hotz-Miller achieves with MLE. Therefore, if an anchor action as with known payoff is then imposed, the recovery of V ⋆, Q⋆, and r(s, a ) follows from the same anchor-action logic in Magnac and Thesmar, 2002. 

This is the key interpretive point. The advantage says how attractive action a is relative to the baseline value of state s. It does not say whether that attractiveness came from high immediate reward, a favorable next-state distribution, or a potential-shaping term. Behavior may tell us that action a is preferred to action b at state s, but it does not by itself say which part is immediate payoff and which part is future continuation value. So the equality fθ,ϕ = A⋆ is not yet reward recovery; it is only the object that reward recovery must decompose. 

Exact equality may be unreachable in stochastic environments. The target A⋆(s, a ) does not depend on the realized next state s′, whereas the AIRL class g(s) + βh (s′) − h(s) generally does. So in a stochastic environment the pointwise equality (41) can fail within the parametrized class, before any reward question is even posed. We return to this in the discussion of restriction (iii) below; until then, transitions are deterministic. 

From advantage to reward: decomposable deterministic dynamics. Assume now s′ =

T (s, a ) deterministic. Reward recovery still does not follow for free. If the AIRL reward head is allowed to be a state-action function, then for any bounded h : S → R the choice 

gh(s, a ) := log π⋆(a | s) − β h (T (s, a )) + h(s)satisfies gh(s, a ) + βh (T (s, a )) − h(s) = log π⋆(a | s) exactly, so infinitely many decompositions of the same saddle logit coexist. Thus writing f = g + βh ′ − h does not, by itself, tell us which piece is the reward. AIRL needs extra structure that makes the split unique. The relevant property is not that next states identify current states; it is almost the opposite; enough overlap in one-step reachability that a next-state-only term cannot hide inside a sum of a 21 current-state term and a next-state term. Define the one-step successor and predecessor sets 

S+ := {x ∈ S : ∃ (s, a ) with P (x | s, a ) > 0}, S− := {s ∈ S : ∃ (a, x ) with P (x | s, a ) > 0}.

In a standard MDP S− = S, but S+ = S only if every state has a one-step in-neighbor. Build an undirected graph on S+, placing an edge between successors x and y whenever they share a predecessor (some s and actions a, b with P (x | s, a ) > 0 and P (y | s, b ) > 0) and close the relation transitively. The transition kernel is decomposable on the relevant state space (the Fu–Luo–Levine condition) if this successor graph is connected and, for any claim of equality on all of S, additionally 

S+ = S− = S.The useful consequence is the following separation lemma. The domain qualification matters: the equation only sees v at states that appear as successors, and only sees u at states that appear as predecessors. 

Lemma 3.7 (Separation under decomposability) . Suppose the successor graph on S+ is connected. Let u, v : S → R be bounded functions with 

u(s) + v(s′) = 0 (42) 

for every supported transition (s, a, s ′), i.e. whenever P (s′ | s, a ) > 0. Then v is constant on S+ and 

u is constant on S−. If moreover S+ = S− = S, both functions are constant on all of S.Proof. Fix a predecessor state s ∈ S −. If x and y are both one-step successors of s under possibly different actions, then (42) gives u(s) + v(x) = 0 and u(s) + v(y) = 0, hence v(x) = v(y): v is constant along every graph edge. Connectedness of the successor graph propagates the equality, so 

v is constant on S+. Substituting that constant back into (42) forces u(s) to equal the negative of that constant for every s ∈ S −.

Proposition 3.8 (AIRL disentanglement under deterministic decomposable dynamics) . Assume the following. (i) The ground-truth reward is state-only: r(s, a ) = r(s).(ii) AIRL uses a state-only reward head and a state-only shaping head: fg,h (s, a, s ′) = g(s)+ βh (s′)−

h(s).(iii) The transition is deterministic, so s′ = T (s, a ).(iv) The supported transitions cover and decompose the relevant state space: S+ = S− = S, and the one-step successor-link graph on S+ is connected. (v) The exact AIRL saddle is reached: on every supported transition, fg,h (s, a, T (s, a )) = A⋆(s, a ).

22 Then there exists a scalar c ∈ R such that 

h(s) = V ⋆(s) + c, g(s) = r(s) + (1 − β)c for all s. 

In particular, AIRL’s reward head identifies the true state-only reward up to an additive constant. Proof. With a state-only reward and deterministic dynamics, the soft Bellman equation reads 

Q⋆(s, a ) = r(s) + βV ⋆(T (s, a )), so the soft advantage is 

A⋆(s, a ) = Q⋆(s, a ) − V ⋆(s) = r(s) + βV ⋆(T (s, a )) − V ⋆(s).

Assumption (v) then gives, on every supported transition, 

g(s) + βh (T (s, a )) − h(s) = r(s) + βV ⋆(T (s, a )) − V ⋆(s).

Write ∆ := h − V ⋆ and δ := g − r. Substituting h = V ⋆ + ∆ and g = r + δ and cancelling the common r, V ⋆ terms yields 

 δ(s) − ∆( s) + β ∆( T (s, a )) = 0 ∀(s, a ). (43) This is exactly the form handled by Lemma 3.7: set u := δ − ∆ and v := β∆. By the coverage and connectedness assumptions in (iv), the lemma makes v, hence ∆, constant on all of S; write ∆ ≡ c.Then (43) reads δ(s) − c + βc = 0, so δ ≡ (1 − β)c. Hence h = V ⋆ + c and g = r + (1 − β)c.

All four restrictions are load-bearing. Proposition 3.8 is sharp: each substantive restriction removes a different obstruction. Here the state-only restriction combines assumptions (i)–(ii), so the four restrictions are state-only structure, deterministic dynamics, decomposable coverage, and an attained exact saddle. 

(i)–(ii) State-only rewards. If the reward head may depend on ( s, a ), the shaping family of Lemma 3.1 comes straight back. For any bounded potential Φ : S → R, set h = V ⋆ + Φ and 

gΦ(s, a ) := r(s, a ) + Φ( s) − β Es′∼P (·| s,a )[Φ( s′)] . (44) Then gΦ(s, a ) + β E[h(s′) | s, a ] − h(s) = A⋆(s, a ) identically. In deterministic dynamics this expectation is just evaluation at T (s, a ); in stochastic dynamics it is the population version of the same ambiguity. Either way, every potential-shaped reward produces the same soft advantage, so AIRL leaves state-action rewards unidentified beyond the baseline shaping equivalence. 

(iii) Deterministic dynamics. Determinism matters because then the next state T (s, a ) is fixed once ( s, a ) is fixed, so the AIRL term h(T (s, a )) can line up pointwise with the Bellman equation. The proof used exactly this identity: A⋆(s, a ) = r(s) + βV ⋆(T (s, a )) − V ⋆(s). Under stochastic 23 transitions, the Bellman identity carries a conditional expectation, while AIRL’s realized term h(s′)depends on one sampled next state. The most one can ask is the population moment 

Es′∼P (·| s,a )[fg,h (s, a, s ′)] = A⋆(s, a ). (45) Even granting (45) , reward recovery is not automatic. With a state-only reward and state-only heads, the soft Bellman equation turns (45) into g(s)+ βE[h(s′) | s, a ]−h(s) = r(s)+ βE[V ⋆(s′) | s, a ]−V ⋆(s); with ∆ := h − V ⋆ and δ := g − r this rearranges to 

δ(s) = ∆( s) − β E[∆( s′) | s, a ] ∀a. (46) The left-hand side is independent of a, so E[∆( s′) | s, a ] must be independent of a as well. This forces ∆ to be locally, and then globally, constant only if the transition family is rich enough that the condition “all action-conditioned expectations of ∆ agree” implies ∆ is constant: a completeness condition on P . Mere dependence of P (· | s, a ) on a is not enough, since two next-state distributions can share the expectation of a non-constant ∆. Under the completeness condition one recovers 

h = V ⋆ + c and g = r + (1 − β)c as before; without it, (46) leaves residual shaping ambiguity. This distinction clarifies the role of transition-aware AIRL variants. Zhan, Wu, et al. (2024) tries to address the stochastic-realizability obstruction: replacing AIRL’s realized-next-state term h(s′)by a model-based approximation to E[h(s′) | s, a ], equivalently a transition-aware shaping term such as R(s, a ) + βE ˆT [φ(s′) | s, a ] − φ(s), makes the logit compatible with the stochastic Bellman identity up to learned-transition error. That repair does not by itself supply the completeness or normalization condition needed for identification. After the expectation is inserted, the remaining equation is still (46) , so reward recovery still requires that the only potential differences ∆ whose action-conditioned transition expectations agree are constants. 

(iv) Connectivity and coverage. If the successor graph splits into several components, the separation argument forces ∆ constant only within each component, and different per-component constants give genuinely different decompositions consistent with the same saddle. States that never appear as one-step successors leave h unconstrained there; states that never appear as predecessors leave g

unconstrained. Deterministic systems with little branching, or with disconnected recurrent classes, routinely fail the condition. 

(v) Exact saddle. If the policy player has not matched the expert occupancy, or if the discriminator class cannot realize its Bayes optimum, the equality fg,h = A⋆ need not hold on the expert support. The learned logit then reflects the current policy/discriminator game rather than the expert’s soft advantage, so there is no exact advantage identity to decompose into reward plus shaping terms. 

Relation to anchor actions. The bottom line is that AIRL is elegant because it builds potential shaping into the discriminator, but that removes the baseline identification problem only under assumptions such as state-dependent rewards and deterministic transitions. At the perfect saddle, 24 it recovers the expert’s soft advantage; reward recovery requires additional structure that makes the advantage decomposition unique. To summarize: 

• AIRL recovers Hotz-Miller conversion under deterministic transitions. In other words, under deterministic transitions, making the anchor action assumption additionally yields exact reward recovery (Magnac and Thesmar, 2002). However, under stochastic transitions, the Hotz-Miller conversation is not recovered because it stems from the trajectory energy model (which is fundamentally myopic under stochastic transitions (Cao et al., 2021)). 

• AIRL can avoid the anchor-action assumption, following Fu et al. (2017), by making extra assumptions such as deterministic transitions and state-only rewards. For the remainder of these notes, we go back to the anchor action assumption Assumption 3.3: it identifies state-action rewards directly, on the expert support, without requiring state-only rewards, deterministic dynamics, or a decomposable transition graph. 

# 4 Classical DDC Methods 

We now survey the classical estimation routes that grew out of the Magnac–Thesmar identification: Rust’s nested fixed-point algorithm (Section 4.2), the conditional-choice-probability approach of Hotz–Miller (Section 4.3), and the Adusumilli–Eckardt temporal-difference route (Section 4.4), which itself contains two methods: linear semi-gradient TD and approximate value iteration. Each route gets one of the following ingredients right, but pays a price elsewhere: 

• Scalability in the dimension of S,

• Transition-kernel freedom (no separate estimate of P required), 

• Stability (no deadly-triad-style divergence). Section 6 is then about how we can address this issue. The starting point is common to all. By Theorem 3.4, π⋆ pins down Q⋆ once an anchor action is fixed. Operationally, this requires solving two coupled conditions (the softmax MLE condition and the soft Bellman equation at the anchor action) that we record now. 

4.1 The Two Pieces: NLL and Bellman 

Lemma 4.1 (Softmax MLE optimality) . Let ρ ∈ ∆( S × A ) have state marginal ρS and conditional 

ρ(· | s) = π⋆(· | s) for ρS -a.e. s. Then 

arg min 

> Q∈Q

E(s,a )∼ρ

− log ˆ pQ(a | s) = {Q ∈ Q : ˆ pQ(· | s) = π⋆(· | s) ρS -a.e. },

25 where ˆpQ(a | s) := eQ(s,a )/ P 

> a′

eQ(s,a ′).Proof. The objective decomposes as 

E(s,a )∼ρ

− log ˆ pQ(a | s) = Es∼ρS

KL( π⋆(· | s) ∥ ˆpQ(· | s))  + Es∼ρS

H( π⋆(· | s)) .

The second term is independent of Q. The first is non-negative and zero iff ˆpQ(· | s) = π⋆(· | s) for 

ρS -a.e. s (Gibbs’ inequality). Notice what the NLL gives us and what it does not. The softmax ˆpQ is invariant under state-only shifts Q(s, a ) 7 → Q(s, a ) + c(s) (Lemma 2.3). So fitting π⋆ alone pins down Q only up to such shifts, exactly the shaping ambiguity of Lemma 3.1. The Bellman condition is what locks down those shifts. 

The classical methods differ in how they enforce it. 

4.2 Rust’s Nested Fixed-Point Algorithm (NFXP) 

Rust (1987) proposed to parametrize the reward rθ in a low-dimensional class and treat the Bellman equation as an inner-loop constraint: Outer (over θ): θk+1 = arg min 

> θ

bED

− log ˆ pQθ (a | s),

Inner (at each θ): Qθ := (unique fixed point of T rθ

> soft

) computed by soft value iteration. Convergence of the inner loop is guaranteed by Lemma 2.4; consistency of the outer MLE is the classical result of Rust (1987). This is the right thing to do, statistically; the problem is doing it. 

The computational obstruction. The inner soft value iteration is over S ×A . For each iteration, the LSE LSE( Q(s′, ·)) = log X

> a′∈A

exp( Q(s′, a ′)) must be evaluated at every s′ the outer expectation might query. Discretizing S to |S| points and running value iteration costs Θ( |S| · |A| ) per iteration to convergence in Θ( log (1 /ε )/(1 − β)) iterations, and the inner loop sits inside the outer loop over θ. When the underlying state has intrinsic dimension d, fixed-grid discretization to resolution h produces |S| = Θ( h−d), so the inner cost scales as h−d: the curse of dimensionality of dynamic programming . Workarounds (sieve approximations (Kristensen et al., 2021; Arcidiacono, Bayer, Bugni, et al., 2013), recursive partitioning (Barzegary and Yoganarasimhan, 2022), state aggregation (Geng, Nassif, and Manzanares, 2023), neural-network approximations (Norets, 2012)) push the boundary but do not change the scaling. 26 The transition kernel is also needed. There is a second problem hidden inside the first. The inner soft value iteration requires the expectation Es′∼P (·| s,a )[LSE (Q(s′, ·))], which presumes either (i) closed-form access to P (rare outside textbook problems), or (ii) a separately estimated ˆP . In option (ii), error in ˆP propagates through the fixed-point computation; Rust (1994) requires P to be known a priori or consistently estimable in a first stage, a non-trivial demand in high dimensions. A constrained-optimization reformulation (MPEC) that avoids the nested loop in low dimensions is given by Su and Judd (2012): treat both θ and the value function as optimization variables and minimize the likelihood subject to the Bellman equation as a constraint. MPEC still requires P .

4.3 Hotz–Miller / Conditional Choice Probabilities (CCP) 

Hotz and Miller (1993) attack the inner-loop bottleneck differently. Their starting point is the observation underpinning (29) : the differences of Q⋆ across actions at the same state are already pinned down by the observed log choice-probability ratios. Identity (30) rearranges to 

V ⋆(s) = Q⋆(s, a s) − log π⋆(as | s), (47) and the linear fixed-point equation (32) that follows is a Bellman-like equation for V ⋆ alone, with reward − log π⋆(as | · ). Once V ⋆ is in hand, Q⋆(s, a ) follows from (29) and (47) , and r follows from the soft Bellman equation (33). 

Forward simulation. The expectation in (32) can be approximated by forward Monte Carlo under the anchor-conditioned kernel: simulate trajectories of length T starting at s, set the action at each simulated state st to the anchor action ast , draw st+1 from ˆP (· | st, a st ), and accumulate the per-step pseudo-reward − log ˆ π⋆(ast | st). Drawing actions from ˆπ⋆ would solve a different Bellman equation; the anchor equation (32) specifically uses P (· | s, a s). This sidesteps the high-dimensional fixed-point computation of NFXP entirely. Recursive variants of CCP (Aguirregabiria and Mira, 2002; Aguirregabiria and Mira, 2007) interleave π⋆ updates and value iteration. 

The statistical obstruction. We bought the computational savings of CCP at a price, and it is a steep one. Forward simulation requires estimating ˆP in high dimensions , then propagating its error over T = Θ(1 /(1 − β)) steps. With a continuous or high-dimensional state space, ˆP is itself subject to a curse of dimensionality: a nonparametric kernel density estimator of P (s′ | s, a ) has rate N −c/d for some smoothness-dependent c. The statistical complexity blows up exactly where NFXP’s computational complexity did. 

Recovery of r. Once V ⋆ and Q⋆ are recovered, the reward is read off (33) . This expression requires another P -expectation; in continuous states it is again subject to transition-estimation error. 27 The summary. NFXP and CCP are duals. NFXP fits r in an outer loop and solves the Bellman fixed point in the inner. CCP fits π⋆ first and exploits Hotz–Miller inversion to avoid the inner fixed point but needs P everywhere. Both require either knowing or estimating P , and both impose either computational ( |S| · |A| DP table) or statistical ( ˆP in d dimensions) burdens scaling exponentially in d. Neither produces a single differentiable objective whose minimizer is Q⋆.

4.4 Temporal-Difference DDC: Linear Semi-Gradient and Approximate Value Iteration 

The natural next thought is: can we avoid the transition kernel by using temporal-difference updates based directly on observed transitions? Adusumilli and Eckardt (2019) propose exactly this. More precisely, they propose two transition-density-free TD-style approaches for the recursive terms that enter CCP estimation: (1) Linear semi-gradient TD , which approximates the recursive terms with basis functions and updates the coefficients by stochastic approximation. (2) Approximate Value Iteration (AVI) , which builds a sequence of approximations by freezing the previous continuation-value estimate and solving a fresh nonparametric regression problem at each iteration. In the full DDC pseudo-likelihood these recursive terms are the continuation-value objects induced by Hotz–Miller inversion. In our anchor-action notation they collapse to the auxiliary value function in (32) . Thus the same analysis covers both Adusumilli–Eckardt methods: one is the semi-gradient form of TD, and the other is fitted/approximate value iteration. The only twist is that we are now bootstrapping the auxiliary value function (32), not the original value function of an MDP. 

4.4.1 Linear Function Approximation and the Projected Bellman Equation 

Fix a feature map ϕ : S → Rd and let Φ denote the |S| × d feature matrix (in the finite-state case; the general case is analogous in L2). Parametrize Vθ(s) := ϕ(s)⊤θ, θ ∈ Rd. The hypothesis class is the linear span F := {V : V (s) = ϕ(s)⊤θ, θ ∈ Rd}, a closed subspace of bounded functions on S.Fix a distribution ρ ∈ ∆( S) (the data distribution) with associated L2(ρ)-inner product ⟨f, g ⟩ρ := 

Es∼ρ[f (s)g(s)] and norm ∥f ∥2 

> ρ

:= ⟨f, f ⟩ρ. The L2(ρ)-projection onto F is ΠF,ρ V := arg min  

> Vθ∈F

∥V − Vθ∥2

> ρ

,

which is linear in V and a non-expansion in ∥ · ∥ ρ.28 Let T πb denote the (linear) Bellman expectation operator under a behavior policy πb with corre-sponding kernel P πb (s′ | s) = P 

> a

πb(a | s)P (s′ | s, a ): 

T πb V (s) := rπb (s) + β Es′∼P πb (·| s)[V (s′)] , rπb (s) := X

> a

πb(a | s)r(s, a ). (48) For the TD-DDC application below, πb will be the anchor-conditioned chain πb(a | s) = 1[a = as]and the per-state “reward” will be − log π⋆(as | s). 

Lemma 4.2 (On-policy contraction) . If ρ is the stationary distribution of P πb , then T πb is a 

β-contraction on L2(ρ), and ΠF,ρ T πb is a β-contraction on F.Proof. For any V1, V 2 ∈ L2(ρ), since ρ is stationary under P πb , ρP πb = ρ. By Jensen’s inequality applied conditionally, 

∥T πb V1−T πb V2∥2 

> ρ

= β2Es∼ρ

(Es′∼P πb (·| s)[V1(s′)−V2(s′)]) 2 ≤ β2Es′∼ρP πb

(V1(s′)−V2(s′)) 2 = β2∥V1−V2∥2

> ρ

.

Since Π F,ρ is a non-expansion, the composition is also a β-contraction. The unique fixed point V ⋆ 

> F,ρ

of Π F,ρ T πb is the projected Bellman fixed point . It solves the projected Bellman equation VF,ρ = Π F,ρ T πb VF,ρ ; in matrix form, θ⋆ solves Aθ = b with 

A = Φ ⊤Dρ(Φ − βP πb Φ) , b = Φ ⊤Dρrπb . (49) When ρ is the stationary distribution of P πb , Tsitsiklis and Van Roy (1996a) show that A is positive definite. Linear TD(0), whose update direction has expectation b − Aθ , then converges to A−1b

with rate determined by the smallest eigenvalue of A.

4.4.2 The Deadly Triad: Off-Policy Divergence of Linear TD 

The on-policy stability of Lemma 4.2 relies critically on ρ being the stationary distribution of P πb .The combined presence of (i) linear (or more general) function approximation, (ii) bootstrapping, and (iii) off-policy data is known as the deadly triad (Sutton and Barto, 2018; Van Hasselt et al., 2018). Here is its TD-DDC face. 

Example 4.3 (Off-policy divergence with two states) . Take S = {1, 2}, A = {a} (so πb is trivial), with deterministic transitions 1 7 → 2 and 2 7 → 2, discount β = 0 .99 , and reward r ≡ 0. The true value is V ⋆ ≡ 0. Choose feature map ϕ(1) = 1 , ϕ(2) = 2 , so F is one-dimensional with parameter θ.The stationary distribution of P is concentrated at state 2. Now suppose the data distribution gives most weight to state 1: ρ = (0 .99 , 0.01) . Compute 

A = Φ ⊤Dρ(Φ − βP Φ) = [1 , 2] diag(0 .99 , 0.01)  [1 , 2] ⊤ − 0.99 · [2 , 2] ⊤

= 0 .99 · 1 · (1 − 1.98) + 0 .01 · 2 · (2 − 1.98) ≈ − 0.97 ,

29 so A < 0. The linear TD update direction is ∝ (b − Aθ ) = −Aθ ≈ 0.97 θ: for any nonzero initializa-tion and a fixed small positive stepsize, the iterates move away from zero and grow exponentially. The finite TD equilibrium is θ = 0 , but it is a repelling equilibrium for this off-policy update, despite 

V ⋆ ≡ 0 being trivially representable. 

This is not a pathological construction. Wang et al. (2021) document the same phenomenon with neural-network function approximation, where it persists even with mild representation pre-training. And critically for us: the deadly triad applies in particular to the DDC application below. The offline DDC dataset is generated by the expert acting according to π⋆ under all actions, so its state marginal is dπ⋆ 

> S

. The TD-DDC update, however, bootstraps along the anchor-conditioned chain whose kernel is P (· | · , a s), a deterministic “policy” that always picks the state-specific anchor. After restricting to anchor transitions, the effective sampling distribution is ρA(s) ∝ π⋆(as | s) dπ⋆ 

> S

(s), which is in general not the stationary distribution of P (· | · , a s). The on-policy contraction of Lemma 4.2 therefore does not apply to this update, and Example 4.3’s pathology is on the table. 

4.4.3 Approximate Value Iteration as Fitted Value Iteration 

The second Adusumilli–Eckardt method is not the same as the linear semi-gradient recursion above. It is Approximate Value Iteration : freeze the current approximation inside the continuation term, then solve a new regression problem for the next approximation. In the linear case this takes the form 

θ+ := arg min 

> θ

Es∼ρ, s ′∼P πb (·| s)

(rπb (s) + β ϕ (s′)⊤θ− − ϕ(s)⊤θ)2, θ− ← θ+, (50) and iterate. The inner problem is a well-conditioned least-squares problem whenever Φ ⊤DρΦ ≻ 0; its closed form is 

θ+ = (Φ ⊤DρΦ) −1Φ⊤Dρ(rπb + βP πb Φθ−) = (Φ ⊤DρΦ) −1Φ⊤DρT πb (Φ θ−).

Viewed as a map θ− 7 → θ+, this is exactly fitted value iteration (FVI): Vθ+ = Π F,ρ T πb Vθ− . In a nonparametric or sieve implementation, the same map is computed by replacing the linear least-squares problem with the corresponding regression learner over the chosen function class. Thus, in these notes, Adusumilli–Eckardt’s AVI method is the DDC specialization of the standard fitted-value-iteration operator. 

Proposition 4.4 (FVI fixed point) . Suppose ΠF,ρ T πb is a contraction in ∥ · ∥ ρ (for example, the on-policy case of Lemma 4.2, or any separately verified stability condition on the projected operator). Then FVI/AVI iterates (50) converge to a unique fixed point V ⋆ 

> F,ρ

= Π F,ρ T πb V ⋆

> F,ρ

. In general, 

V ⋆

> F,ρ

̸ = V ⋆.Proof. The first claim follows from Banach’s theorem applied to the FVI map on the closed subspace F ⊆ L2(ρ). For the second, note that any fixed point of Π F,ρ T πb lies in F, while V ⋆, the 30 unconstrained Bellman fixed point, may not. Concretely, V ⋆ satisfies V ⋆ = T πb V ⋆ but generally not 

V ⋆ = Π F,ρ V ⋆, so Π F,ρ T πb V ⋆ = Π F,ρ V ⋆̸ = V ⋆ whenever V ⋆ /∈ F . In particular, whenever the true value function does not lie in the chosen feature class, the FVI fixed point is the closest F-element to a transformed target and differs from V ⋆ by the approximation error. 

Corollary 4.5 (Projected fixed points and realizability) . Assume the projected operator ΠF,ρ T πb has a unique fixed point. If V ⋆ ∈ F , then this fixed point equals V ⋆, because ΠF,ρ T πb V ⋆ = Π F,ρ V ⋆ = V ⋆.If V ⋆ /∈ F , the projected fixed point cannot equal V ⋆, since every projected fixed point lies in F.Closure of F under T πb is a stronger sufficient condition for projection-free iterates, but it is not necessary for equality of the limiting projected fixed point. Thus the two distinct obstructions are stability of the projected operator and realizability of V ⋆ in the chosen class. 

4.4.4 Application: The Two TD-DDC Methods of Adusumilli–Eckardt 

Adusumilli and Eckardt (2019) apply this machinery in the DDC setting with the substitutions of Section 4.3: rπb (s) := − log π⋆(as | s) and πb(a | s) := 1[a = as]. Let 

IA := {i ∈ { 1, . . . , N } : ai = asi }

be the set of observed transitions in which the expert took the anchor action, and write DA := 

{(si, s ′

> i

) : i ∈ IA} for the corresponding sub-dataset with state marginal ρA.

Method 1: linear semi-gradient TD. The first method updates the coefficient vector using the current parameter on both sides of the TD error: 

θt+1 = θt + αt

 − log π⋆(ast | st) + β ϕ (st+1 )⊤θt − ϕ(st)⊤θt

 ϕ(st), (51) sampled from transitions ( st, a t = ast , s t+1 ) in D.

Method 2: approximate value iteration. The second method freezes the previous value approximation. Starting from V0 ∈ F , define pseudo-outcomes for i ∈ IA by 

Y (k) 

> i

:= − log π⋆(asi | si) + βV k(s′

> i

), (52) and compute 

Vk+1 ∈ arg min  

> f∈F

1

|IA|

X

> i∈IA

 Y (k) 

> i

− f (si)2. (53) When F is linear, Vk(s) = ϕ(s)⊤θk and (53) is exactly (50) ; when F is a sieve or nonparametric class, (53) is the nonparametric AVI regression. Either way, the population operator is Vk+1 =ΠF,ρ A T πb Vk.31 Replaying the analysis in the DDC setting. The two methods have different failure modes. 

• The linear semi-gradient update (51) has all three deadly-triad ingredients: function approxi-mation, bootstrapping, and off-policy state weighting relative to the anchor-conditioned chain. By Example 4.3, it has no general guarantee of convergence to V ⋆, the unique solution of (32) .

• The AVI/FVI update (53) avoids the within-iteration moving-target gradient problem by freezing Vk and solving a supervised regression. But it is still an iterated projected Bellman method. If Π F,ρ A T πb is a contraction, Proposition 4.4 gives convergence; the limit is the projected fixed point V ⋆ 

> F,ρ A

, which equals V ⋆ when V ⋆ is realizable in F and the projected fixed point is unique, as in Corollary 4.5. Without such a contraction, even the projected iteration can fail to converge. 

The honest scorecard. Both Adusumilli–Eckardt methods avoid the explicit transition kernel that CCP/Hotz–Miller needed: they use observed ( st, s t+1 ) pairs in place of an explicit EP (·| s,a ).That is a genuine improvement over forward simulation. But the price is not the same for the two methods. Tallying: 

• NFXP: statistically efficient, transition-kernel-needing, computationally cursed in d.

• CCP/Hotz–Miller: computationally cheap, transition-estimation-needing, statistically cursed in 

d.

• Adusumilli–Eckardt linear semi-gradient TD: transition-kernel-free, but deadly-triad-vulnerable. 

• Adusumilli–Eckardt AVI/FVI: transition-kernel-free and stable as a sequence of regression problems, but still a bootstrapped projected-fixed-point method; convergence to the true V ⋆

requires both stability of the projected operator and realizability of V ⋆ in the chosen function class. None produces a single unbiased objective whose global minimum is the true Q⋆, computable with mild conditioning assumptions on the parametrization and without specifying P .

4.4.5 Why “Neural Orthogonalization” (Target Networks) Does Not Resolve It 

A reader familiar with deep RL might wonder whether the linear semi-gradient instability of Method 1 can be cured by introducing a target network, exactly as in the forward-RL setting. The answer is the same here as there, for the same reason: target networks change which method you are running, not where it converges. We make this precise, then explain why it leaves the underlying issue intact. 

Proposition 4.6 (Target-stabilized semi-gradient TD-DDC equals AVI) . Consider Method 1, equation (51) , modified so that the parameter θ inside the bootstrap term ϕ(st+1 )⊤θ is replaced by 

32 a target parameter θ−, refreshed only after the inner optimization in θ has reached its empirical minimum at each outer iteration. Then the outer iterates coincide with Method 2, equation (53) , in both empirical and population form: 

Vθ(k+1) = bΠF,DA

 ˆT πb Vθ(k)

 (empirical) , V (k+1) = Π F,ρ A T πb V (k) (population) , (54) 

where bΠF,DA f := arg min V ∈F bEDA [( f − V )2] is the empirical L2(DA)-projection and ˆT πb V (s, s ′) := 

− log π⋆(as | s) + βV (s′) is the sample version of T πb .Proof. The argument is direct, and is especially clean because T πb is a Bellman expectation operator and there is no max in sight. 

Empirical step. Fix the outer iterate θ(k) and set θ− := θ(k) throughout the inner loop. The per-sample target 

yi := − log π⋆(asi | si) + β ϕ (s′

> i

)⊤θ(k) = ˆT πb Vθ(k) (si, s ′

> i

)does not depend on the optimization variable θ. The inner loss ℓi(θ) := 12 (yi − ϕ(si)⊤θ)2 has gradient 

−(yi − ϕ(si)⊤θ) ϕ(si), which is exactly the negative of the target-stabilized version of the update direction in (51) (the bootstrap term is constant in θ, so contributes nothing). Inner convergence yields 

θ(k+1) ∈ arg min 

> θ

bEDA

( ˆT πb Vθ(k) − ϕ(s)⊤θ)2,

which is the empirical regression (53) defining Method 2. 

Population step. Since T πb is the Bellman expectation operator under the anchor-conditioned chain, 

Es′∼P πb (·| s)[ ˆT πb Vθ(k) (s, s ′)] = T πb Vθ(k) (s). The inner objective splits as 

EρA, P πb

( ˆT πb Vθ(k) − ϕ(s)⊤θ)2 = EρA

(T πb Vθ(k) − ϕ(s)⊤θ)2 + β2 EρA

Var s′|s(Vθ(k) (s′)) ,

by the variance decomposition (cf. Lemma 2.10). The second term depends only on θ(k), not on θ.So the minimizer in θ is Π F,ρ A T πb Vθ(k) , exactly Method 2’s population operator. 

What the proposition says, and what it doesn’t. Target stabilization moves Method 1 onto Method 2’s orbit; it does not change Method 2’s destination. The projected-fixed-point obstruction of Corollary 4.5 is unaffected: even with perfect target stabilization and idealized inner optimization, the limit (when it exists) is the projected fixed point V ⋆ 

> F,ρ A

. That limit equals the true auxiliary V ⋆

only under additional conditions such as realizability of V ⋆ in F and uniqueness/stability of the projected fixed point. These conditions are not innocuous for neural function classes in continuous-state, off-policy settings; they are exactly the kind of structural assumptions the deadly-triad analysis (Section 4.4.2) warns us we cannot expect for free. In particular, three caveats are worth recording: 33 • Linear F, off-policy data (ρA not invariant under P πb ): the projected operator can fail to be a contraction; Example 4.3 applies, and target stabilization does not rescue it. 

• Linear F, on-policy stationary data : the projected operator is a β-contraction by Lemma 4.2, and target-stabilized Method 1 converges to V ⋆ 

> F,ρ A

, the projected fixed point. This limit equals 

V ⋆ if V ⋆ lies in F; otherwise it is a projected approximation. 

• Nonlinear F (the relevant regime for offline IRL/DDC at scale): Π F,ρ A is set-valued, the inner regression may have multiple local minima, and (54) does not uniquely define a single-valued map. Even pointwise selecting an inner minimizer, Π F,ρ A T πb need not be Lipschitz, and convergence requires assumptions far beyond what is verifiable in practice. The methodological takeaway is the following: 

Target networks stabilize the trajectory of bootstrapped TD-DDC updates but do not change their destination. The destination is the projected Bellman fixed point of the auxiliary value function (32) , not the auxiliary value function itself. 

This rules out the most familiar deep-RL fix and points to the same conclusion: the right escape route in offline IRL/DDC is to abandon bootstrapping entirely in favor of direct (bias-corrected) Bellman-residual minimization. That is the gap Section 6 closes. 

# 5 Modern IRL Methods 

We now turn to the ML/IRL literature. Four families dominate it: adversarial IRL, occupancy matching, IQ-Learn, and offline maximum-likelihood IRL. For each we (a) write out the actual training objective, (b) state precisely what it identifies and what it does not, and (c) compare to the anchor-action route. A general survey of imitation learning is given by Zare et al. (2024). A preview of where the section lands: each modern method makes a different bet against the same identification ambiguity (Lemma 3.1), and each pays for the bet somewhere: in the form of reward identified, in stability, in compatibility with offline data, or in what the method actually optimizes. 

5.1 Adversarial IRL (AIRL) 

Section 3.3 introduced AIRL’s language and formulation: the potential-shaped discriminator (39) and the alternating saddle update (40) . We return to AIRL here as a modern IRL method, focusing on what that formulation delivers computationally and what it still cannot identify. 34 What the saddle delivers. The textbook saddle calculation needs two idealizations made explicit. First, the policy player must have converged to the expert, so dπ = dπ⋆

on the relevant support. Second, the AIRL discriminator class must be able to realize its Bayes optimum. Under both, D⋆ ≡ 12 on the matched support, and since (39) gives D = 12 exactly when exp fθ,ϕ = π(a | s), the saddle pins down 

fθ,ϕ (s, a, s ′) = log π⋆(a | s) = A⋆(s, a ).

As emphasized in Section 3.3, this is an advantage identity, not yet a reward identity, and it carries no information beyond the expert’s conditional choice probabilities, the same object Hotz–Miller inversion (29) consumes through log-probability differences. The discriminator’s real burden is therefore not to discover the advantage, but to split it into a reward part and a shaping part. In stochastic environments even the equality above is delicate: the right-hand side A⋆(s, a ) is independent of s′ while the AIRL class g(s) + βh (s′) − h(s) is not, so exact D ≡ 12 within the parametrized class may be unattainable. Section 3.3 treats this carefully through the population moment (45). 

Identification limitations. The self-contained analysis of Section 3.3 (Proposition 3.8) shows precisely what extra structure makes reward recovery valid, and what goes wrong otherwise. The limitations are structural rather than merely computational: 

• With a state-only ground-truth reward, a state-only reward head, deterministic dynamics, an attained exact saddle, and the decomposable-coverage condition, AIRL recovers the reward up to an additive constant: gθ(s) = r(s) + C and hϕ(s) = V ⋆(s) + C/ (1 − β). 

• If gθ is allowed to depend on ( s, a ), the potential-shaped family (44) gives the same advantage, so state-action rewards remain unidentified beyond the baseline shaping equivalence of Lemma 3.1. 

• In stochastic environments there is both a realizability gap, because the parametrized logit cannot match the s′-independent target A⋆(s, a ) pointwise, and an identification gap: even the population moment (45) needs a transition-kernel completeness condition to pin down the shaping potential. The anchor-action route gives strictly more for these notes: it identifies state-action r on the expert support without requiring the reward to be state-only and without relying on the AIRL decomposition (cf. Theorem 3.4 and Theorem 3.2). 

Optimization limitations. On top of this identification ceiling, AIRL inherits the well-documented instabilities of GAN training: the alternating minimax updates on ( θ, ϕ ) and π can fail to converge, exhibit mode collapse, or settle at a non-optimal saddle. And AIRL is fundamentally an online 

method: the discriminator update (40) requires sampling from the current policy π, which is exactly what offline IRL forbids. 35 5.2 Occupancy Matching: GAIL 

Ho and Ermon (2016) introduced Generative Adversarial Imitation Learning (GAIL). The starting point is the occupancy-matching objective min  

> π

DJS 

 dπ ∥ dπ⋆ ,

where DJS is the Jensen–Shannon divergence. The variational identity behind GAIL is the following. For any two distributions p, q on a common space X , the GAN value at the inner D-optimum is max  

> D:X → (0 ,1)

Ep[log D] + Eq[log(1 − D)] = − log 4 + 2 DJS (p ∥ q), (55) with optimum attained at D⋆ = p/ (p + q) (Goodfellow et al., 2020, Prop. 1). The derivation is direct: writing the integrand at a point as V (D) := p log D + q log (1 − D) and setting ∂V /∂D = 0 gives 

D⋆ = p/ (p + q), and substituting back recovers − log 4 + 2 DJS (p∥q) via DJS (p∥q) = 12 KL (p∥(p +

q)/2) + 12 KL (q∥(p + q)/2). So minimizing the JS over π is equivalent (up to additive constants) to the saddle min  

> π

max  

> D

E(s,a )∼dπ⋆ [log D(s, a )] + E(s,a )∼dπ [log(1 − D(s, a ))] . (56) GAIL solves (56) with a GAN-style optimizer over a parametrized Dϕ and πθ, using on-policy policy-gradient methods such as PPO (Schulman et al., 2017; Raffin et al., 2021) for the πθ update. Related state-marginal-matching variants restrict D to depend only on s (Ni et al., 2021). 

What GAIL is and isn’t. GAIL is fundamentally an imitation algorithm: its goal is to make 

dπθ close to dπ⋆

in JS divergence, and it succeeds at this on many tasks. But the crucial point is that it is not a reward-recovery algorithm . At the saddle, the discriminator satisfies log D⋆(s, a )1 − D⋆(s, a ) = log dπ⋆

(s, a )

dπθ (s, a ) ,

which is a log Radon–Nikodym derivative of expert vs. current-policy occupancies, not a reward. There is no way to extract r from D⋆ without further identification assumptions of exactly the kind we already discussed (Theorem 3.2 and the anchor-action assumption). 

Bellman consistency is not built in. The limitation can be seen directly from a one-line Bellman-flow identity. Fix any policy π, reward r, and candidate state-action value function Q.Define the policy Bellman operator (T πr Q)( s, a ) := r(s, a ) + βEs′∼P (·| s,a ), a ′∼π(·| s′)[Q(s′, a ′)] .

36 Using the normalized discounted occupancy 

dπ(s, a ) = (1 − β) X

> t≥0

βtPπ(st = s, a t = a),

we have 

Edπ

Q(s, a ) − βE[Q(s′, a ′) | s, a ] = (1 − β)Es0∼ν0, a 0∼π(·| s0)[Q(s0, a 0)] , (57) 

Edπ [Q(s, a ) − (T πr Q)( s, a )] = (1 − β)E[Q(s0, a 0)] − Edπ [r(s, a )] . (58) The first line is just telescoping: the second term equals (1 − β) P 

> t≥0

βt+1 E[Q(st+1 , a t+1 )], so subtracting it from (1 − β) P 

> t≥0

βtE[Q(st, a t)] leaves only the t = 0 term. The second line subtracts the reward. Equation (58) is a single weighted average of Bellman residuals under the occupancy dπ. Matching 

dπ to dπ⋆

can at most match such occupancy-weighted averages. It does not imply the pointwise Bellman equation 

Q(s, a ) = ( T πr Q)( s, a ) for every ( s, a ).

Residuals can be positive on one part of the support and negative on another while their dπ-average remains zero. Thus occupancy matching places a distributional constraint on where the policy goes ;it does not place an equation-of-motion constraint on a candidate Q.This also explains why a reward cannot be recovered from GAIL by first extracting some Q and then applying the Bellman rearrangement r = Q − βE[VQ(s′) | s, a ]. At a perfect GAIL match the discriminator itself is uninformative ( D⋆ = 1 /2), and any separately fitted Q is unconstrained by the pointwise Bellman residual unless that residual is explicitly included in the objective. The anchor-action and ERM routes of Section 3.2 and Section 6 do exactly that: they enforce the Bellman equation at the anchor action rather than hoping it follows from occupancy matching. 

Offline limitations. GAIL’s policy update is on-policy: it requires sampling from πθ to compute 

Edπθ [log (1 − D)]. An offline variant, ValueDice (Kostrikov et al., 2019), re-derives the objective using off-policy distribution-ratio estimation via the Fenchel dual of the f -divergence: instead of sampling from πθ, it estimates the ratio dπθ /d π⋆

from offline data and substitutes it into the GAIL objective. The empirical performance of these off-policy corrections is mixed; Li et al. (2022) show that improvements over a strong behavior-cloning baseline can be entirely attributed to architectural and regularization differences, not the off-policy correction itself. The deeper issue is the curse of horizon all over again: trajectory importance sampling for T -step corrections has variance Θ(( ρmax )T ) where 

ρmax is the per-step importance ratio bound and T ∼ 1/(1 − β) is the effective horizon, exponentially bad in the horizon for any non-trivial policy shift, as standard importance-sampling calculations show. 37 5.3 IQ-Learn 

Garg et al. (2021) propose IQ-Learn (Inverse soft-Q Learning), which sidesteps both forward simulation and adversarial training by changing variables from ( r, π ) to ( Q, π ). The key identity is the soft Bellman equation (6), rearranged: 

rQ(s, a ) := Q(s, a ) − β Es′∼P (·| s,a )[VQ(s′)] , VQ(s) := LSE( Q(s, ·)) . (59) Given any Q, equation (59) defines a reward rQ for which Q satisfies the soft Bellman equation by construction. IQ-Learn parametrizes Qθ directly and maximizes 

J (Qθ) := E(s,a )∼dπ⋆ [rQ(s, a )] − (1 − β) Es0∼ν0 [VQ(s0)] − λ E(s,a )∼dπ⋆ [ψ(rQ(s, a ))] , (60) where ψ is a strongly convex regularizer (the chi-squared ψ(x) = x2/4 in the simplest version) and λ > 0. To see why the first two terms behave as an entropy-regularized return gap, use the Bellman-flow identity: for any function V and any policy π,

E(s,a )∼dπ

V (s) − β Es′∼P (·| s,a )[V (s′)]  = (1 − β) Es0∼ν0 [V (s0)] , (61) which is the discounted-occupancy form of the telescoping sum P 

> t

βt(V (st) − βV (st+1 )). Apply-ing (61) with V = VQ and π = π⋆, and using the definition of rQ,

E(s,a )∼dπ⋆ [rQ(s, a )] − (1 − β)Es0∼ν0 [VQ(s0)] = E(s,a )∼dπ⋆

Q(s, a ) − VQ(s).

The RHS is exactly E(s,a )∼dπ⋆ [log ˆ pQ(a | s)] = −Es∼dπ⋆S

KL (π⋆(·| s) ∥ ˆpQ(·| s))  − Es∼dπ⋆S

[H(π⋆(·| s))] by the same decomposition as in Lemma 4.1. So maximizing the first two terms of J in Q is exactly minimizing the KL between the softmax of Q and the expert policy, i.e., it fits Q to a value function whose softmax matches π⋆, hence to the soft Bellman equation of rQ by construction. The chi-squared term keeps rQ from blowing up. 

What IQ-Learn does and does not pin down. The first two terms of (60) do not fix the state-only shaping ambiguity. Let c : S → R be bounded and define Qc(s, a ) := Q(s, a ) + c(s). Then 

VQc (s) = VQ(s) + c(s), rQc (s, a ) = rQ(s, a ) + c(s) − βE[c(s′) | s, a ].

The change in the reward term is, by the Bellman-flow identity (61), 

Edπ⋆ [rQc − rQ] = Edπ⋆

c(s) − βE[c(s′) | s, a ] = (1 − β)Eν0 [c(s0)] .

The initial-value term changes by exactly the same amount with the opposite sign: 

−(1 − β)Eν0 [VQc (s0) − VQ(s0)] = −(1 − β)Eν0 [c(s0)] .

38 Therefore the first two terms of IQ-Learn are invariant under every bounded state-only shift c,not merely under a scalar normalization. The regularizer −λE[ψ(rQ)] is the only part of (60) that selects a representative within the potential-shaped equivalence class, and that selection is a penalty preference rather than statistical identification of the ground-truth reward. Thus IQ-Learn can fit a softmax policy and define a Bellman-consistent reward by construction, but without an additional normalization such as the anchor action it does not identify a unique state-action reward. This is materially weaker than the anchor-action identification of Theorem 3.4, which fixes the shaping function rather than choosing a regularizer-preferred representative. 

5.4 Offline Maximum-Likelihood IRL (Zeng et al.) 

Zeng et al. (2023) propose offline ML-IRL . Stripped of vocabulary, the method is essentially a parametric, neural re-skinning of Hotz–Miller / CCP (Section 4.3) with two modifications: (a) the closed-form auxiliary inversion of Section 4.3 is replaced by an iterated forward-soft-RL inner loop with an explicit reward parametrization rθ, driven by an outer softmax-MLE objective; and (b) a conservatism penalty, analogous to Conservative Q-Learning (CQL) of Kumar et al. (2020), is added inside the soft-RL inner loop to handle distribution shift. Related conservative offline IRL methods appear in Yue et al. (2023). It is worth making the structural parallel to Hotz–Miller explicit, because it is the right way to understand what the method does and does not buy. Both methods (i) estimate ˆP from D, and (ii) alternate between a forward step that produces a value function from ˆP together with the current reward (or current value) and a fitting step that updates the reward (or the choice probabilities). CCP performs the forward step in closed form by solving the auxiliary linear fixed-point equation (32) and reading off r from (33) ; Zeng et al. instead solve a parametric soft-RL problem at each outer step. The substrate is the same; only the parametrization and the objective differ. The procedure is a doubly-nested alternation. At outer iteration k with current reward parameter 

θk:(1) Fit a transition model ˆPϕ from D by maximum likelihood (this is done once or refreshed periodically). (2) Inner loop: solve the soft-RL problem under ( rθk , ˆPϕ) for the soft optimal πk via soft value iteration or actor–critic with a CQL-style penalty added to the value function to discourage extrapolation beyond the support of D.(3) Outer update: θk+1 ← θk + η ∇θ bED[log ˆ pQθk (a | s)], where Qθk is the soft Q-function corre-sponding to πk.(4) Repeat until convergence. 39 The conservatism penalty is the one substantive new ingredient, and it is genuinely useful: it handles the unvisited states that vanilla CCP could not, by discouraging the inner-loop value from extrapolating wildly outside the support of D. Everything else inherits CCP’s structure, as well as CCP’s limitations: 

• It needs to estimate ˆPϕ in high dimensions, with the same statistical curse-of-dimensionality as CCP/Hotz–Miller. 

• The inner-loop policy fit must contend with the bias of ˆPϕ and the distribution-shift concerns that motivate the conservatism penalty. 

• The outer loop is nested optimization, which is the very thing we have been trying to avoid since Rust. Zeng et al. (2023) prove finite-time guarantees of the form ∥br − r∥22 ≤ O (N −1/2) + (model error) under linear-MDP assumptions and bounded transition-model error, but the analysis does not extend directly to neural function approximation without further structure. The honest takeaway is that offline ML-IRL inherits Hotz–Miller’s strengths and weaknesses essentially unchanged; conservatism is orthogonal to the underlying estimator and could in principle be bolted onto any method in this section, including the ERM framework of Section 6. 

Where modern IRL leaves us. None of the methods reviewed in this section gives all of the following at once: (a) A single (non-nested) gradient-based objective. (b) Provable global convergence in nonlinear function classes. (c) State-action reward recovery in continuous-state, stochastic-transition environments. (d) No transition-kernel estimation. The next section gives one route through this gap under explicit anchor, empirical Jacobian-conditioning/PL, realizability, and stability assumptions. 

# 6 The Empirical Risk Minimization Approach 

We finally reach the ERM framework of Kang, Yoganarasimhan, et al. (2025). The easiest way to understand the method is not to start with the minimax objective. Instead, start from the two restrictions that have appeared throughout the notes: 40 (i) the expert’s observed choices identify the within-state differences of Q⋆ through the softmax likelihood; (ii) the anchor-action Bellman equation fixes the remaining state-wise level of Q⋆.The ERM estimator is simply the one-shot version of these two restrictions. It parametrizes Q

directly, rather than parametrizing r and repeatedly solving the forward Bellman equation. This is the step that removes the nested r 7 → Q optimization that classical DDC methods require. Throughout this section, as denotes the anchor action at state s, and 

rA(s) := r(s, a s)denotes its known reward. The normalized presentation often sets rA(s) = 0 for all s; the formulation below allows any known anchor payoff, which is the notation used in the paper. The build-up is as follows. Section 6.1 rewrites the classical identification equations as losses. Section 6.2 combines them into the population expected risk. Section 6.3 explains why the empirical problem cannot be obtained by naively replacing the Bellman expectation by one next-state sample. Section 6.4 introduces the conditional-mean correction that removes that bias. Sections 6.5 and 6.6 state the resulting GLADIUS algorithm and its conditional convergence guarantees. 

6.1 From classical DDC equations to two losses 

Recall the two soft optimality equations. If 

VQ(s) := LSE( Q(s, ·)) = log X

> b∈A

exp( Q(s, b )) , ˆpQ(a | s) := exp( Q(s, a )) 

P 

> b∈A

exp( Q(s, b )) ,

then the expert policy and the optimal Q satisfy 

π⋆(a | s) = ˆ pQ⋆ (a | s), (62) 

Q⋆(s, a ) = r(s, a ) + βE[VQ⋆ (s′) | s, a ]. (63) The first equation is what likelihood-based estimation uses. The second equation is what dynamic programming, CCP, and Bellman-residual methods try to enforce. Under the anchor-action assumption, the key Magnac–Thesmar observation is that we do not need the Bellman equation for every action in order to identify Q⋆. The likelihood condition (62) identifies all within-state differences Q⋆(s, a ) − Q⋆(s, b ). It is only missing one scalar level per state. The anchor-action Bellman equation supplies exactly that missing scalar: 

Q⋆(s, a s) = rA(s) + βE[VQ⋆ (s′) | s, a s]. (64) 41 This is the bridge from classical DDC to ERM. Rust’s nested fixed-point method enforces the full Bellman equation after parametrizing r; Magnac–Thesmar identification says that, once the anchor is fixed, the likelihood plus the anchor-action Bellman equation is enough. Therefore, if we parametrize Q directly, the two identifying restrictions can be expressed as two loss-minimization problems. The first loss is the negative log-likelihood, 

LNLL (Q)( s, a ) := − log ˆ pQ(a | s). (65) By Lemma 4.1, its population minimizers are precisely the Q functions whose softmax equals the expert policy on the expert-covered states. The second loss is the anchor Bellman-error loss. For any candidate Q, define the anchor residual ∆A(Q)( s) := rA(s) + βE[VQ(s′) | s, a s] − Q(s, a s). (66) Then Q satisfies the anchor Bellman equation exactly when ∆ A(Q)( s) = 0. Because the expert softmax puts positive probability on every action at each covered state, this is equivalently the minimizer set of 

E(s,a )∼dπ⋆

1{a = as} ∆A(Q)( s)2 . (67) This is the main conceptual simplification: the Bellman part contains no unknown reward term, because it is evaluated only at the anchor action where rA(s) is known. 

6.2 Expected risk: the one-shot population problem 

Combining the two losses gives the population’s expected risk 

Rexp (Q) := E(s,a )∼dπ⋆

h

− log ˆ pQ(a | s) + 1{a = as}  rA(s) + βE[VQ(s′) | s, a s] − Q(s, a s)2i

. (68) This is the population ERM-DDC/IRL objective. It has the flow that the classical methods suggest, but without the classical nesting: 

fit the observed choices through NLL, and simultaneously fit the anchor Bellman equation through a squared residual. 

The NLL term has no unknown reward in it. The anchor Bellman term also has no unknown reward in it. Thus, at the population level, the inverse problem has become a direct optimization over Q.

Theorem 6.1 (Identification via expected risk minimization) . Under the anchor-action assumption and the softmax support condition, every minimizer of (68) agrees with Q⋆ on the expert-covered 

42 state-action support. Once Q⋆ is recovered, the reward is identified on that support by 

r(s, a ) = Q⋆(s, a ) − βE[VQ⋆ (s′) | s, a ]. (69) 

Equivalently, if 

ζ⋆Q(s, a ) := E[VQ(s′) | s, a ],

then 

r(s, a ) = Q⋆(s, a ) − βζ ⋆Q⋆ (s, a ).

Proof sketch. The NLL term is minimized exactly when ˆpQ(· | s) = π⋆(· | s) on the expert state support. The anchor Bellman term is nonnegative and is zero at Q⋆. Hence a minimizer of the sum must attain the minimum of the likelihood term and zero anchor Bellman error. These are exactly the two equations in the Magnac–Thesmar identification argument: the likelihood pins down within-state differences, and the anchor Bellman equation pins down the state-wise level. The reward formula is the Bellman rearrangement. 

Remark 6.2 (The support condition is automatic for softmax experts) . For a bounded Q⋆ and finite action set A, the softmax policy satisfies π⋆(a | s) > 0 for every action at every covered state. Thus the anchor action appears with positive population probability whenever the state is covered. 

6.3 Why squared TD is not the empirical Bellman error 

The expected risk (68) is still a population object. The hard term is 

E[VQ(s′) | s, a s],

which is an expectation over the transition kernel. If the transition kernel were known, one could evaluate this term directly. In the offline setting, however, we only observe samples ( s, a, s ′). The first tempting move is to replace the conditional expectation by the one observed next state. For a general state-action pair, define the sampled Bellman operator 

bT Q(s, a, s ′) := r(s, a ) + βV Q(s′), (70) and define the sampled TD residual 

b∆( Q)( s, a, s ′) := bT Q(s, a, s ′) − Q(s, a ). (71) At anchor actions, r(s, a ) is the known quantity rA(s). The sampled operator is unbiased for the Bellman operator: 

E[ bT Q(s, a, s ′) | s, a ] = ( T Q)( s, a ).

43 But the squared TD loss is not unbiased for the squared Bellman error. The variance decomposition gives 

E b∆( Q)( s, a, s ′)2 | s, a  =  (T Q)( s, a ) − Q(s, a )2 + β2 Var( VQ(s′) | s, a ). (72) The second term is not a harmless constant: it depends on Q. Therefore, minimizing squared TD error generally does not minimize squared Bellman error. This is the double-sampling problem in this setting. It disappears under deterministic transitions, but not under stochastic transitions. 

6.4 Bias correction and the minimax ERM formulation 

The correction is to subtract the conditional variance term in (72) without estimating the transition kernel. The variance has the least-squares representation Var( VQ(s′) | s, a ) = min  

> z∈R

E[( VQ(s′) − z)2 | s, a ], z⋆Q(s, a ) = E[VQ(s′) | s, a ]. (73) Promote the scalar minimizer to a function ζ(s, a ). Combining (72) and (73) yields the corrected Bellman-error identity 

 (T Q)( s, a ) − Q(s, a )2 = max  

> ζ

E

h b∆( Q)( s, a, s ′)2 − β2 VQ(s′) − ζ(s, a )2 | s, a 

i

. (74) The maximization appears because subtracting the best least-squares error is the same as maximizing the negative squared error. Plugging the corrected identity into the anchor-weighted expected risk gives the population minimax version of ERM-DDC/IRL: min  

> Q

max  

> ζ

E(s,a )∼dπ⋆ , s ′∼P (·| s,a )



− log ˆ pQ(a | s)+ 1{a = as}

 rA(s) + βV Q(s′) − Q(s, a )2 − β2 VQ(s′) − ζ(s, a )2

 

. (75) The anchor indicator is important. The Bellman-identification term is imposed at the anchor action because that is where the reward is known. The auxiliary function ζ, however, has an additional role: at the solution, 

ζ⋆Q⋆ (s, a ) = E[VQ⋆ (s′) | s, a ],

so it is exactly the continuation-value object needed to recover rewards for all supported state-action pairs: 

r(s, a ) = Q⋆(s, a ) − βζ ⋆Q⋆ (s, a ). (76) For this reason, the paper estimates ζ over observed state-action pairs, not only at anchor observations. The anchor-weighted corrected Bellman term identifies Q⋆; the all-pair conditional-mean regression makes reward recovery transition-estimation-free. 44 Replacing the population expectations in (75) and (73) by empirical averages gives the finite-sample objective. For parametrizations Qθ and ζϕ, define the auxiliary regression loss 

bFζ (ϕ; θ) := 1

N

X

> (s,a,s ′)∈D

 VQθ (s′) − ζϕ(s, a )2. (77) For fixed Qθ, descending this loss estimates the conditional mean E[VQθ (s′) | s, a ] on the observed support. Given a current auxiliary function, the empirical Q objective is 

bfN (θ, ϕ ) := 1

N

X

> (s,a,s ′)∈D



− log ˆ pQθ (a | s)+ 1{a = as}

 rA(s) + βV Qθ (s′) − Qθ(s, a )2 − β2 VQθ (s′) − ζϕ(s, a )2

 

.

(78) Equivalently, one may view the profiled empirical risk as 

bgN (θ) := bfN (θ, bϕ(θ)) , bϕ(θ) ∈ arg min 

> ϕ

bFζ (ϕ; θ).

This is the empirical-risk version of the expected-risk problem above. 

6.5 GLADIUS: alternating gradients for the corrected ERM 

GLADIUS solves the empirical problem by alternating the two natural gradient steps. The ζ step is a regression step: fit ζϕ(s, a ) to VQθ (s′). The Q step is a descent step on the NLL plus the anchor-weighted corrected Bellman term. 45 Algorithm 1 GLADIUS: Gradient-based Learning with Ascent–Descent for Inverse Utility learning from Samples 

Require: Dataset D = {(s, a, s ′)}, known anchor rewards rA(s) = r(s, a s), parametrizations Qθ

and ζϕ, stepsizes ηθ,t , η ϕ,t , iterations T . 

> 1:

Initialize θ0, ϕ 0. 

> 2:

for t = 0 , . . . , T − 1 do  

> 3:

Draw minibatches Bζ , BQ ⊆ D . 

> 4:

Update ζ by descending the auxiliary regression loss: 

ϕt+1 ← ϕt − ηϕ,t ∇ϕ



1

|B ζ |

X

> (s,a,s ′)∈B ζ

 VQθt (s′) − ζϕt (s, a )2

 . 

> 5:

Update Q by descending the corrected empirical risk: 

θt+1 ← θt − ηθ,t ∇θ bfBQ (θt, ϕ t+1 ). 

> 6:

end for  

> 7:

Set bQ := QθT and bζ := ζϕT . 

> 8:

Recover rewards on supported state-action pairs by 

br(s, a ) := bQ(s, a ) − β bζ(s, a ). 

> 9:

return bQ, bζ, br.The language “ascent–descent” comes from the minimax display (75) : maximizing the negative quadratic correction in ζ is equivalent to descending the positive regression loss (77) . The practical algorithm uses the regression form because it is clearer and numerically standard. 

Remark 6.3 (Deterministic transitions) . If s′ is deterministic conditional on (s, a ), then VQ(s′)

has zero conditional variance. The correction term in (74) is zero, so the ζ step is unnecessary for optimizing Q. In that special case, the empirical problem reduces to the NLL plus the anchor TD-squared term, and reward recovery is simply 

br(s, a ) = bQ(s, a ) − βV bQ(s′),

where s′ is the deterministic next state from (s, a ).

46 6.6 PL geometry and convergence 

The objective in (78) is generally nonconvex in θ. The paper’s convergence argument therefore relies on Polyak– Lojasiewicz geometry, not convexity. A differentiable function f satisfies a PL inequality with constant c > 0 on a set U if 12 ∥∇ f (x)∥22 ≥ c f (x) − f ⋆, x ∈ U. (79) PL is weaker than convexity but still rules out spurious stationary points on the relevant set. For the minimax objective, the relevant geometry is two-sided. The profiled Q objective must satisfy PL in θ, while the auxiliary regression problem must satisfy the corresponding PL condition in ϕ

for the ascent side (equivalently, the regression loss is PL for descent). Before stating the conditioning assumption, it is useful to say what kind of condition it is. We are not assuming directly that the nonconvex parameter-space objective is PL. Instead, the argument first proves PL-type inequalities in the finite empirical output coordinates, and then transfers them to the parameters through the Jacobians of the output maps 

θ 7 → (Qθ(z)) z∈ZQ , ϕ 7 → (ζϕ(¯ z)) ¯z∈Zζ .

This transfer condition is checkable for common parametrizations. For linear models it is exactly a full-row-rank empirical feature condition. For sufficiently wide smooth neural networks, the same type of lower bound is obtained in the usual NTK/lazy-training regime on any fixed finite empirical evaluation set. We state the assumption first and then record these two verification cases as lemmas. 

Assumption 6.4 (Empirical-output Jacobian conditioning) . Let ZQ(D) be the finite set of empirical state-action points at which Qθ is evaluated, and let Zζ (D) be the finite set at which ζϕ is evaluated. On finite-radius neighborhoods BQ and Bζ containing the iterates and empirical minimizers: (i) Qθ(z) and ζϕ( ¯z) are twice continuously differentiable with bounded first and second derivatives on the empirical evaluation sets. (ii) The empirical output Jacobian of Qθ is uniformly well conditioned: 

JQ(θ; ZQ)JQ(θ; ZQ)⊤ ⪰ μQI for all θ ∈ BQ.

(iii) The empirical output Jacobian of ζϕ is uniformly well conditioned: 

Jζ (ϕ; Zζ )Jζ (ϕ; Zζ )⊤ ⪰ μζ I for all ϕ ∈ Bζ .

Lemma 6.5 (Linear parametrizations satisfy Assumption 6.4) . Suppose 

Qθ(z) = θ⊤φ(z), ζϕ(¯ z) = ϕ⊤χ(¯ z).

47 Let ΦQ be the matrix with rows φ(z)⊤ for z ∈ ZQ(D), and let Xζ be the matrix with rows χ( ¯z)⊤ for 

¯z ∈ Zζ (D). If 

ΦQΦ⊤ 

> Q

⪰ μQI, Xζ X⊤ 

> ζ

⪰ μζ I

for some μQ, μ ζ > 0, and the feature evaluations on these finite empirical sets are bounded, then Assumption 6.4 holds. Proof sketch. For the linear Q class, (Qθ(z)) z∈ZQ = Φ Qθ, JQ(θ; ZQ) = Φ Q,

so the assumed Gram lower bound is exactly Assumption 6.4(ii). The first derivative is φ(z) and the second derivative is zero, so the smoothness and bounded-derivative part follows from bounded feature evaluations on the finite set ZQ. The same calculation with Xζ gives Assumption 6.4(iii) and the corresponding derivative bounds for ζϕ.

Lemma 6.6 (Wide smooth neural networks satisfy Assumption 6.4) . Fix the empirical evaluation sets ZQ(D) and Zζ (D). Consider sufficiently wide feedforward neural networks with C2 activations and standard over-parameterized random initialization. If the neural tangent Gram matrices at initialization on ZQ and Zζ have strictly positive minimum eigenvalues, and the SGDA iterates remain in a lazy-training neighborhood where the empirical output Jacobians move by less than half of these eigenvalue margins, then Assumption 6.4 holds with high probability. Proof sketch. On a fixed finite evaluation set, standard over-parameterized initialization gives well-conditioned empirical tangent kernels with high probability under nondegenerate data. Because the networks use C2 activations and the iterates stay in a finite-radius lazy-training neighborhood, first and second derivatives are uniformly bounded on the finite empirical sets. The small-movement condition implies the empirical Jacobian Gram matrices along the optimization path remain close to their initialization values. By Weyl’s inequality, losing at most half the initial spectral margin preserves positive lower bounds μQ and μζ throughout the balls BQ and Bζ .

Theorem 6.7 (Empirical PL guarantees) . Under Assumption 6.4, the empirical Q objective obtained after profiling the auxiliary correction satisfies a PL inequality on BQ. For each fixed θ ∈ BQ, the auxiliary regression loss bFζ (·; θ) also satisfies a PL inequality on Bζ ; equivalently, − bFζ (·; θ) satisfies the ascent-side PL condition. Proof sketch. The proof is not the generic claim that sums of PL functions are always PL; that claim is false. Here the two pieces control complementary directions. The NLL term controls within-state action-logit directions, while the anchor Bellman term controls the state-wise constant directions left invisible by the softmax likelihood. The empirical-output Jacobian lower bounds transfer these output-space inequalities to the parameter space. The auxiliary ζ problem is a quadratic regression 48 in empirical ζ outputs, so it has the corresponding PL geometry after the same Jacobian-conditioning transfer. 

Theorem 6.8 (GLADIUS convergence and population excess risk) . Suppose the realizability and empirical-output Jacobian-conditioning assumptions hold, and suppose the stochastic gradients use stepsizes ηt = c1/(c2 + t) satisfying the two-sided PL SGDA conditions. Then the GLADIUS iterates satisfy 

E

hbgN (bθT ) − bg⋆N

i

≤ νγ0 + T . (80) 

Moreover, combining the empirical optimization bound with the paper’s stability/generalization argument yields 

E

h

Rexp (QbθT ) − R exp (Q⋆)

i

≤ (1 + L/ρ )G ε N,T + νγ0 + T , (81) 

where 

εN,T = O (c2 + T )−α + CN , α := min 

 12 , 3cc 1

8



.

If the quantitative identification inequality for the full ERM-DDC/IRL risk also holds, then 

E

h

∥QbθT − Q⋆∥22,∗

i

= O (c2 + T )−α + O

 1

N



+ O

 1

T



.

Thus the L2(d⋆) estimation error has statistical contribution O(N −1/2) and, when α = 1 /2, opti-mization contribution O(T −1/4).

The main takeaway is now straightforward. ERM-DDC/IRL is a transition-estimation-free minimax estimator for Q⋆. It is built by translating the two classical identifying equations into losses, correcting the empirical Bellman term for double-sampling bias, and recovering rewards from the same learned conditional-mean correction bζ.

# Closing Remarks 

The classical DDC literature gave us identification through the anchor-action argument, plus computational templates such as NFXP and CCP. The modern IRL literature gave us scalable objectives, but often without pointwise Bellman identification of the reward. The ERM framework of Kang, Yoganarasimhan, et al. (2025) combines the useful pieces from both sides: use the likelihood term for within-state action-value differences, use the anchor Bellman equation for the state-wise level, use the bias-corrected TD identity to avoid transition-kernel estimation, and solve the resulting objective by alternating gradients. This is why the section should be read as a build-up from classical DDC to a one-shot gradient-based estimator, rather than as an isolated minimax formula. 49 A Deferred Proofs 

A.1 Local AIRL Gradient and Policy-Objective Identities 

For completeness, we give the precise finite-horizon version of Lemma 3.5. Work on a finite horizon 

H and suppress discounting; the discounted version inserts the weight βt in every time-t term. Write a transition as xt := ( st, a t, s t+1 ). For any policy π, write 

ρπt (s, a, s ′) := Pν0,π (st = s) π(a | s) P (s′ | s, a )for the time-t transition marginal, and define the policy’s pre-action transition base 

bπt (s, a, s ′) := Pν0,π (st = s) P (s′ | s, a ), ρπt (s, a, s ′) = bπt (s, a, s ′)π(a | s).

For a differentiable transition score fω(s, a, s ′), define the finite-horizon maximum-causal-entropy trajectory model 

pω(τ ) := 1

Zω

ν0(s0)

> H−1

Y

> t=0

P (st+1 | st, a t) exp 

H−1X

> t=0

fω(st, a t, s t+1 )



, (82) and let pω,t denote its transition marginal at time t. The maximum-causal-entropy log-likelihood is 

JMCE (ω) := Eν0,π ⋆ [log pω(τ )] .

For any sampler μt whose support contains that of pω,t , the same gradient can be written in importance-sampled form. AIRL’s local approximation replaces the intractable marginal pω,t by the policy-based energy measure 

bpπω,t (s, a, s ′) := exp( fω(s, a, s ′)) bπt (s, a, s ′), (83) and compares it with the policy transition marginal through 

bμπω,t (s, a, s ′) := 12 bpπω,t (s, a, s ′) + 12 ρπt (s, a, s ′). (84) The following proposition is the rigorous version of the localization statement used in the main text. 

Proposition A.1 (Finite-horizon local AIRL surrogate) . Fix the current policy π and a differentiable score fω. The maximum-causal-entropy gradient is 

∇ωJMCE (ω) = 

> H−1

X

> t=0

n

Ex∼ρπ⋆t [∇ωfω(x)] − Ex∼pω,t [∇ωfω(x)] 

o

, (85) 50 or equivalently 

∇ωJMCE (ω) = 

> H−1

X

> t=0



Eρπ⋆t [∇ωfω] − Eμt

 pω,t (x)

μt(x) ∇ωfω(x)

 

. (86) 

For each time t, let 

¯μπt (s, a, s ′) := 12 ρπ⋆ 

> t

(s, a, s ′) + 12 ρπt (s, a, s ′)

be the actual half-expert, half-policy transition-sampling mixture used by the discriminator. Define the transition discriminator Dπfω by (37) , equivalently by (38) , and define 

Jdisc (ω; π) := 

> H−1

X

> t=0

n

Ex∼ρπ⋆t [log Dπfω (x)] + Ex∼ρπt [log(1 − Dπfω (x))] 

o

. (87) 

Then 

∇ωJdisc (ω; π) = 

> H−1

X

> t=0

(

Eρπ⋆t [∇ωfω] − E¯μπt

" bpπω,t (x)

bμπω,t (x) ∇ωfω(x)

#) 

. (88) 

Thus, under the local sampler-consistency approximation ¯μπt = bμπω,t , the discriminator gradient is exactly the importance-sampled gradient obtained from (86) after replacing pω,t by bpπω,t . If the adapted policy sampler is consistent in the stronger sense that bpπω,t = pω,t , this is the maximum-causal-entropy IRL gradient (85) .Moreover, the discriminator log-odds reward 

brπf (s, a, s ′) := log Dπf (s, a, s ′) − log(1 − Dπf (s, a, s ′)) 

satisfies 

brπf (s, a, s ′) = f (s, a, s ′) − log π(a | s), (89) 

and therefore 

Eν0,π 

"H−1X

> t=0

brπf (st, a t, s t+1 )

#

= Eν0,π 

"H−1X

> t=0

 f (st, a t, s t+1 ) + H( π(· | st)) #

. (90) 

So optimizing the policy against the log-odds reward is exactly entropy-regularized policy optimization with reward f .Proof of Proposition A.1. First derive the maximum-causal-entropy gradient. From (82), log pω(τ ) = − log Zω + terms independent of ω +

> H−1

X

> t=0

fω(xt).

51 Therefore 

∇ωJMCE (ω) = 

> H−1

X

> t=0

Eρπ⋆t [∇ωfω(x)] − ∇ ω log Zω.

Differentiating the normalizer gives 

∇ω log Zω = Eτ ∼pω

"H−1X

> t=0

∇ωfω(xt)

#

=

> H−1

X

> t=0

Ex∼pω,t [∇ωfω(x)] ,

which proves (85). The importance-sampled form (86) follows by writing 

Ex∼pω,t [∇ωfω(x)] = Ex∼μt

 pω,t (x)

μt(x) ∇ωfω(x)



whenever μt dominates pω,t .We now prove the local discriminator identity. From (38), log Dπfω (x) = fω(x) − log  exp fω(x) + π(a | s),

and log(1 − Dπfω (x)) = log π(a | s) − log  exp fω(x) + π(a | s).

Substituting these two identities into (87) yields 

Jdisc (ω; π) = 

> H−1

X

> t=0

n

Eρπ⋆t [fω(x)] + Eρπt [log π(a | s)] 

− Eρπ⋆t

log  exp fω(x) + π(a | s) − Eρπt

log  exp fω(x) + π(a | s) o

.

The second term is independent of ω. Differentiating the remaining terms gives 

∇ωJdisc (ω; π) = 

> H−1

X

> t=0



Eρπ⋆t [∇ωfω(x)] − 2E¯μπt

 exp fω(x)exp fω(x) + π(a | s) ∇ωfω(x)

 

=

> H−1

X

> t=0

(

Eρπ⋆t [∇ωfω(x)] − E¯μπt

"

exp fω(x) 

> 12

exp fω(x) + 12 π(a | s) ∇ωfω(x)

#) 

.

Multiplying the numerator and denominator of the fraction by bπt (s, a, s ′), and using bpπω,t = exp (fω)bπt

and ρπt = π(a | s)bπt , gives exp fω(x) 

> 12

exp fω(x) + 12 π(a | s) = bpπω,t (x) 

> 12

bpπω,t (x) + 12 ρπt (x) = bpπω,t (x)

bμπω,t (x) ,

which proves (88) . The two sampler-consistency consequences follow immediately by comparing (88) with (86) and (85). 52 Finally, divide (38) by 1 − Dπf (s, a, s ′) = π(a | s)exp f (s, a, s ′) + π(a | s) .

This gives (89). Summing over a trajectory and taking expectation under π,

Eν0,π 

"H−1X

> t=0

brπf (st, a t, s t+1 )

#

= Eν0,π 

"H−1X

> t=0

f (st, a t, s t+1 ) − log π(at | st)

#

= Eν0,π 

"H−1X

> t=0

f (st, a t, s t+1 ) + H( π(· | st)) 

#

,

where the last equality uses Eat∼π(·| st)[− log π(at | st)] = H( π(· | st)). 

# References 

Adusumilli, K. and D. Eckardt (2019). “Temporal-Difference estimation of dynamic discrete choice models”. In: arXiv preprint arXiv:1912.09509 .Aguirregabiria, V. and P. Mira (2002). “Swapping the nested fixed point algorithm: A class of estimators for discrete Markov decision models”. In: Econometrica 70.4, pp. 1519–1543. – (2007). “Sequential estimation of dynamic discrete games”. In: Econometrica 75.1, pp. 1–53. Allen-Zhu, Z., Y. Li, and Z. Song (2019). “A Convergence Theory for Deep Learning via Over-Parameterization”. In: Proceedings of the 36th International Conference on Machine Learning .Vol. 97. Proceedings of Machine Learning Research. PMLR, pp. 242–252. Antos, A., C. Szepesvari, and R. Munos (2008). “Learning near-optimal policies with Bellman-residual minimization based fitted policy iteration and a single sample path”. In: Machine Learning 

71, pp. 89–129. Arcidiacono, P., P. Bayer, J. R. Blevins, and P. B. Ellickson (2016). “Estimation of dynamic discrete choice models in continuous time with an application to retail competition”. In: The Review of Economic Studies 83.3, pp. 889–931. Arcidiacono, P., P. Bayer, F. A. Bugni, and J. James (2013). “Approximating high-dimensional dynamic models: Sieve value function iteration”. In: Structural Econometric Models . Emerald Group Publishing Limited, pp. 45–95. Arcidiacono, P. and P. B. Ellickson (2011). “Practical methods for estimation of dynamic discrete choice models”. In: Annual Review of Economics 3.1, pp. 363–394. Arcidiacono, P. and R. A. Miller (2011). “Conditional choice probability estimation of dynamic discrete choice models with unobserved heterogeneity”. In: Econometrica 79.6, pp. 1823–1867. Barnes, M., M. Abueg, O. F. Lange, M. Deeds, J. Trader, D. Molitor, M. Wulfmeier, and S. O’Banion (2023). “Massively scalable inverse reinforcement learning in Google Maps”. In: arXiv preprint arXiv:2305.11290 .53 Barzegary, E. and H. Yoganarasimhan (2022). “A Recursive Partitioning Approach for Dynamic Discrete Choice Modeling in High Dimensional Settings”. In: arXiv preprint arXiv:2208.01476 .Bas-Serrano, J., S. Curi, A. Krause, and G. Neu (2021). “Logistic Q-learning”. In: International Conference on Artificial Intelligence and Statistics . PMLR, pp. 3610–3618. Benitez-Silva, H., G. Hall, G. J. Hitsch, G. Pauletto, and J. Rust (2000). “A comparison of discrete and parametric approximation methods for continuous-state dynamic programming problems”. In: manuscript, Yale University .Brockman, G. (2016). “OpenAI Gym”. In: arXiv preprint arXiv:1606.01540 .Cao, H., S. Cohen, and L. Szpruch (2021). “Identifiability in inverse reinforcement learning”. In: 

Advances in Neural Information Processing Systems 34, pp. 12362–12373. Chen, J. and N. Jiang (2019). “Information-theoretic considerations in batch reinforcement learning”. In: International Conference on Machine Learning . PMLR, pp. 1042–1051. Chernozhukov, V., J. C. Escanciano, H. Ichimura, W. K. Newey, and J. M. Robins (2022). “Locally robust semiparametric estimation”. In: Econometrica 90.4, pp. 1501–1535. Chiong, K. X., A. Galichon, and M. Shum (2016). “Duality in dynamic discrete-choice models”. In: 

Quantitative Economics 7.1, pp. 83–115. Dai, B., A. Shaw, L. Li, L. Xiao, N. He, Z. Liu, J. Chen, and L. Song (2018). “SBEED: Convergent reinforcement learning with nonlinear function approximation”. In: International Conference on Machine Learning . PMLR, pp. 1125–1134. Du, S. S., J. Chen, L. Li, L. Xiao, and D. Zhou (2017). “Stochastic variance reduction methods for policy evaluation”. In: International Conference on Machine Learning . PMLR, pp. 1049–1058. Du, S. S., X. Zhai, B. P´ oczos, and A. Singh (2019). “Gradient Descent Provably Optimizes Over-parameterized Neural Networks”. In: International Conference on Learning Representations . url :

https://openreview.net/forum?id=S1eK3i09YQ .Ermon, S., Y. Xue, R. Toth, B. Dilkina, R. Bernstein, T. Damoulas, P. Clark, S. DeGloria, A. Mude, C. Barrett, et al. (2015). “Learning large-scale dynamic discrete choice models of spatio-temporal preferences with application to migratory pastoralism in East Africa”. In: Proceedings of the AAAI Conference on Artificial Intelligence . Vol. 29. 1. Feng, Y., E. Khmelnitskaya, and D. Nekipelov (2020). “Global concavity and optimization in a class of dynamic discrete choice models”. In: International Conference on Machine Learning . PMLR, pp. 3082–3091. Finn, C., P. Christiano, P. Abbeel, and S. Levine (2016). “A connection between generative adversarial networks, inverse reinforcement learning, and energy-based models”. In: arXiv preprint arXiv:1611.03852 .Foster, D. J., A. Block, and D. Misra (2024). “Is behavior cloning all you need? Understanding horizon in imitation learning”. In: Advances in Neural Information Processing Systems 37, pp. 120602– 120666. Foster, D. J., A. Krishnamurthy, D. Simchi-Levi, and Y. Xu (2021). “Offline reinforcement learning: Fundamental barriers for value function approximation”. In: arXiv preprint arXiv:2111.10919 .54 Fu, J., K. Luo, and S. Levine (2017). “Learning robust rewards with adversarial inverse reinforcement learning”. In: arXiv preprint arXiv:1710.11248 .Garg, D., S. Chakraborty, C. Cundy, J. Song, and S. Ermon (2021). “IQ-Learn: Inverse soft-Q learning for imitation”. In: Advances in Neural Information Processing Systems 34, pp. 4028–4039. Geng, S., H. Nassif, C. Manzanares, M. Reppen, and R. Sircar (2020). “Deep PQR: Solving inverse reinforcement learning using anchor actions”. In: International Conference on Machine Learning .PMLR, pp. 3431–3441. Geng, S., H. Nassif, and C. A. Manzanares (2023). “A data-driven state aggregation approach for dynamic discrete choice models”. In: Uncertainty in Artificial Intelligence . PMLR, pp. 647–657. Goodfellow, I., J. Pouget-Abadie, M. Mirza, B. Xu, D. Warde-Farley, S. Ozair, A. Courville, and Y. Bengio (2020). “Generative adversarial networks”. In: Communications of the ACM 63.11, pp. 139–144. Guo, D., D. Yang, H. Zhang, J. Song, R. Zhang, R. Xu, Q. Zhu, S. Ma, P. Wang, X. Bi, et al. (2025). “Deepseek-r1: Incentivizing reasoning capability in LLMs via reinforcement learning”. In: arXiv preprint arXiv:2501.12948 .Ho, J. and S. Ermon (2016). “Generative adversarial imitation learning”. In: Advances in Neural Information Processing Systems 29. Hotz, V. J. and R. A. Miller (1993). “Conditional choice probabilities and the estimation of dynamic models”. In: The Review of Economic Studies 60.3, pp. 497–529. Imai, S., N. Jain, and A. Ching (2009). “Bayesian estimation of dynamic discrete choice models”. In: Econometrica 77.6, pp. 1865–1899. Jacot, A., F. Gabriel, and C. Hongler (2018). “Neural Tangent Kernel: Convergence and General-ization in Neural Networks”. In: Advances in Neural Information Processing Systems . Vol. 31, pp. 8580–8589. Jiang, N., A. Krishnamurthy, A. Agarwal, J. Langford, and R. E. Schapire (2017). “Contextual decision processes with low Bellman rank are PAC-learnable”. In: International Conference on Machine Learning . PMLR, pp. 1704–1713. Jiang, N. and T. Xie (2024). “Offline Reinforcement Learning in Large State Spaces: Algorithms and Guarantees”. In: Statistical Science .Kang, E. H. and K. Jang (2025a). “Stability and Generalization for Bellman Residuals”. In: arXiv preprint arXiv:2508.18741 .– (2025b). Stability and Generalization for Bellman Residuals . arXiv: 2508.18741 [cs.LG] . url :

https://arxiv.org/abs/2508.18741 .Kang, E. H., H. Yoganarasimhan, and L. Jain (2025). An Empirical Risk Minimization Approach for Offline Inverse RL and Dynamic Discrete Choice Model . arXiv: 2502.14131 [cs.LG] . url :

https://arxiv.org/abs/2502.14131 .Karmakar, P. and S. Bhatnagar (2018). “Two time-scale stochastic approximation with controlled Markov noise and off-policy temporal-difference learning”. In: Mathematics of Operations Research 

43.1, pp. 130–151. 55 Kasahara, H. and K. Shimotsu (2009). “Nonparametric identification of finite mixture models of dynamic discrete choices”. In: Econometrica 77.1, pp. 135–175. Kodinariya, T. M., P. R. Makwana, et al. (2013). “Review on determining number of Cluster in K-Means Clustering”. In: International Journal 1.6, pp. 90–95. Kostrikov, I., O. Nachum, and J. Tompson (2019). “Imitation learning via off-policy distribution matching”. In: arXiv preprint arXiv:1912.05032 .Kristensen, D., P. K. Mogensen, J. M. Moon, and B. Schjerning (2021). “Solving dynamic discrete choice models using smoothing and sieve methods”. In: Journal of Econometrics 223.2, pp. 328– 360. Kumar, A., A. Zhou, G. Tucker, and S. Levine (2020). “Conservative Q-learning for offline re-inforcement learning”. In: Advances in Neural Information Processing Systems 33, pp. 1179– 1191. Lagoudakis, M. G. and R. Parr (2003). “Least-squares policy iteration”. In: Journal of Machine Learning Research 4.Dec, pp. 1107–1149. Levine, S., A. Kumar, G. Tucker, and J. Fu (2020). “Offline reinforcement learning: Tutorial, review, and perspectives on open problems”. In: arXiv preprint arXiv:2005.01643 .Li, Z., T. Xu, Y. Yang, and Z. -Q. Luo (2022). “Rethinking ValueDice - Does It Really Improve Performance?” In: ICLR Blog Track . url : https://iclr-blog-track.github.io/2022/03/25/ rethinking-valuedice/ .Liu, B., J. Liu, M. Ghavamzadeh, S. Mahadevan, and M. Petrik (2015). “Finite-sample analysis of proximal gradient TD algorithms”. In: Proceedings of the Thirty-First Conference on Uncertainty in Artificial Intelligence , pp. 504–513. Liu, C., L. Zhu, and M. Belkin (2022). “Loss landscapes and optimization in over-parameterized non-linear systems and neural networks”. In: Applied and Computational Harmonic Analysis 59, pp. 85–116. Maei, H. R. (2011). “Gradient temporal-difference learning algorithms”. PhD thesis. University of Alberta. Magnac, T. and D. Thesmar (2002). “Identifying dynamic discrete decision processes”. In: Econo-metrica 70.2, pp. 801–816. McFadden, D. (2001). “Economic choices”. In: American Economic Review 91.3, pp. 351–378. Munos, R. (2003). “Error bounds for approximate policy iteration”. In: Proceedings of the Twentieth International Conference on Machine Learning , pp. 560–567. Ng, A. Y., D. Harada, and S. Russell (1999). “Policy invariance under reward transformations: Theory and application to reward shaping”. In: International Conference on Machine Learning .Vol. 99, pp. 278–287. Ni, T., H. Sikchi, Y. Wang, T. Gupta, L. Lee, and B. Eysenbach (2021). “f-IRL: Inverse reinforcement learning via state marginal matching”. In: Conference on Robot Learning . PMLR, pp. 529–551. Norets, A. (2009). “Inference in dynamic discrete choice models with serially correlated unobserved state variables”. In: Econometrica 77.5, pp. 1665–1682. 56 Norets, A. (2012). “Estimation of dynamic discrete choice models using artificial neural network approximations”. In: Econometric Reviews 31.1, pp. 84–106. Patterson, A., A. White, and M. White (2022). “A generalized projected Bellman error for off-policy value estimation in reinforcement learning”. In: Journal of Machine Learning Research 23.145, pp. 1–61. Rafailov, R., J. Hejna, R. Park, and C. Finn (2024). “From r to Q*: Your Language Model is Secretly a Q-Function”. In: arXiv preprint arXiv:2404.12358 .Raffin, A., A. Hill, A. Gleave, A. Kanervisto, M. Ernestus, and N. Dormann (2021). “Stable-Baselines3: Reliable reinforcement learning implementations”. In: Journal of Machine Learning Research 22.268, pp. 1–8. Rajaraman, N., L. Yang, J. Jiao, and K. Ramchandran (2020). “Toward the fundamental limits of imitation learning”. In: Advances in Neural Information Processing Systems 33, pp. 2914–2924. Reich, G. (2018). “Divide and conquer: recursive likelihood function integration for hidden Markov models with continuous latent variables”. In: Operations Research 66.6, pp. 1457–1470. Rudelson, M. and R. Vershynin (2009). “Smallest singular value of a random rectangular matrix”. In: Communications on Pure and Applied Mathematics 62.12, pp. 1707–1739. Rust, J. (1987). “Optimal replacement of GMC bus engines: An empirical model of Harold Zurcher”. In: Econometrica , pp. 999–1033. – (1994). “Structural estimation of Markov decision processes”. In: Handbook of econometrics 4, pp. 3081–3143. Schulman, J., F. Wolski, P. Dhariwal, A. Radford, and O. Klimov (2017). “Proximal policy opti-mization algorithms”. In: arXiv preprint arXiv:1707.06347 .Sinaga, K. P. and M. -S. Yang (2020). “Unsupervised K-means clustering algorithm”. In: IEEE Access 8, pp. 80716–80727. Su, C. -L. and K. L. Judd (2012). “Constrained optimization approaches to estimation of structural models”. In: Econometrica 80.5, pp. 2213–2230. Sutton, R. S. and A. G. Barto (2018). Reinforcement learning: An introduction .Sutton, R. S., H. R. Maei, D. Precup, S. Bhatnagar, D. Silver, C. Szepesv´ ari, and E. Wiewiora (2009). “Fast gradient-descent methods for temporal-difference learning with linear function approximation”. In: Proceedings of the 26th Annual International Conference on Machine Learning . ACM, pp. 993– 1000. Sutton, R. S., C. Szepesv´ ari, and H. R. Maei (2009). “A convergent O(n) algorithm for off-policy temporal-difference learning with linear function approximation”. In: Advances in Neural Information Processing Systems . Vol. 21, pp. 1609–1616. Tesauro, G. et al. (1995). “Temporal difference learning and TD-Gammon”. In: Communications of the ACM 38.3, pp. 58–68. Torabi, F., G. Warnell, and P. Stone (2018). “Behavioral cloning from observation”. In: arXiv preprint arXiv:1805.01954 .57 Tsitsiklis, J. N. and B. Van Roy (1996a). “Analysis of temporal-difference learning with function approximation”. In: Advances in Neural Information Processing Systems 9. – (1996b). “Feature-based methods for large scale dynamic programming”. In: Machine Learning 

22.1, pp. 59–94. Uehara, M., J. Huang, and N. Jiang (2020). “Minimax weight and Q-function learning for off-policy evaluation”. In: International Conference on Machine Learning . PMLR, pp. 9659–9668. Van Hasselt, H., Y. Doron, F. Strub, M. Hessel, N. Sonnerat, and J. Modayil (2018). “Deep reinforcement learning and the deadly triad”. In: arXiv preprint arXiv:1812.02648 .Wang, R., Y. Wu, R. Salakhutdinov, and S. Kakade (2021). “Instabilities of offline RL with pre-trained neural representation”. In: International Conference on Machine Learning . PMLR, pp. 10948–10960. Xie, T. and N. Jiang (2021). “Batch value-function approximation with only realizability”. In: 

International Conference on Machine Learning . PMLR, pp. 11404–11313. Yang, J., N. Kiyavash, and N. He (2020). “Global convergence and variance-reduced optimization for a class of nonconvex-nonconcave minimax problems”. In: arXiv preprint arXiv:2002.09621 .Yang, Y. H. F. (2024). “Estimation of Dynamic Discrete Choice Models with Unobserved State Variables Using Reinforcement Learning”. In. Yue, S., G. Wang, W. Shao, Z. Zhang, S. Lin, J. Ren, and J. Zhang (2023). “Clare: Conserva-tive model-based reward learning for offline inverse reinforcement learning”. In: arXiv preprint arXiv:2302.04782 .Zanette, A. (2023). “When is realizability sufficient for off-policy reinforcement learning?” In: 

International Conference on Machine Learning . PMLR, pp. 40637–40668. Zare, M., P. M. Kebria, A. Khosravi, and S. Nahavandi (2024). “A survey of imitation learning: Algorithms, recent developments, and challenges”. In: IEEE Transactions on Cybernetics .Zeng, S., C. Li, A. Garcia, and M. Hong (2023). “Understanding expertise through demonstrations: A maximum likelihood framework for offline inverse reinforcement learning”. In: arXiv preprint arXiv:2302.07457 .Zhan, S. S., Q. Wu, P. Wang, Y. Wang, R. Jiao, C. Huang, and Q. Zhu (2024). “Model-Based Reward Shaping for Adversarial Inverse Reinforcement Learning in Stochastic Environments”. In: 

arXiv preprint arXiv:2410.03847 .Zhan, W., B. Huang, A. Huang, N. Jiang, and J. Lee (2022). “Offline reinforcement learning with realizability and single-policy concentrability”. In: Conference on Learning Theory . PMLR, pp. 2730–2775. Zhong, H., G. Feng, W. Xiong, X. Cheng, L. Zhao, D. He, J. Bian, and L. Wang (2024). “DPO meets PPO: Reinforced token optimization for RLHF”. In: arXiv preprint arXiv:2404.18922 .Zolna, K., A. Novikov, K. Konyushkova, C. Gulcehre, Z. Wang, Y. Aytar, M. Denil, N. de Freitas, and S. Reed (2020). “Offline learning from demonstrations and unlabeled experience”. In: arXiv preprint arXiv:2011.13885 .58