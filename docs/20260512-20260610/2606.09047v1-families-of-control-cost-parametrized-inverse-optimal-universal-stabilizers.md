---
title: Families of Control-Cost-Parametrized Inverse-Optimal Universal Stabilizers
title_zh: 控制成本参数化的逆最优通用稳定器族
authors: "Miroslav Krstic, Luke Bhan"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09047v1"
tags: ["query:inverse-rl"]
score: 10.0
evidence: 逆最优控制与成本参数化
tldr: 该工作针对经典通用镇定公式缺乏设计自由度的问题，提出一族控制成本参数化的反馈律。用户通过指定控制运行成本函数，经由三步构造（成本微分、函数求逆等）得到非线性扩展器，该扩展器可求解具有有意义状态成本的无限时域最优控制问题。证明Cost-to-expander算子具有Lipschitz连续性，支持用神经算子均匀逼近整个族，实现离线性能探索与在线自适应。在近似下建立了半全局实际渐近稳定性和二阶次优性界，数值实验验证了方法有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09047v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 471, \"height\": 858, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09047v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1580, \"height\": 1104, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.09047v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1843, \"height\": 1063, \"label\": \"Figure\"}]"
motivation: 经典通用镇定公式为零参数，缺乏设计灵活性；需要一种允许用户自由选择控制成本并仍保持最优性的框架。
method: 提出Cost-to-expander算子，通过三步构造将用户指定的控制运行成本映射为扩展的反馈律，并用Lipschitz连续的神经算子逼近整个族。
result: 在神经算子近似下，闭环系统达到半全局实际渐近稳定，且满足二阶次优性界，数值结果验证了方法的有效性。
conclusion: 所提“半直接最优”设计介于直接最优与逆最优之间，兼具灵活性与理论保证，为自适应最优镇定提供新途径。
---

## 摘要
经典的通用稳定化公式不提供设计自由度：它是一个单一的、无参数的对象。我们引入了一个由成本参数化的稳定反馈律族，其中（1）用户选择一个函数，该函数在逆最优成本泛函中作为控制的运行成本，（2）通过一个公式获得一个已有的通用控制器的非线性“扩展器”，该扩展器解决了一个具有有意义的成本的状态的无限时域最优控制问题。成本到扩展器公式是一个三步构造，涉及成本微分和函数反演——总体上是非线性的无限维算子。证明了成本到扩展器算子是Lipschitz的，这使得整个族的均匀神经算子逼近成为可能，并支持离线性能探索和在线自适应。在逼近下建立了半全局实际渐近稳定性和二阶次优性界。通过数值示例说明了算子学习及其在半全局稳定化中的应用。我们将结果称为“半直接最优”，因为本文的设计不如一般的“直接最优”（导致HJB）控制，但比完全的逆最优更多，因为用户对任意给定的控制成本进行最小化。我们解决的半直接问题的对偶是状态成本任意且给定的问题。这个对偶问题更简单，不在本文讨论范围内。

## Abstract
A classical universal stabilization formula offers the practitioner no design freedom: it is a single, parameter-free object. We introduce a cost-parametrized family of stabilizing feedback laws, where (1) the user chooses a function that serves as the running cost on control in an inverse-optimal cost functional, and (2) obtains, through a formula, a nonlinear "expander" of a pre-existing universal controller, which solves an infinite-horizon optimal control problem with a meaningful cost on the state. The cost-to-expander formula is a three-step construction, involving, inter alia, cost differentiation and function inversion-overall, a nonlinear infinite-dimensional operator. The cost-to-expander operator is proven Lipschitz, which enables uniform neural operator approximation of the entire family and supports both offline performance exploration and online adaptation. Semiglobal practical asymptotic stability and second-order suboptimality bounds are established under the approximation. The operator learning and its use in semiglobal stabilization are illustrated numerically. We call the result 'half-direct-optimal' because the paper's design is less than a general 'direct optimal' (HJB-inducing) control, but more than the fully inverse optimal, since the user performs minimization for an arbitrary given cost on control. The dual to the half-direct problem we solve is the problem in which the cost on the state is arbitrary and given. This dual problem is easier and outside of the scope of the paper.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：经典的Sontag通用镇定公式虽然理论上优雅，但**缺乏设计自由度**（零参数），工程师无法直接调节瞬态性能或控制代价。Curtis–Beard公式虽提供了两个状态依赖函数的完全参数化，但设计空间过大且难以直观解释，实际调参困难。
- **核心问题**：如何构造一族**用户可指定控制运行成本**、同时保证**逆最优性**和**易用性**的稳定反馈律？即，让设计者通过选择一个直观的成本函数γ（而非复杂的辅助函数）来生成一个可解释的、最优的控制器族。
- **整体含义**：本文提出“半直接最优”设计——介于完整的直接最优（HJB求解）和完全的逆最优（事后反推代价）之间。用户事先给定控制成本γ，系统自动导出对应的逆最优控制器，兼具理论严谨性和工程可操作性。

## 2. 方法论：核心思想、关键技术细节、公式流程

### 核心思想
- 设计一个**映射算子** \( \mathcal{K}: \gamma \mapsto \kappa \)，其中 \(\gamma\) 是用户指定的控制运行成本函数，\(\kappa\) 是**扩展器函数**。该算子通过三步构造：**微分 → 代数映射 → 函数求逆**。
- 得到的扩展器 \(\kappa\) 用于调整已有的半Sontag名义控制器 \(u_0\)，得到最终反馈律：
  \[
  u^*(x) = \operatorname{sgn}(u_0(x))\, \kappa(|u_0(x)|)
  \]
- 该反馈律是以下无限时域代价泛函的**唯一最小化器**：
  \[
  J(u) = \int_0^\infty \left[ q(x(t)) + r(x(t)) \, \gamma(|u(t)|) \right] dt
  \]
  其中 \(q(x)\) 和 \(r(x)\) 由 \(\gamma\) 和CLF自动导出。

### 关键技术细节（三步构造）
1. **成本微分**：对 \(\gamma\) 求导得到 \(\gamma'\)。
2. **代数映射**：定义承包函数 \(\Theta(s) = s - \frac{\gamma(s)}{\gamma'(s)}\)。当 \(\gamma\) 满足凸性、\(\gamma'(0)=0\)、\(\gamma''>0\) 时，\(\Theta\) 为 \(K_\infty\) 类函数。
3. **函数求逆**：扩展器 \(\kappa = \Theta^{-1}\)。等价表达为：
   \[
   \kappa(s) = (\ell_\gamma)' \circ \left( \frac{\ell_\gamma}{\mathrm{Id}} \right)^{-1}(s)
   \]
   其中 \(\ell_\gamma\) 是Legendre-Fenchel变换。

### 性质与理论保证
- **Lipschitz连续性**：映射 \(\mathcal{K}\) 在紧集上关于 \(C^1\) 拓扑是Lipschitz的，界由 \(\gamma\) 的导数和曲率决定。
- **神经算子逼近**：Lipschitz性保证可用傅立叶神经算子（FNO）对整个族进行**均匀逼近**（Proposition 1）。
- **稳定性与次优性**（Theorem 2）：在神经算子逼近误差 \(\varepsilon\) 下，闭环系统保持**半全局实际渐近稳定**（SPAS），且诱导状态代价的偏差为 \(O(\varepsilon^2)\)（二阶次优性）。
- **有限时域最优性**（Proposition 2）：用近似控制器时，带终端惩罚的有限时域代价与最优代价之差为 \(O(\varepsilon^2 T)\)。

## 3. 实验设计：数据集/场景、Benchmark、对比方法

- **系统场景**：单轮车模型（unicycle），通过极坐标变换和双向反步法构建严格CLF。
- **控制成本函数族**：采用 Example 1 中的非线性 \(\gamma(s) = s \ln(1+s) + \alpha / \ln(e+1/s)\)，其中 \(\alpha\) 在 \([10^{-4}, 10^4]\) 内对数均匀采样，生成200组 \((\gamma, \kappa)\) 训练数据。
- **神经算子**：使用**傅立叶神经算子（FNO）**，输入为 \([\gamma(s), s]\)（1024点网格），输出为 \(\hat{\kappa}(s)\)（1024点）。架构：2层隐藏层，每层32个神经元，16个傅立叶模式。
- **对比方法**：
  - **精确逆最优控制器** \(u^*\)：\(\kappa\) 通过逐点二分法精确计算。
  - **FNO近似控制器** \(\hat{u}^*\)：\(\kappa\) 用训练好的FNO前向推理+线性插值。
  - **名义控制器** \(u_{S/2}\)：即半Sontag公式（对应 \(\gamma(s)=s^2, \kappa(s)=2s\)）。
- **评价指标**：闭环轨迹（xy位置）、控制输入幅值和、CLF数值、有限时域代价 \(J^*\)（含终端代价）。

## 4. 资源与算力

- **未明确说明**：文中未报告所使用的GPU型号、数量、训练时长等具体硬件信息。
- **仅提及架构**：FNO包含2层、32神经元、16个傅立叶模式，训练集200个样本，验证集相对MSE为0.15。
- **推测**：这类小规模FNO在单GPU（如V100或A100）上数分钟至数小时内可完成训练。

## 5. 实验数量与充分性

- **实验组数**：主要实验为**一组**单轮车闭环仿真（初始条件 \((1,1,\pi/4)\)）。对比了三种控制器（精确、FNO近似、名义），展示了轨迹、控制输入、CLF和代价对比图。
- **消融实验**：**无**。未改变系统参数、不同CLF、不同\(\gamma\)形状、不同逼近误差水平或不同初始条件进行系统消融。
- **充分性评价**：作为概念验证论文，实验**基本充分**，验证了理论核心结论（FNA能近似\(\kappa\)，闭环稳定，代价接近最优）。但缺乏泛化性测试（如多系统、多\(\gamma\)、噪声扰动），客观性一般。

## 6. 主要结论与发现

- 成功构建了**控制成本参数化的逆最优稳定器族**，用户通过选择 \(\gamma\) 即可获得对应的最优反馈律。
- 映射 \(\gamma \mapsto \kappa\) 是**Lipschitz**的，从而可用神经算子**均匀逼近**，实现“一次离线训练，任意在线选择”。
- 在神经算子逼近下，闭环系统保持**半全局实际渐近稳定**，且次优性为**二阶**（误差平方量级）。
- 数值算例表明：FNO近似控制器与精确最优控制器的轨迹**几乎不可区分**，且比名义半Sontag控制器**收敛更快、代价更低**。
- 论文标题为“半直接最优”，既非完全直接最优（需解HJB），也非完全逆最优（事后分析代价），而是中间路线——**控制成本在先，状态成本在后**。

## 7. 优点

- **理论完备**：同时证明了映射的Lipschitz性、稳定性（SPAS）、次优性界（二阶）、有限时域最优性差距。
- **设计直观**：设计自由度从两个不易解释的函数（Curtis–Beard）简化为一个直观的**控制成本函数** \(\gamma\)，工程师可依据对控制代价的偏好直接选择。
- **可学习性强**：Lipschitz性保证了神经算子逼近的理论可行性，且仅需200组数据即可训练，实用化门槛低。
- **在线自适应潜力**：神经算子可离线训练，在线快速推理，支持实时控制器切换或自适应调整。

## 8. 不足与局限

- **实验覆盖有限**：仅测试了单轮车系统，未验证于其他非线性系统（如机械臂、航天器、化学反应器等），也未考虑噪声、模型不确定性或执行器饱和。
- **缺乏与Curtis–Beard参数化的直接比较**：文中虽提及Curtis–Beard更难使用，但未在相同场景下定量对比其与所提方法的性能差异。
- **\(\gamma\)的选择指导缺失**：无系统性分析或工具指导用户如何根据性能需求选择具体的 \(\gamma\)（如闭式关系或经验准则）。
- **紧集假设的现实性**：Lipschitz和逼近定理依赖于紧集上的均匀条件，实际中需验证CLF水平集是否包含期望的工作区域。
- **计算资源不透明**：未报告训练和推理的具体耗时及硬件信息，不利于可重复性评估。
- **无消融研究**：未分析不同\(\gamma\)形状、不同逼近误差对稳定性和代价的具体影响，也未测试不同FNO架构或超参数的鲁棒性。

（完）
